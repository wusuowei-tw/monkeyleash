# 規則清冊(唯讀盤點)—— agent-gates

**日期**:2026-08-16
**票**:45
**性質**:**唯讀盤點。不修任何東西。** 發現的死規則各自登記,修復另開票。
**跑這份的機器**:**桌機**。個人 pattern 清單 **17 條**
(`<家目錄>\.claude\leak-patterns.local.txt`,**不進版控、跟人走**)。
**筆電那台的第 4/5 欄可能不同,這份不代表它。**

---

# ⚠ 頭條結論:第 4 欄多數不可回答,而原因是**沒有東西在記**

**這不是「查過沒有」,是「沒有東西可查」。兩者對決策的意義相反。**

- 「查過沒有」→ 規則活著但還沒遇到違規 → 可以繼續等
- 「沒有東西可查」→ **規則有沒有攔過,這個 repo 沒有能力回答** → 等下去不會變清楚

實測:本 repo **沒有通用的攔截帳本**。

| 帳本 | 狀態 |
|---|---|
| `.dev/gate-exemptions.jsonl` | 121 筆,但它是**豁免帳本**;`outcome`:granted **80** / blocked **1**;`blocked_by`:**R3 × 1**,其餘全 `None` |
| `.dev/shadow-log.jsonl` | **不存在**(成因見附錄一,**不是死規則**) |
| `.dev/test-runs.jsonl` | 2781 筆 —— 紅燈紀錄,**不記攔截** |

**全 repo 唯一一筆有紀錄的實際攔截是 R3。**

為什麼會這樣:**擋下訊息走 stderr → 對話 → 從不落地成檔案。**
而閘門只看得到檔案(F-092 的同一句話,套在它自己身上)。
所以本清冊的第 4 欄,對 R1/R2/R4/R5/R6/R7/R8 一律標
**「無帳本可查」**,而不是「否」。

---

# ⚠ 頭條結論之二:**連「當時規則是什麼設定」也沒有紀錄**

第一條頭條只講了一半。**事後要重現任何一次判定,兩邊的證據都不在**:

| 缺的那一半 | 內容 |
|---|---|
| **判定的結果** | 沒有攔截帳本(頭條一) |
| **判定的前提** | **決定規則行為的設定檔,幾乎全部不進版控** |

F-083 掃過一遍(不只裁決者列的六處,把守衛程式讀的**所有**路徑常數都撈出來對版控狀態),
實得 **9 處**:

| # | 檔案 | 決定什麼 | 版控狀態 |
|---|---|---|---|
| 1 | `.agents/legacy-no-redlight.txt` | **R6 的 go-live 錨 + 豁免名單本身** | gitignored |
| 2 | `.dev/pipeline.json` | **R2 的站別、R3 的票號** | gitignored |
| 3 | `.dev/shadow.json` | 影子模式開沒開 | gitignored(**且不存在**) |
| 4 | **`.dev/test-runs.jsonl`** | **R3 的紅燈紀錄** | gitignored |
| 5 | **`.cache/mount-check.json`** | R4 的掛載快取 | gitignored |
| 6 | `~/.claude/shadow-clamp.txt` | 影子安全閥 | **使用者層,不在任何 repo** |
| 7 | `~/.claude/g1-protected.txt` | **G1 第一級的保護清單** | 同上 |
| 8 | `~/.claude/leak-patterns.local.txt` | **leak_scan 一半的涵蓋** | 同上 |
| 9 | **`~/.claude/upstream-roots.txt`** | **R3 provenance 的上游指標** | 同上 |

**第 4、5、9 是這次 F-083 掃出來的,不在原本那六處裡。**
第 9 特別要緊:它決定「上游是哪一個 repo」,而 provenance 豁免整條建立在它之上。

**唯一的例外是 `.dev/gate-exemptions.jsonl` —— 它進版控**,
而那正是本 repo 唯一有攔截紀錄的來源(頭條一那一筆 R3)。
**兩件事是同一件事的兩面:進版控的那一個,剛好就是唯一留得下證據的那一個。**

## 合起來的意思

> **每一次判定,都是「一份不進版控的設定」對「一份不進版控的輸入」做出的,
> 而結果也不進版控。** 三樣缺一就無法複現,而現在**三樣都缺**。

這不是「紀錄不夠詳細」,是**判定在原則上不可事後複驗**。
與 F-091「驗證的判準必須與程式實際的選法一致」是同一個方向的問題,
只是換成時間軸:**當時的判準是什麼,現在已經問不出來了。**

## 補述(2026-08-16):「輸入」那一樣**不是均勻缺失的**

上面說「設定 / 輸入 / 結果三樣都缺」。**「輸入」那一樣要拆開看,而拆開之後
結論會變得可操作。**

| 判定結果 | 它的輸入後來去了哪 |
|---|---|
| **通過** | 那份輸入變成了 commit —— **留得下來,而且進了版控** |
| **被擋(pre-commit)** | 那份輸入**從未落地**,工作樹隨後被改掉 |
| **被擋(前哨)** | **那筆寫入根本沒有發生過** —— 連工作樹都沒進去 |

> **輸入唯一留不下來的情況,正是最想事後複驗的那一種。**

通過的判定事後隨時查得到:`git show <commit>` 就是當時的輸入。
被擋的判定則什麼都沒有 —— 而**「為什麼那次被擋」正是會被爭論、
會被拿來質疑規則、會被當成放寬規則理由的那一種**。

### 這讓將來的機制範圍小很多

要記的不是「所有判定」,是「**被擋的判定**」:

- 通過的那些**已經有紀錄了**(就是 commit 本身),再記一次是重複
- 被擋的那些**一筆都沒有**,而它們是全部的爭議來源
- 量級差很多:本 repo 96 個 commit vs 未知但顯然少得多的攔截次數

**範圍從「每一次判定」縮到「每一次擋下」**,而那正好也是
頭條一(沒有攔截帳本)缺的那一半 —— **兩條頭條指向同一個東西**。

### 與最小可用一步的關係

上面登記四寫的最小步是「**判定時把當時的設定指紋寫進那筆紀錄**」。
配上這一節:那筆紀錄只需要在**被擋時**產生,而它要帶三樣 ——
**設定指紋 + 被拒的輸入 + 擋下的理由**。三樣同處,事後才複驗得動。

---

# 清冊本文

三態:**(a)** 擋過(有紀錄) / **(b)** 沒擋過但有正對照 / **(c)** 沒擋過也沒驗過。
額外標 **(b−)**:有測試,但測的是**規則本身**,沒測**餵給它的輸入**。

## R1 — 規格書不得含程式碼

| 欄 | 內容 |
|---|---|
| **1 守什麼** | `docs/specs/**` 內容含 ``` / `def` / `import` / `function` 即擋 |
| **2 掛哪層** | **兩層**(`check()`,前哨與 pre-commit 共用) |
| **3 輸入怎麼算** | 路徑經 `rel()`(`abspath` → 相對 `ROOT` → 分隔符正規化);`ROOT` 由 `__file__` 上推三層。pre-commit 的清單來自 `staged_paths()` —— **見下方共通風險 A** |
| **4 攔截紀錄** | **無帳本可查** |
| **5 正對照** | ✅ 3 條,含 `test_a_spec_that_cannot_be_read_is_blocked_not_allowed`(讀不到也要擋) |
| **判定** | **(b)** |

## R2 — 非可寫站不得寫原始碼

| 欄 | 內容 |
|---|---|
| **1 守什麼** | `current_stage` 不在宣告 `allows_src_write` 的站時,寫入原始碼目錄即擋 |
| **2 掛哪層** | **兩層** |
| **3 輸入怎麼算** | `current_stage` ← `PIPELINE`(`ROOT/.dev/pipeline.json`);值域 ← `STAGES_DEF`(`ROOT/.agents/pipeline-stages.yaml`)。**兩者都綁 `ROOT`**。路徑判定 `is_source_path()` 取 `r.split("/")[0]` —— **根錨定 + 分段,形狀正確**。**但跨視窗時 `ROOT` 是來源視窗的(F-094)** |
| **4 攔截紀錄** | **無帳本可查** |
| **5 正對照** | ✅ **17 條**(全庫最多),含 fail-closed 分支:站別定義不可用、pipeline 讀不到 |
| **判定** | **(b)**,但**跨界情境是 (c)** —— F-094 那條路沒有任何測試 |

## R3 — 沒有測試檔 / 沒有合格紅燈即擋

| 欄 | 內容 |
|---|---|
| **1 守什麼** | 寫 `<name>.py` 而 `tests/test_<name>.py` 不存在、或無當前票的紅燈紀錄 |
| **2 掛哪層** | **兩層** |
| **3 輸入怎麼算** | 紅燈 ← `.dev/test-runs.jsonl`;`ticket_id` ← `PIPELINE`;HEAD 錨點 ← `head_blob()` 用 **`cwd=ROOT`**;豁免 ← `PROVENANCE`(`ROOT/.dev/`)+ 上游指標 ← **`~/.claude/upstream-roots.txt`(使用者層,不綁 ROOT)** |
| **4 攔截紀錄** | ✅ **(a)** —— `gate-exemptions.jsonl` 唯一一筆 `blocked`,`blocked_by=R3` |
| **5 正對照** | ✅ **22 條**,含 provenance 五態的反控 |
| **判定** | **(a)** —— 全清冊唯一 |

## R4 — skill 鏡像與正典不一致即擋

| 欄 | 內容 |
|---|---|
| **1 守什麼** | `.claude/skills/` 與 `skills/` 的 SKILL.md 與正典 `.agents/skills/` 不一致 |
| **2 掛哪層** | **僅 pre-commit**(`skill_mirror_violations`,由 `check_skill_copies()` 呼叫) |
| **3 輸入怎麼算** | 三個路徑全部 `os.path.join(ROOT, …)`。**已知缺口**:硬連結佈局下內容由構造保證相同,**內容比對不會觸發**,實際只守得住「檔案在不在」(CLAUDE.md 已載明) |
| **4 攔截紀錄** | **無帳本可查** |
| **5 正對照** | ✅ 7 條(斷掉的 symlink、指向正典外、實體複本漂移、鏡像缺檔 / 缺目錄) |
| **判定** | **(b−)** —— 測試造得出漂移,**而真實佈局造不出**;測的是規則,不是本機實際輸入 |

## R5 — code-review 第三軸掛載點

| 欄 | 內容 |
|---|---|
| **1 守什麼** | 正典 `code-review` 缺第三軸掛載點(patch 未重套)即擋 |
| **2 掛哪層** | **僅 pre-commit**(`check_third_axis_mount()` / `check_to_spec_override()`) |
| **3 輸入怎麼算** | `CANON_CODE_REVIEW = ROOT/.agents/skills/code-review/SKILL.md` —— 綁 `ROOT`,單一檔案,無清單推導 |
| **4 攔截紀錄** | **無帳本可查** |
| **5 正對照** | ✅ 2 條 |
| **判定** | **(b)** |

## R6 — legacy 豁免清單只減不增

| 欄 | 內容 |
|---|---|
| **1 守什麼** | 清單條目必須在 `# go-live: <sha>` 那個 commit 的樹裡 |
| **2 掛哪層** | **僅 pre-commit**(`check_legacy_list()`) |
| **3 輸入怎麼算** | 清單 ← `LEGACY_LIST = ROOT/.agents/legacy-no-redlight.txt`(**gitignored,不進版控**);驗證 ← `git cat-file -e <go-live>:<path>`,**`cwd=ROOT`**。**`go-live` sha 是唯一的時間錨,而它住在一個不進版控的檔案裡** |
| **4 攔截紀錄** | **無帳本可查**。但**票 39 開刀時實測過**:sha 失效會讓 9 條全部變違規(未實際觸發,是推導) |
| **5 正對照** | ⚠ **僅 1 條**,而且是 `test_the_shipped_gate_enumerates_all_of_its_rules`(**那是「規則有被列舉」,不是「規則會擋」**) |
| **判定** | **(c) 危險區** —— 見下方登記 |

## R7 — Bash/PowerShell 寫入收口回檔案工具

| 欄 | 內容 |
|---|---|
| **1 守什麼** | 指令會寫檔而不在許可清單內即擋 |
| **2 掛哪層** | **僅前哨**(明文,ADR 0008)—— commit 看得到內容,看不到你用什麼工具寫的 |
| **3 輸入怎麼算** | **不吃路徑清單,吃指令字串**。逐段切(`&&` / `\|\|` / `;`),每段比對許可前綴;不過再搜 `WRITE_CONSTRUCT`。**已知**:切段看不見引號(TSA-003);**已修**:引號不成對即 refuse(票 21)、抽不出目標即 refuse(票 36) |
| **4 攔截紀錄** | ✅ **(a)** —— **本輪對話內就攔了 9 次以上**(heredoc、`echo` 重導向、`pip install`、含 `(` 的引號歧義…),原始擋下訊息逐次貼進報告 |
| **5 正對照** | ✅ 4 條,含刪除動詞家族 |
| **判定** | **(a)** —— 但**紀錄只活在對話裡,不在任何檔案** |

## R8 — 生產程式碼不得 `import research/`

| 欄 | 內容 |
|---|---|
| **1 守什麼** | 非 `research/` 底下的碼不得 import `research/` |
| **2 掛哪層** | **兩層**(`check()`) |
| **3 輸入怎麼算** | 判定對象是**套用編輯後的整檔內容**(`content_after_edit()`,TSA-001 修過),不是片段;AST 解析,解不開 fail-closed。路徑判定 `_under_research()` 用 `rel_path.replace("\\","/")` |
| **4 攔截紀錄** | **無帳本可查**(agent-gates 內)。⚠ **下游有活例**:量化 `morning_run.py` 15+ 處違反 |
| **5 正對照** | ✅ 12 條 |
| **判定** | **(b)**;**它不在 `CLAUDE.md` 的規則表裡** —— 見下方登記 |

## G1 — 檔案系統災難防護(使用者層)

| 欄 | 內容 |
|---|---|
| **1 守什麼** | 第一級:保護清單命中即擋(無豁免,不分讀寫)。第二級:破壞性動詞 + 專案外絕對路徑 |
| **2 掛哪層** | **其他** —— 使用者層 `~/.claude/settings.json`,**絕對路徑掛載**,`python "C:/Users/…/.claude/hooks/g1_guard.py"`。**不走 `$CLAUDE_PROJECT_DIR`** |
| **3 輸入怎麼算** | **第一級**:`HOME = expanduser("~")/.claude`,清單 `g1-protected.txt` —— 檔案第 40 行明文寫「**從使用者家目錄解析,不從 `__file__` 推導**」。**第二級**:`level2_hit(text, os.environ.get("CLAUDE_PROJECT_DIR"))`(第 306 行)|
| **4 攔截紀錄** | ✅ **(a)** —— 票 22 Phase 1 盤點時實撞一次(連 `ls` 都擋),當時**沒有繞過** |
| **5 正對照** | ✅ `tests/test_g1_guard.py` 有整組,含第二級的 xfail 標本 |
| **判定** | 第一級 **(a)**;**第二級 (c) —— 見下方登記** |

---

# 登記:三條進危險區 / 半死規則

## 登記一:R6 —— **守得最多,驗得最少**(全清冊最重的單一發現)

`test_the_shipped_gate_enumerates_all_of_its_rules` 驗的是
**規則代號有出現在列舉裡**,不是**規則會擋下違規**。
換句話說:**它證明 R6 存在,不證明 R6 會動。**

> **⚠ 更正(2026-08-16):上面這句話掛錯規則了。**
> 「證明規則存在,不證明規則會動」**精確描述的是 R5,不是 R6** ——
> R6 有 5 條真正對照(見下方更正段),而 R5 只有那條列舉測試。
> **一句對的判準,貼在錯的對象上。** 原句保留(F-036),
> 正確的歸屬見**登記五**。

### 不對稱擺在一起看

| | R6 |
|---|---|
| **守什麼** | R3 的豁免入口 —— 本 repo **14 個受管 `.py` 裡有 9 個靠它豁免(64%)** |
| **有多少正對照** | **0 條**(唯一那條是「被列舉」) |
| 相比之下 R2 | 17 條 |
| 相比之下 R3 | 22 條 |

**它管的檔案比例是全清冊最高的,而驗它的測試是全清冊最少的。**

### 而失效的樣子與正常一模一樣

R6 的判定是 `git cat-file -e <go-live>:<path>` —— 對每一條逐一問「在不在那棵樹裡」。

- 名單是**空的** → 迴圈零次 → **零違規** → 通過
- 名單**讀不到** → 那一支有 fail-closed(回一條違規)—— 這半是對的
- **`go-live` 指向一個不存在的 commit** → 每一條都 `rc != 0` → **全部變違規** → 吵鬧,會被發現
- **`go-live` 指向一棵「什麼都在裡面」的樹** → 每一條都通過 → **靜默放行**

最後那一種就是票 39 分析「第四條路」時算出來的形狀:
新 repo 的 initial commit 樹包含全部檔案,於是**門檻對每個檔案都成立** ——
**規則還在跑、訊息不會出現、報告全綠,而它擋不住任何東西。**

**沒有任何測試會為那一種出聲**,因為唯一那條測試只問「R6 有沒有被列舉」。

---

## ⚠ 更正(2026-08-16)—— 上面那段的**事實是錯的**,原文不刪(F-036)

影音的清冊推翻了本則,證據硬:`tests/test_gate.py` 在兩個 repo 是**同一份 blob**
(`80caa4250f18ecd04807c7b44fb705fad1e82d69`),而我手上就是那份。

### 一、R6 由 **(c)** 改為 **(b)**

**R6 有 5 條真正對照,不是 0 條。** 我逐條手讀了三個相關類別的全部 20 條斷言:

| 測試 | 斷言原文 |
|---|---|
| `test_an_entry_absent_from_the_go_live_tree_is_a_violation` | `assert len(v) == 1 and "not/in/the/tree.py" in v[0], v` |
| `test_a_path_absent_from_that_tree_is_still_named` | `assert len(v) == 1 and "never/existed.py" in v[0], v` |
| `test_an_unreadable_list_is_not_silently_clean` | `assert gate.check_legacy_list() != []` |
| `test_a_list_without_a_sha_is_a_violation_not_a_pass` | `assert v and "go-live" in v[0], v` |
| **`test_the_rule_is_actually_invoked_at_the_authoritative_layer`** | **`assert gate.mode_pre_commit() == 1`** |

**實跑:20 passed。**

最後那條比本清冊給任何規則的評價都強 —— 它驗的是**規則真的被權威層呼叫**,
而不只是函式回對值。**我原本說它「連自己會不會被呼叫都沒驗」,那是錯的。**

### 二、**所有「正對照條數」一律撤回,而且不換新數字**

我用了四套方法算同一件事,得到四個答案:

| 規則 | 一(搜代號) | 二(認實作) | 三(讀斷言) |
|---|---|---|---|
| R1 | 3 | 1 | 2 |
| R2 | **17** | 9 | 9 |
| R3 | **22** | 26 | 19 |
| R4 | 7 | 6 | 10 |
| R5 | 2 | **0** | 3 |
| R6 | **1** | 2 | 8 |
| R7 | 4 | 3 | 19 |
| R8 | 12 | 4 | 10 |

而方法三**也是錯的** —— 抽查它給 R8 的「正控例」:

```
test_an_indented_fragment_without_imports_is_not_an_r8_violation
    assert not (msg and "R8" in msg)          ← 這是反控,被歸成正控
```

**壞的是單位,不是計數器。** 再換一個自動數字只是換一個錯法。

### 三、根因:搜的是**規則代號**,而測試斷言的是**行為**

原方法只認 body 含 `[R6` / `"R6"` / `'R6'` 的測試。而那條被我判定不存在的測試,
斷言是 `assert len(v) == 1 and "not/in/the/tree.py" in v[0]` —— **body 裡沒有 `R6`。**

> **系統性偏差**:訊息文字**碰巧含有自己代號**的規則(`R3/紅燈`、`R2/範圍`)被高估;
> 訊息不含代號的規則被歸零。
> **17 / 22 這兩個大數字,與 R6 的 0,是同一個 bug 的兩端。**

這是 F-091 家族的第八次:**驗證用的表示法與被驗證的資料不是同一個**。
而這一次它污染的不是一格,是**整份清冊的第 5 欄**。

### 完整根因(影音清冊補上,比我自己查的更細)

我原本停在「訊息碰巧含不含代號」。**真正的分界在更下面一層:
規則的回傳型別本來就有兩種,而「斷言規則會擋」因此有兩種寫法。**

| 回傳型別 | 規則 | 測試怎麼斷言它會擋 |
|---|---|---|
| **訊息字串**(`check()` 回 `None` 或違規訊息) | R1 / R2 / R3 / R7 / R8 | `assert msg and "R8" in msg` —— **字串裡有代號** |
| **違規清單**(`check_legacy_list()` / `skill_mirror_violations()` 回 `list`) | **R6 / R4** | `assert len(v) == 1 and "not/in/the/tree.py" in v[0]` —— **驗清單長度與內容** |

`check_legacy_list()` **根本不回訊息字串**。所以用第一種寫法搜全庫,
**R6 必然得零** —— 而那個零與「有沒有測試」無關。

> **不是漏看,是量尺量不到那個形狀。**

這也解釋了「四套方法四個答案」:**每一套各自認得不同的形式子集**,
所以它們不是「四個估計值」,是**四個對不同集合的正確計數** ——
而沒有一套涵蓋全部兩種形式。把它們並排比較本身就是錯的,
它們量的不是同一個東西。

**判準**:盤點一個由多種介面組成的系統時,**先枚舉介面的形式,再設計量尺** ——
否則量尺會安靜地只涵蓋其中一種,而**結果看起來仍然是一個完整的表**。
(與 F-087「先問集合是開放還是封閉」同族:這裡的封閉集合是**回傳型別**。)

### 四、結論方向**部分成立**

那 5 條正控**沒有一條**測「`go-live` 指向一棵什麼都在裡面的樹」。
最接近的 `test_it_holds_when_the_gate_itself_is_absent_from_the_tree`
構造的是**樹裡少東西**、斷言 `check_legacy_list() == []`(防誤報)—— **方向相反**。

所以「**那一種失效沒有測試看著**」**仍然成立**。
錯的是據以成立的事實(「R6 沒有正對照」),不是那個判斷。

### 五、R1 / R2 / R5 / R8 四格的重驗(**各自手找一條實例,不數**)

| 規則 | 實例 | 結果 |
|---|---|---|
| **R1** | `test_a_spec_that_cannot_be_read_is_blocked_not_allowed`(`test_gate_boundaries.py:50`)<br>`assert msg and "R1" in msg` | **(b) 成立** —— ⚠ 但**只涵蓋 fail-closed 分支**;**主路徑(規格書含程式碼 → 擋)沒有正控**。`CORPUS` 裡有那個輸入,但只餵給結構不變式與 trace 測試 |
| **R2** | `test_unknown_file_types_in_source_dirs_are_guarded`(`test_gate.py:177`)<br>`assert msg is not None and "R2" in msg`(`load_stage` → `("spec", None)`,寫 `macro_audit/`) | **(b) 成立,主路徑** |
| **R5** | **找不到** | **降為 (c)** —— 見下方登記五 |
| **R8** | `test_a_result_that_imports_research_is_blocked`(`test_edit_result.py:188`)<br>`assert msg and "R8" in msg`;另有 `test_a_whole_file_write_importing_research_is_still_blocked`(`:241`) | **(b) 成立,主路徑** |

**從此這四格是實例支撐,不是數字支撐。**

## 登記二:R8 不在 `CLAUDE.md` 的規則表

`CLAUDE.md` 的四條規則表列 R1–R5(另有 R6/R7 在散文裡),**R8 完全沒有出現**。
它只活在 `gate.py:1700–1727` 與 12 條測試裡。

**一條沒有被寫進總表的規則,查表的人不會知道它存在** ——
而它現在正在下游擋人(量化 `morning_run.py` 15+ 處)。

## 登記三:G1 第二級吃 `$CLAUDE_PROJECT_DIR`,而第一級明文拒絕

**同一個檔、同一個陷阱、一半躲過一半沒躲過。**

| 級 | 輸入來源 | 對 F-094 |
|---|---|---|
| 第一級 | `expanduser("~")` —— **明文拒絕從 `__file__` 推導**(第 40 行) | **免疫** |
| 第二級 | `os.environ.get("CLAUDE_PROJECT_DIR")`(第 306 行) | **不免疫** |

**方向性**:跨視窗操作時,第二級的「專案外」判定用的是**來源視窗**的專案根目錄 ——

- 影音視窗 → 寫 agent-gates:agent-gates 的路徑對它是「專案外」→ **變嚴**
- agent-gates 視窗 → 寫影音:反過來 → **變鬆**

第一級的設計者已經想過這個陷阱並寫下理由,**第二級沒有跟上**。

**只登記,不跑實測** —— 攻擊面分析是另一件事,會把本票撐爆。

## 登記五(2026-08-16 重驗新增):**R5 沒有任何正對照 —— 真正的 (c)**

原本 R6 被誤判為 (c);重驗之下 R6 是 (b),而**真正落在 (c) 的是 R5**。

`check_third_axis_mount()` / `check_to_spec_override()` 在測試裡只出現三種形態,
**沒有一種在斷言它會擋**:

| 出現處 | 形態 |
|---|---|
| `test_gate.py:819–820`、`1857–1858` | **被 monkeypatch 成 `lambda: []`** —— 為了讓別的測試跑得過而**停用**它 |
| `test_gate.py:406` | `lambda: ["[R5] 掛載點消失"]` —— **餵一個假違規**進去測快取,不是測 R5 |
| `test_gate.py:901` | `assert {"R1"…"R6"} <= codes` —— **只驗代號被列舉** |

`test_apply_patches.py` 那 5 條測的是**貼 patch 的腳本**(`_count(grill) == 1`),
不是 R5 的判定。

**R5 的實際偵測邏輯(`MOUNT_MARKERS` 有沒有出現在正典 body 裡)零測試涵蓋。**

而它守的是:`npx skills update` 會用上游版覆蓋正典 code-review、**靜默移除本地第三軸**。
**「靜默」正是它要防的東西,而它自己現在也是靜默的。**

> **「證明規則存在,不證明規則會動」——這句話的正確歸屬在這裡。**
> R5 唯一碰得到它的測試是 `test_the_shipped_gate_enumerates_all_of_its_rules`
> (`assert {"R1"…"R6"} <= codes`),而那只驗代號被列舉。

### 為什麼這一條該優先於其他 (c):它是 9/15 決策的**其中一隻腳**

`CLAUDE.md:43` 寫著:

> wrapper 三步:更新 → 冪等重套 patch → gate.py 全規則驗證。
> **R5 是這條規矩的機器保證。**

而 9/15 退出影子模式的決策**站在兩隻腳上**:

| 腳 | 管什麼 | 狀態 |
|---|---|---|
| **票 21** | 樣本**乾不乾淨**(垃圾目標、誤報污染分子) | 已處理 |
| **R5** | 樣本**存不存續**(第三軸被靜默移除 → 後續樣本從此少一個軸) | **(c) 零涵蓋** |

**保證本身沒有被保證。** 而它的失效方式與它要防的東西同形:
`npx skills update` 靜默覆蓋 → R5 該擋 → 而**沒有任何測試證明 R5 會擋** →
於是「有 R5 在守」這件事本身是一個未經驗證的假設。

**建議:另開修復票,優先於其他 (c) 項。** 理由不是它比較嚴重,
是**它有期限**(9/15),而其他 (c) 沒有。**本票仍不修。**

## 登記四:**「決定規則行為的東西不進版控」是一整族,九處**

明細見上方頭條結論之二。這裡只記**處置方向**,**本票不做**。

### 已有先例可搬:量化 `dc-01`

`data_collector/.scratch/identity-hardening/issues/01-externalize-hardcoded-paths.md`
第 209–221 行:

> **版本指紋**(2026-08-16 取,四份缺一不可)…
> 不進版控、跟機器走。少了它的指紋,「只剩 N 筆」這個結論在別台機器上
> **不可複現,也無從證偽**。檔名是 `leak-patterns.local.txt`(點,不是連字號)——
> **指紋的價值在於能照同一路徑重算,路徑錯即作廢。**

**招式**:內容**不進版控**(照抄 = 假清單),但**指紋進版控**。
理由不是完整性,是**可複現性** —— 少了它,結論無從證偽。

### 搬過來的形狀(方向,不是設計)

九處各自的敏感度不同,**不會是同一種處置**:

| 類 | 例 | 可能的形狀 |
|---|---|---|
| 秘密內容 | `leak-patterns.local.txt`、`g1-protected.txt` | **只記指紋 + 條數**(dc-01 原樣) |
| 非秘密但綁機器 | `pipeline.json`、`shadow.json`、`upstream-roots.txt` | 指紋?或**判定時把值寫進判定紀錄** |
| 帳本類 | `test-runs.jsonl`、`gate-exemptions.jsonl` | 後者**已經進版控**,是現成的對照組 |

**最小可用的一步可能不是「全部進版控」,而是「判定時把當時的設定指紋寫進那筆紀錄」**
—— 那樣事後複驗需要的三樣(設定、輸入、結果)會落在同一個地方。
但那是設計,**不在本票**。

---

# 共通風險(不屬於單一規則)

## A. `staged` 清單:同一個操作,兩個呼叫端,兩種輸入來源

| 呼叫端 | 寫法 | 綁在哪 |
|---|---|---|
| `leak_scan.py:269` | `staged_paths(cwd=ROOT, …)` | **綁 `ROOT`** |
| **`gate.py:2328`**(`mode_pre_commit`) | `staged_paths(gitlinks=gitlinks)` | **沒帶 cwd → 繼承行程 cwd** |

pre-commit 情境下 git 會把 cwd 設在 repo 頂層,所以**現在剛好是對的** ——
但它是**環境給的,不是推導出來的**。**R1 / R2 / R3 / R8 在 pre-commit 都跑在這份清單上。**

## B. 六個 git 子行程全部指定了 repo(**這一項是乾淨的**)

AST 實測 `gate.py` 的 6 處 git 呼叫:5 個 `cwd=ROOT`、1 個 `git -C`,
**繼承 cwd 的:0**。`scanner.py` 兩處是 `cwd=cwd`(參數),由呼叫端決定 —— 見 A。

## C. 跨視窗:前哨讀來源視窗的狀態(F-094)

R1–R8 全部受影響,因為它們共用 `ROOT`。**權威層不受影響**
(`git rev-parse --show-toplevel` 綁被提交的 repo)。

---

# 方法論:**這份清冊自己犯過一次同族的錯**

第 3 欄第一版用「含 `subprocess.run` 的**那一行**有沒有 `cwd=`」判,
回報 **5 處沒帶 cwd**。**全部是假陽性** —— 那些呼叫是多行的,`cwd=ROOT` 在下一行。
改用 **AST**(看呼叫節點,與換行無關)重驗:**繼承 cwd 的 0 處**。

> **用字串搜尋去判程式結構,就是拿錯的表示法問問題** ——
> 與本清冊要查的東西(`SKIP_PARTS` 裸字串無邊界、`.gitignore` 缺斜線、
> F-091 的表示法不一致)**同一族**。
>
> 差別只在:那些是規則的輸入算錯,這次是**清冊的輸入算錯**。
> **盤點工具與被盤點的東西適用同一條判準。**

寫在這裡而不是刪掉,因為**一份沒有犯過錯的盤點報告,讀者無從判斷它查得多深**。

---

# 附錄一:`shadow-log.jsonl` 不存在的成因(**不是死規則**)

`gate.py:1834` 有 `SHADOW_LOG` 常數指著一個不存在的檔。
兩種可能長得一樣,拆開查:

**寫入點**:`log_shadow()` 只在三處被呼叫(`gate.py:1959 / 1992 / 2352`),
**三處都在 `if shadow_active():` 底下**。

**`shadow_active()` 的三個條件,逐一實測**:

| 條件 | 實測 |
|---|---|
| 1. 安全閥合法 | ✅ `read_shadow_clamp()` → `2026-09-15` |
| 2. **本 repo 宣告了影子** | ❌ **`.dev/shadow.json` 不存在**(`FileNotFoundError`) |
| 3. 今天 ≤ min(兩個到期日) | 到不了(前一條已 False) |

`shadow_active()` = **False**。

**結論:條件 2 未滿足 → 寫入路徑從未執行 → 檔案不存在是預期,不是死規則。**

**但要記一句**:`.dev/shadow.json`(本 repo 開沒開影子)也**不進版控**,
所以「本 repo 從未開過影子」這件事**沒有任何版控紀錄** ——
判斷它只能靠現場探測,而現場探測只反映**這台機器的此刻**。
與 R6 的 `go-live` 是同一個形狀:**決定某條規則行為的東西,住在不進版控的檔案裡**。

---

# 附錄二:待查項(**不在本 repo,由影音 VS 查**)

| repo | hash | 待查 |
|---|---|---|
| **台股分析師影音自動化提取與勝率追蹤系統**(`analyst-tracker`) | `1df929b` | dangling commit 的來歷 |
| **同上** | `26391bb` | dangling commit 的來歷 |

**本 repo 不跨過去查。** 理由:那是影音自己的物件庫,
而 F-094 剛記完「跨視窗操作時判定用錯 repo 的狀態」——
**跨界去查,正是那則在講的動作。**
