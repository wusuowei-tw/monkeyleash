# 交班檔

> **本檔只記「為什麼這樣決定」,不記「現在是什麼」。**
> 現況一律跑 `python .claude/portable/status.py --root .`。
> 一份會記錄現況的交接文件一定會安靜地過期 —— `F-109`。

**寫於**:2026-09-09,上游桌機(hostname `DESKTOP-T2I45T1`),HEAD `e5abad8`。
**搭配讀**:`docs/agents/commander.md`(角色分工與硬規則)。

## 一、目前的階段

- monkeyleash 在**維護模式**:收斂四票 + 七格出口檢查過後,agent 不自發開票,
  開票要 Jeff 點頭。2026-09-09 開的 **123–129** 都是 Jeff 逐次核准的。
- 桌機 **2026-09-10** 打包,約 **10 月初**送達美國。**9/10–9/28 只有筆電。**

> **⚠ 123–129 這七張不是同一台機器開的。**
> 123–127 開在**筆電**(hostname `14X`),上游 HEAD `63cd0fa`;
> 128 / 129 開在**上游桌機**(hostname `DESKTOP-T2I45T1`),上游 HEAD `3a37399`。
> 逐張票面自己寫了發現於哪台、哪個 HEAD —— **要引用就去讀票面,不要引用這一行。**
> (`commander.md` 硬規則 10:機器不憑內容猜。)

## 二、2026-09-09 的裁決與理由(沒進票面的那些)

- **票 115 裁「甲+」**:只做結構斷言 + 一條只針對新 `_out` 的行為測試;
  不抽函式。理由三條寫在票 115 落地段,此處只記**否決乙的關鍵**:
  `install.py` 在 legacy-no-redlight 上 ⇒ 結構改動不會被任何紅燈要求擋下,
  **那是方便,也是沒有護欄**;在編碼範圍的票裡做無護欄的十參數抽函式,
  正是這套系統要防的東西。
- **不進第五站(`/code-review`)**:票 122 未收之前,那個站在這個 repo 上會產生
  100% 偽陽性(散文前提在 223 筆帳本上命中 0 筆),是壞的訊號源(`F-031`)。
- **桌機 `.dev/` 進最終備份**:git 存法條,`.dev/` 存執法紀錄;
  程式碼在 GitHub 上已安全,`.dev/` 只活在那顆硬碟上。
  這是保險,保的是「桌機在運送途中壞掉」。
- **回報檔一律進版控**(`docs/audits/`):`.dev/reports/` 被 gitignore,
  隨機器消失。2026-09-09 一天之內同一個形狀出現六次。
- **兩份目錄清單、四份 `_out` 刻意不合併**:那個維護成本是為了避開 fail-open
  買的,理由記在票 114 決策表 —— **買來的成本和沒人整理的成本長得一樣,
  所以理由必須寫下來。**

## 三、接下來的順序(筆電期 9/10–9/28)

1. **票 125**(`refuse_if_dirty` 的 untracked 口徑)—— **最先做**,
   它卡住整個下游同步能力,而筆電是唯一一台。
2. **票 123**(影音下游 SKILL.md 消失)—— 唯一有資料風險的。
3. **票 128 / 129**(第六站 2026-09-09 挖出的驗證落差)。
4. 票 126 / 127 / 86 / 89-2 / 113。
5. 甲類 34 張唯讀驗證掃描。
6. 遊戲(賽車技師)裝框架 —— 原型放 `.scratch/<feature>/prototype/`。

> **⚠ 票 129 動工的第一步是【突變】,不是盤點。** 裁決 2026-09-09,已寫進票面:
> 把 `tests/` 那支 `_imported_names()` 打樁成回空集合,跑全套,量它是不是仍然全綠。
> 一次就能把「它擋不住」從讀碼推論變成量測。**順序記在這裡是為了不要先做盤點** ——
> 盤點會產生「處理過了」的感覺,而主張仍然只是推論。

## 四、已知的待決/未證明(不要當成已解決)

- 影音下游 `--regenerate-canon` **未跑**(要先確認上游已推)。
- 量化下游 11 則 `F-047`~`F-057` 要人判哪些算專案條目。
- **兩台機器今天跑全套的指令口徑不同**(`--ignore` / `--deselect` /
  `PYTHONIOENCODING`),通過數對得上但「同一條指令」這句話是假的。**未重量。**
- ~~`endpoints_match()` 的消費端是誰,未量(票 128)。~~

  > **⚠ 這一格已經量了,不是未證明** —— 依 `commander.md` **硬規則 11** 更正。
  > 2026-09-09 上游桌機量出消費端是 `.claude/portable/ledger_verify.py:283` 的
  > `report()`,它印的是「首尾相等 : 是」。原文與 `grep` 輸出在票 128 第四節。
  > **舊句不刪**(`F-036`),留著是因為它正好是硬規則 11 舉的那個實例本身。
  >
  > **這一格仍未量的是**:`report()` 的**實際輸出**(那一輪禁止跑 `.py`,
  > 票面貼的是原始資料與原始碼,由它們推出結論 —— **推論不是量測**),
  > 以及下游兩份帳本的首尾長什麼樣(那一輪不碰下游)。

> **⚠ 這一節的用法:**
> 每一格底下都要答得出「**不做會有什麼東西叫?**」——
> 答不出來的就不是待決事項,是一句安慰(`F-110`)。
> 本節現在**一格都答不出來** —— 它們全靠下一個讀這份檔的人。
> **這句話寫在這裡不是為了謙虛,是為了讓人不要把這一節當成機制。**
