# 轉公開審查 —— 已知項登記表

**格式與規則的定義在 `docs/adr/F-0015`(Q6-C 裁決)。本檔只承載資料。**

分開的理由:ADR 的性質是「決定寫下來就不太動」,登記表**會成長**。
混在一起會養出「不敢改 ADR」或「習慣 ADR 會變」—— 兩個都是壞習慣。

## 三條規矩

1. **不記字串本身。** 只記 commit、路徑、類別、範圍、期望值。
   理由與個人 pattern 不進版控同一條:**清單本身列的就是它要保護的東西**。
2. **藏寶圖條款** —— 公開前,每一筆對應的內容必須**已清除或泛化**。
   本表即使不含字串,它仍然把搜尋範圍從數百個 blob 版本縮到一個。
   **縮小範圍就是主要的價值**,對想找東西的人也一樣。
3. **每一筆在任何時點都要有明確的期望值**(下表「期望」欄)。
   說不出期望值本身就是 refuse 的理由。

## 期望值:清除前後**方向相反**

已知項回歸(ADR F-0015 的 Q2-C,必要條件)拿本表逐筆去掃:

| 該筆狀態 | 回歸要驗的 | 驗不到代表 |
|---|---|---|
| `未清除` | **掃得到** | 掃描器涵蓋不足 —— 個人 pattern 缺條、或規則退化 |
| `已清除` / `已泛化` | **掃不到** | 清創沒做乾淨,或做完又長回來 |

兩邊都是 fail-closed:**不符期望一律 refuse,不是警告。**

## 登記表

| # | commit | 路徑 | 類別 | 範圍(實測) | 狀態 | 期望 |
|---|---|---|---|---|---|---|
| K1 | `3a265f2` | `docs/tickets/framework-updates/22-machine-recovery-drill.md` | 個人資料夾名(C3) | 全歷史 376 個 blob 版本中**僅此 1 個**;該檔另有 7 個版本不含 | `未清除` | **掃得到** ✅ |
| K2 | 現樹 + 歷史 | `docs/tickets/framework-updates/22-machine-recovery-drill.md:291` | 個人備份資料夾名(C3) | 現樹 1 行;全歷史 7 個 blob 版本 | `未清除` | **掃得到** ✅ |
| K3 | 歷史(現樹已遮) | `docs/agents/friction-log.md`(原 :803) | 真實使用者名 + 工作區資料夾名(C3/C4) | 現樹**已遮罩**;全歷史 30 個 blob 版本 | `現樹已泛化 / 歷史未清除` | 現樹**掃不到** ✅ / 歷史**掃得到** ❌ |

### 2026-08-15:回歸狀態

| | 回歸 | 說明 |
|---|---|---|
| K1 | ✅ 綠 | 新 pattern(1 條)打中 |
| K2 | ✅ 綠 | **同一條 pattern**打中 —— K1 與 K2 是同類、不同字串,所以這一條**本身就是防過度擬合的實證** |
| K3 | ❌ 紅 | 真樣本是 **JSON 跳脫形式**,而現行 pattern 照裁決 4 **只打未跳脫形式** |

**K3 紅是設計的,不是漏的。** 跳脫形式在妥協聲明內;要不要擴張範圍去涵蓋它,
成本已量、待裁(見票 39)。

**K3 現樹已依裁決遮蔽**(票 32 整行遮蔽形式、留記號、裁決者明示授權),
歷史那一份留待開刀一併清除。

**K2 / K3 是 2026-08-15 定性時新找到的,兩筆都在現樹**(不只歷史)。

**裁決(2026-08-15):兩筆照登記不處置。**
**K3 的遮蔽留待開刀票時裁** —— 它是一段引用的證據片段,
遮掉會動到證據原文,而那不是代理人能自行決定的(TSI-002)。
K2 的教訓已入原則:見 F-085 與 CLAUDE.md 常駐檢查項。

### K1 的現況與已知偏差

- 現樹已修正(`e34ce21`),**歷史仍在**
- 範圍是量出來的,不是估的:該檔歷史 8 個版本逐一比對,只有一個含那一行;
  全歷史所有 blob 版本比對亦然。**登記單一 commit 是準確的,不是簡化**
- ⚠ **目前這一筆的回歸是紅的**:這台機器的個人 pattern(16 條)與通用(10 條)
  對那一行**都是 0 命中**。發現它的那條 pattern 在演練那台筆電上。
  這正是 ADR F-0015「缺口二」的實證 —— **不是本表的錯,是涵蓋的洞**

### K2:同一個檔案裡,**修了一行、漏了它的同類**

`22-machine-recovery-drill.md:348` 是 F-082 修掉的那一行(現在是 `<備份資料夾>`)。
**`:291` 在同一個檔案裡,同一類字串,沒有被修。**

這是 F-083 的形狀,而且比原始標本更緊:不是「收了 PowerShell 忘了收刪除家族」,
是**同一個檔案、同一種東西、隔了 57 行**。修的時候看的是「那一行」,
不是「這個檔案裡還有幾個同類」。

K2 的字串與 K1 **不同**(逐位元組比對過),所以它是獨立的一筆,不是 K1 的重複。

### K3:偵測器看不見**跳脫過的**形式

`friction-log.md:803` 是一段引用的 JSON 證據,路徑在裡面是
`C:\\Users\\...`(原文兩個反斜線)。

定性時我第一版的比對式是 `[A-Za-z]:[\\/]Users[\\/](...)` ——
**它對雙反斜線不匹配**,於是第一次跑完回報「現樹全是佔位符」。
正規化連續分隔符之後才看見這一筆。

> **同一個字串換一種跳脫寫法就穿過去了,而報告仍然是綠的。**

這一條要往上推一層:**通用 / 個人 pattern 若寫成字面路徑,
就同樣看不見 JSON / 原始字串 / URL 編碼形式。**
這不是本表的問題,是掃描面的問題 —— 已列入 Gemini 審計的重點。

## 已知良性(Q1-B 聚合)

**不是洩漏**,是掃描器掃到自己。按「歷史路徑 × pattern 類」聚合,
**不逐筆記** —— 聚合之後不隨歷史成長,再加一百個版本仍是兩行。

| 歷史路徑 | pattern 類 | 起草時筆數 |
|---|---|---|
| `.claude/portable/leak_scan.py` | 憑證副檔名 | 39 |
| `.claude/portable/leak-patterns.txt` | 憑證副檔名 | 3 |

聚合鍵刻意是**歷史路徑**而不是 commit:commit 會變(開刀),路徑不會。

**出現第三行 = 有新東西,要定性。** 這是本表存在的主要作用 ——
自我豁免(Q1-A)會靜靜吞掉那一行,而這裡它會被看見。
