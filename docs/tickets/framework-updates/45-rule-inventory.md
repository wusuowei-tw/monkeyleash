# 45 — 規則清冊(唯讀盤點)

**性質**:唯讀。**不修任何東西。** 只盤點、登記,修復另開票。
**日期**:2026-08-16
**產出**:`docs/audits/2026-08-16-rule-inventory.md`(清冊本文)
**跑這份的機器**:桌機。個人 pattern 17 條(不進版控,跟人走)。

> agent-gates 先做,它是正典;這份出來當另外兩個 repo 的樣板。
> `docs/audits/` 在 manifest 標 `skip` **不出貨** —— 清冊是**本 repo 的實測結果**,
> 照抄到別 repo 就是假清單。**樣板是欄位與判準,不是內容。**

## 盤點範圍

**9 條**:R1–R8 + G1。比 `CLAUDE.md` 的表多兩條 —— **R8 沒進總表**(登記二)、
**G1 掛載方式與其他八條不同**(絕對路徑,不走 `$CLAUDE_PROJECT_DIR`)。

## 六欄 + 三態

六欄照裁決。三態**不合併**:

- **(a)** 擋過(有紀錄)
- **(b)** 沒擋過,但有正對照證明它會擋
- **(c)** 沒擋過,也沒驗過 ← 危險區
- **(b−)**(額外)有測試,但測的是**規則本身**,沒測**餵給它的輸入**

> 把 (b) 與 (c) 併成「有/沒有」,**正常規則與死規則在表上會長得一樣**。

## 兩條頭條結論

**一、第 4 欄對多數規則不可回答,而原因是「沒有東西在記」,不是「查過沒有」。**
全 repo 唯一一筆有紀錄的實際攔截是 **R3**(`gate-exemptions.jsonl`,
`outcome=blocked`,`blocked_by=R3`)。**沒有通用的攔截帳本。**

**二、連「當時規則是什麼設定」也沒有紀錄。**
F-083 掃出 **9 處**「決定規則行為卻不進版控」的檔案(比原本點名的六處多三處:
`.dev/test-runs.jsonl`、`.cache/mount-check.json`、`~/.claude/upstream-roots.txt`)。

> **每一次判定,都是「一份不進版控的設定」對「一份不進版控的輸入」做出的,
> 而結果也不進版控。三樣缺一就無法複現,而現在三樣都缺。**

唯一例外是 `.dev/gate-exemptions.jsonl` —— **它進版控,而它剛好就是
唯一留得下證據的那一個**。兩件事是同一件事的兩面。

## 登記(四條,各自修復另開票)

| # | 登記項 | 判定 |
|---|---|---|
| 一 | **R6:守得最多、驗得最少,而失效的樣子與正常一模一樣** —— 守 R3 的豁免入口(**14 個受管 `.py` 有 9 個靠它,64%**),正對照 **0 條**(R2 有 17、R3 有 22)。`go-live` 指向一棵「什麼都在裡面」的樹時,**每一條都通過、訊息不會出現、報告全綠,而它擋不住任何東西** —— 沒有任何測試會為那一種出聲 | **(c)** |
| 二 | **R8 不在 `CLAUDE.md` 的規則表** —— 只活在 `gate.py:1700–1727` 與 12 條測試裡,而它現在正在下游擋人(量化 `morning_run.py` 15+ 處) | 規則活著,**總表漏列** |
| 三 | **G1 第二級吃 `$CLAUDE_PROJECT_DIR`,而第一級明文拒絕從 `__file__` 推導** —— 同一個檔、同一個陷阱、一半躲過一半沒躲過 | 第二級 **(c)** |
| 四 | **「決定規則行為的東西不進版控」是一整族,9 處** —— 見頭條二。處置方向已有先例:量化 `dc-01` 的「**內容不進版控、指紋進版控**」(理由是**可複現性**,不是完整性:少了它結論無從證偽)。九處敏感度不同,不會是同一種處置;**最小可用的一步可能是「判定時把當時的設定指紋寫進那筆紀錄」**,讓設定 / 輸入 / 結果落在同一個地方 —— **但那是設計,不在本票** | 族,非單點 |

另記兩條**共通風險**(不屬單一規則):

- **A**:`staged` 清單同一操作兩個呼叫端 —— `leak_scan` 綁 `ROOT`,
  **`gate.py:2328` 的 pre-commit 沒帶 cwd、繼承行程 cwd**。
  現在剛好對,但**是環境給的,不是推導出來的**。R1/R2/R3/R8 都跑在這份清單上。
- **C**:跨視窗時前哨讀來源視窗的狀態(F-094),R1–R8 全部受影響;
  權威層不受影響。

**B 是乾淨的**:AST 實測 `gate.py` 六個 git 子行程**全部指定 repo**,繼承 cwd 的 0 處。

## 附錄一:`shadow-log.jsonl` 不存在 —— **查了成因,不是死規則**

`log_shadow()` 三個呼叫點**都在 `if shadow_active():` 底下**;
`shadow_active()` 的三個條件逐一實測,**第 2 條(`.dev/shadow.json` 存在)未滿足** ——
所以寫入路徑從未執行,檔案不存在是**預期**。

**但 `.dev/shadow.json` 也不進版控**,於是「本 repo 從未開過影子」這件事
**沒有任何版控紀錄**。與 R6 的 `go-live` 同形:
**決定某條規則行為的東西,住在不進版控的檔案裡。**

## 附錄二:待查項(不在本 repo)

`analyst-tracker`(影音)的 dangling commit **`1df929b`** 與 **`26391bb`** ——
**由影音 VS 查**,本 repo 不跨過去。理由:F-094 剛記完「跨視窗判定用錯 repo」,
**跨界去查正是那則在講的動作**。

## 本票不做的事

- **不修任何規則**、不加測試、不改 `CLAUDE.md` 的規則表
- 不跑 G1 第二級的攻擊面實測(會把本票撐爆)
- 不跨進影音查那兩筆 dangling commit
