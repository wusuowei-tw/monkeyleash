# 票 138 —— 權威層端對端驗證需要人工步驟,而該步驟沒有被寫下來

**狀態**:**candidate。只登記,本輪不查、不修。**
**優先度**:**未定** —— 見〈時鐘〉欄。
**發現於**:2026-09-12,做票 133 批一 ② R1 與 ③ R8 的權威層探測時。
**來源**:票 133 批一 ② / ③。**該票只登記本票,不處理。**

---

## ⚠ 本票的證據邊界

**本票只記錄「發生了什麼」,沒有做任何診斷、沒有提修法。**
下面沒有寫的東西就是沒查。

---

## 三件事實

### 一、兩格的端對端證據都無法用 agent 工具取得

票 133 批一 **② R1** 與 **③ R8** 兩格,要驗「權威層在真的 commit 路徑上會擋」,
需要造出一個違規樣本進 index。兩條造法各自撞上一道前哨:

**(a) 造違規樣本 → 被同一條規則的前哨擋。**

R1(2026-09-12,`.scratch/r1probe/spec.md`,語料 `R1_CODE_SHAPES` 的 `def` 樣式):

```
PreToolUse:Write hook error: [python "$CLAUDE_PROJECT_DIR/.claude/hooks/gate.py"]:
[六站閘門/前哨] [R1][enforce] .scratch/r1probe/spec.md:規格書禁止含程式碼。spec 站只描述『要解決什麼問題』與『怎樣算做完』,請移除 code 圍籬 / def / import / function。
(權威判定在 pre-commit,繞過前哨仍會在 commit 被擋)
```

R8(2026-09-12,`.scratch/r8probe/mod.py`,語料 `import research`):

```
PreToolUse:Write hook error: [python "$CLAUDE_PROJECT_DIR/.claude/hooks/gate.py"]:
[六站閘門/前哨] [R8][enforce] .scratch/r8probe/mod.py:生產程式碼不得 import research/。
     research/ 是探索區,可以被丟棄;生產碼依賴它,研究一被殺掉生產就壞。
     反方向可以:research/ 底下的碼 import 生產資料層是允許的。
     要用到某段研究成果,把它移出 research/ 走六站,不是 import 它。
(權威判定在 pre-commit,繞過前哨仍會在 commit 被擋)
```

**(b) 刪工作樹那份 → 被 R7 擋。**

R1 那次的替代造法是「乾淨版進 index、刪掉工作樹那份」(乾淨內容前哨不擋)。
刪檔被 R7 擋:

```
PreToolUse:Bash hook error: [python "$CLAUDE_PROJECT_DIR/.claude/hooks/gate.py"]:
[六站閘門/前哨] [R7][enforce] 這個 Bash 指令會寫到沒有被許可的位置(.scratch/r1probe/spec.md、(引號或跳脫使目標無法可靠切分))。
     …
     **Write / Edit 不能刪除或改名** —— 這條的出口是 `git rm`(`git` 在許可清單裡)。
```

**R7 指定的出口 `git rm` 會同時從 index 與工作樹移除**,而本實驗需要 index 保留那一份
⇒ **用它就毀掉實驗本身。**

### 二、兩次都靠 Jeff 本人手動操作才取到

- **② R1**:Jeff 手動執行 `del .scratch\r1probe\spec.md`(工作樹),
  之後 agent 接手 `--pre-commit` 取 rc。結果:`AD` 狀態下 **rc=0**,
  證明權威層讀的是 index(舊碼讀工作樹會 `FileNotFoundError` → fail-closed → rc=1)。
  紀錄:`.dev/reports/2026-09-12T022816Z-r1-authority-layer-reads-index-verified.md`
- **③ R8**:**待做。** 2026-09-12 的探測在第 1 步被前哨 R8 擋下,
  第 2–6 步未執行。紀錄:
  `.dev/reports/2026-09-12T062635Z-r8-probe-blocked-by-sentry-same-shape-as-r1.md`

### 三、這一步目前沒有任何文件寫下來

「權威層端對端驗證需要人工做哪一個動作」這件事,**不在** `CLAUDE.md`、
**不在** `docs/adr/`、**不在** 票 133 的票面流程裡、**不在**任何 skill 裡。

⇒ **下一個做 ④–⑦ 的人會重新撞一次。**

---

## 時鐘

**有觸發條件,沒有日期。**

> **下一次有人動票 133 的 ④–⑦ 任何一格時。**

照 `docs/agents/issue-tracker.md` 的時鐘欄規矩,沒有日期 ⇒ 停在 `candidate`,
不排進任何順序。

---

---

## 狀態:**結案**(裁決 **2026-09-24**)

~~candidate。只登記,本輪不查、不修。~~(`F-036`:舊狀態不刪)

**結案依據**:命題「**人工步驟沒被寫下來**」已由
**工具**(`scripts/e2e_authority_layer.py`)+
**操作說明**(`docs/agents/authority-layer-e2e.md`)+
**一次 Jeff 本人的真實執行**(A 輪,2026-09-24,受測 SHA `9726ab4c…`,
持久化於 `docs/audits/2026-09-24-authority-layer-e2e-round-a.md`)解決。

### ⚠ 結案不代表的事(隨狀態段留在票面)

**說明文件對 ④–⑦ 是否足夠,未經驗證。** 拆開寫,兩者不同:

| 範圍 | 狀態 |
|---|---|
| **批一 ①–⑥** | **待補的真 `git commit` 端對端驗證 → 見票 144 §H** |
| **⑦ R4** | **不在票 144 §H 的範圍** —— 它**經量測後保留工作樹語意**(裁決 2026-09-13),不是待修的格子。**操作說明對它的適用性【未另驗】** |

⚠ **不得**寫成「④–⑦ 全部由票 144 §H 追蹤」 —— **§H 不含 ⑦**。

其餘不代表的事:**人工步驟沒有消失,只是被寫下來了**
(`sh bootstrap.sh`、**看 `~/.claude/upstream-roots.txt`**(G1 擋 agent)、
**判讀「意外提交成功」**、**決定要不要跑 B 輪**、
**接受不接受自建的 `provenance.jsonl`**);
**A 輪只做過一次、單機**;**`--round B` 未實作**。

---

## 立案當時的評估(**保留,供對照**)

**本票的命題逐字是**:「權威層端對端驗證**需要人工步驟,而該步驟沒有被寫下來**」。
⚠ **命題的主語是【那個步驟沒被寫下來】,不是【端對端沒做過】** ——
所以判結案要看的是**有沒有寫下來**。

### 三件事現在都有了

| # | 命題缺的那一半 | 現在的落點 |
|---|---|---|
| 1 | **工具** | `scripts/e2e_authority_layer.py`(manifest 標 `skip`)—— 每案例獨立 clone、移除 origin、釘 SHA、防誤用檢查、`--self-check`、`--smoke`、構造前提斷言、三道路徑斷言後才清理 |
| 2 | **操作說明** | **`docs/agents/authority-layer-e2e.md`** —— 含〈為什麼要 clone / 為什麼不能在正式 repo 跑 / 為什麼移除 origin〉、〈Jeff 的步驟〉(含先看 `upstream-roots.txt` 那一步)、**〈判讀表:哪些失敗不算規則擋下〉**、〈意外提交成功的處置〉、#12 與 #11② 的限制原文、〈構造前提要被斷言〉 |
| 3 | **一次真實執行** | **A 輪,2026-09-24,Jeff 本人**,受測 SHA `9726ab4c0954933048e26791c2f8040b3ca5ee85`,**17 項 = 16 commit + 1 靜態**,持久化於 **`docs/audits/2026-09-24-authority-layer-e2e-round-a.md`** |

**本票原文第三節逐字是**「這一步目前沒有任何文件寫下來……
**不在** `CLAUDE.md`、**不在** `docs/adr/`、**不在**票 133 的票面流程裡、
**不在**任何 skill 裡」,並預測「**下一個做 ④–⑦ 的人會重新撞一次**」。

⇒ **那個預測在 2026-09-24 被證實了兩次**,而兩次都是**因為沒寫下來**:

| 撞到的 | 現在寫在哪 |
|---|---|
| 普通 PowerShell 的 PATH 沒有 `sh` ⇒ 第 1 案就停 | `authority-layer-e2e.md`〈為什麼這支腳本一定要由 Jeff 跑〉 |
| 防誤用把 `git config --get`(讀)當成寫而擋下 | 同檔 §7.1〈讀與寫要分得開〉 |

### ⚠ 結案【不代表】的事

- **人工步驟沒有消失,只是被寫下來了。** 仍然需要人做的:
  `sh bootstrap.sh`(腳本可代跑,但那是 clone 的 local config)、
  **看 `~/.claude/upstream-roots.txt`**(G1 第一級擋 agent,只有人能做)、
  **判讀「意外提交成功」**、**決定要不要跑 B 輪**、
  **接受不接受自建的 `provenance.jsonl`**。
- **A 輪只做過一次、單機**;`--round B` **未實作**。
- 本票只解「**沒被寫下來**」;**它從來不是一張「把端對端做完」的票**。

### 提出結案建議時的判斷(**保留原文**)

**建議結案** —— 命題指名的缺口(**沒有文件**)已經有文件,
而且那份文件的內容是**被實測打出來的**(兩次撞牆都寫進去了),
不是憑空寫的一份操作手冊。

⚠ **但有一個相反方向的理由,一併寫出來供裁決**:
本票原文把缺口描述成「**下一個做 ④–⑦ 的人會重新撞一次**」,
而 ④–⑦ 那幾格(R6 / R5 兩個掛載點 / R4)**本輪一格都沒有走過端對端** ——
A 輪做的是批二的 #8–#13。
**所以「下一個做 ④–⑦ 的人」還沒有出現過,那份說明對他有沒有用【沒有被驗證】。**
要不要把「④–⑦ 也跑一次」設為結案條件,**是裁決,不是我能定的**。

> **裁決(2026-09-24)**:**那個理由不擋結案,轉成一筆候選** ——
> **票 144 §H**(批一 ①–⑥ 無真 `git commit` 端對端)。
> ⚠ **⑦ R4 不列入 §H**(保留工作樹語意,不是待修的格子);
> 說明文件對它的適用性**未另驗**,寫在上面的狀態段裡。

---

## 相關

- **票 133** —— A 輪的執行者與範圍(批二 #8–#13);
  〈真 hook 端對端 A 輪的結果〉在那張票。
- **`docs/agents/authority-layer-e2e.md`** —— **本票命題的答案**(操作說明)。
- **`scripts/e2e_authority_layer.py`** —— 工具(manifest 標 `skip`,理由寫在表裡)。
- **`docs/audits/2026-09-24-authority-layer-e2e-round-a.md`** —— A 輪 REAL 證據的持久化落點。
- **`docs/agents/commander.md` §六** —— 「REAL 層材料須由裁決者本人或 CI 產生,
  agent 自跑不算」的條文(來源:本輪裁決確認;轉述源於 2026-09-03,
  **未取得該日原始裁決文件**)。
- **票 144** —— 票 133 轉出的剩餘候選;**其 §H 是本票結案時轉出的那一筆**
  (批一 ①–⑥ 無真 `git commit` 端對端)。⚠ **§H 不含 ⑦ R4。**
- 票 133(批一 ② / ③)—— 本票的來源。
- `docs/adr/0009` —— `g1_verify.py` 的覆蓋那一步同樣「只有人能做」,**而那件事有明文流程**。
  (**本行只記兩者都存在一個人工步驟,不主張它們同類、也不主張該照搬。**)
