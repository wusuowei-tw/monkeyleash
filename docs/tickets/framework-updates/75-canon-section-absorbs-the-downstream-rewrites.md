# 75 — C 輪:正典段收容影音六處改寫,一份字面對所有 repo 為真

**狀態**:**implement**(2026-08-24 立案即動工,同日裁決)
**立案**:C 輪唯讀偵察(2026-08-24)→ 六處裁決 + 落地令(裁決者,同日)
**基準**:上游 `3e6b056` / 影音 `3729bb7` / 量化 `55b1f91`(三邊 `CLAUDE.md` 工作樹皆乾淨)
**交叉引用**:票 53(偵測器)、票 51(②③④ 由本輪吸收,票面收窄)、
影音 `poc-analyst-call-tracking/27`(六處的出生地)、票 69 ③(X1b 不照抄的理由)

---

## 題目

票 53 的偵測器 II 要求正典段兩邊逐位元組同,而影音票 27 裁過六處本地改寫。
裁 C:**上游改正典段,讓一份字面對所有 repo 為真**;影音之後 `--regenerate-canon`。
偵察量得:上游正典段 9570 chars / 影音 9827 / 量化 3449(sha 前 12 碼
`105d7a0d1cff` / `f4ae519f4c71` / `211ffbed85bd`)。

## 六處裁決(2026-08-24,逐處)

| # | 處 | 裁 | 內容 |
|---|---|---|---|
| ① | M9 證據檔句 | (i) 通用化 | 三帳本 + 「開影子模式的 repo 另有影子日誌」;`docs/adr/0012` 指標併同一括號(③ 的殘值) |
| ② | 「130 筆」例 | (i) | 留上游例,標「例(出自上游的一次批次分類):」—— 例子標了出處在任何 repo 都真 |
| ③ | 無影子模式附註 | 丟棄 | 前提句為假:`git show 8552830:CLAUDE.md` 全文零「影子」命中,觸發句當時已是通用句。定性為 M10 實例;更正登記進影音票 27(影音視窗做),不進 friction 新號 |
| ④ | X2「刻意不寫 N 次」 | (iii) 吸收 | 影音結構照收;**錨點修正 `F-075` → `F-011` / `F-021` 並引**(影音錯引 —— 兩邊 friction-log 的 F-075 都是「判錯對象」族);量化「先問兩件事(判定邏輯與唯一的觀測)」擴句併入 —— 更強形式且對三 repo 皆真 |
| ⑤ | D1 第三實例句 | (i) | 「每把這一段裝進一個 repo,那個行號都會再換一次」—— 比兩邊現行都準 |
| ⑥ | X1b | (i) | 補 R8 內容(生產程式碼不得 import `research/`)、「在下游擋過人」→「在安裝的 repo 實際擋過人」;無閉區間字面(H2)、無 friction-local 專屬引用 |

清單外三裁:**R1 列裁 A**(本輪吸收影音實測版;票 51 ②③④ 改標「已由 C 輪吸收」——
B 案會讓 regenerate 把修好的蓋回過窄版且中間無聲);「票 67 / 票 68」→
「**上游**票 67 / 票 68」(防下游誤讀成本地票號);I-5 分類表補列。
規則表頭不裁 —— 兩邊語意等價,regenerate 取上游版。

## 落地時的既定事實(開工前實測,理由欄照它寫)

- `.dev/provenance.jsonl` **不存在**(`ls .dev/` 只有 gate-exemptions / pipeline / test-runs)。
  `.gitignore` 對它有 negation(票 31 裁「進版控」,當時註明「本 repo 目前尚未產生,先立規則」——
  今天仍未產生)。分類理由照「尚未產生;產生後照票 31 進版控」寫,**不寫「gitignore 排除」**,那是錯的。
- `.dev/test-runs.jsonl` 存在且被 `.gitignore` 命中。
- `research/` 追蹤集合為空 —— 它是 R8 的判定對象,安裝的 repo 的目錄慣例,本 repo 沒有。
  ⑥ 的通用句引入這個反引號 token,**分類表因此多一列**(裁決令列兩列,本列是被
  已核准字面機械逼出的第三列,登記於此不另請示 —— 迴避寫法是靜默改已核准字面,更糟)。

## 同輪順序(裁定)

1. 上游落地推送(本票)→ CI 對帳(預測帶基準:collect 978,以 `3e6b056` 為底,本輪加 0 條測試)
2. 影音 `sync --apply` + `--regenerate-canon` 同輪(F-116 隨 friction-log copy 到達即不懸空)
   → 影音票 29 改驗收式收票(驗:句子在 + 出處可解 + canon sha 與上游一致);
   影音票 27 更正段登記 ③(「該附註前提未查證,C 輪查證推翻,裁決者當時核准同認」)
3. ⚠ 量化排「票 13 尾格收完之後」(2026-08-25)—— 量化 canon 只有上游 36%,
   regenerate 是大改動且 sync 會動測試集合改 CI 基準,不擠進觀察窗。
   量化本地五實例句留 friction,不進正典。

## Acceptance criteria

- [x] 正典段六處 + R1 列 + 兩字修落地(C2 `8b441ba`);H2 / I-4 綠(在全套內實跑,非推定)
- [x] `tests/test_canon_section.py` 分類表補三列,I-5 綠(同 C2)
- [x] 全套測試綠:**972 passed / 3 skipped / 3 xfailed,collected 978**(以 `3e6b056` 的 978 為底,+0 如預測)
- [ ] 推送 + CI 對帳(CI 預測:975 passed / 0 skipped —— 本機 972+3=975,Linux 上 symlink 三條會跑;基準同上)

## 票 51 收窄留痕(C3)

②③與⑤的 R1 那列改標「已由 C 輪吸收」;**④ 與 ① 明標仍開**
(④ 是實作面,改字吸收不到;① 的 R8 表列與否由票 51 自己裁)。
偵察回報裡「票 51 ②③④」的 ④ 是筆誤,C3 已按實況修正 —— 記在這裡,不改回報原文。
