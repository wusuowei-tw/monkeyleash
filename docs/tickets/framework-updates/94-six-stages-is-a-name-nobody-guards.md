# 94 — 「六站」是一個沒有人在守的數字,而定義檔有 8 個 id

**狀態**:**立案**(2026-08-30)。**不擋 8/31,不擋發文。**
**時鐘**:**上線之後** —— 上線當天不動守衛、不動 CI
**站別**:立案時 `implement`;動工前重設 `ticket_id`
**前置**:票 04(CLAUDE.md 只帶 FRAMEWORK 段)、票 51(條數不寫死)

> **票號取得時點:2026-08-30,重查最大號 93(同輪剛建)加一,
> 並確認全庫未使用 94。** 不提前占號(`F-118`)。

---

## 一、事實(2026-08-30 實測)

`.agents/pipeline-stages.yaml`(**唯一定義來源**)有 **8 個 `id`**:

```
idle 待命 / grill 拷問共識 / spec 寫規格 / tickets 拆票
research 探索研究 / implement TDD 實作 / review 代碼審查 / arch 架構掃除
```

扣掉 `idle`(待命,不是工作站)是 **7**。**都不是 6。**

而**全庫一致地寫「六站」**:

| 處 | 逐字 |
|---|---|
| `.claude/hooks/gate.py:2` | `"""六站流程閘門 — 單一判定邏輯,兩層共用。` |
| `CLAUDE.md:1` | `# monkeyleash(原 agent-gates)—— 六站流程閘門` |
| `README.md:5` | `Machine-enforced gates for a six-stage, test-first development pipeline —` |
| `README.zh-TW.md:5` | `六站、測試先行開發流程的**機器強制層**` |
| `tests/test_gate.py:2106` | `權威層沒接上 —— 六站閘門只剩前哨,commit 時不會判定。` |

## 二、🔴 真正的缺陷:**沒有任何測試在守這個數字**

票 51 D3 立過的判準是「**條數不寫死,指向 `rule_codes()`**」——
R 系列照做了(`rule_codes()` 從規則自己的訊息掃出來,現為 R1–R9)。

> ### **而「六站」這個數字寫死在五處散文與程式碼註解裡,
> ### 沒有任何一處從 `pipeline-stages.yaml` 算出來,也沒有任何測試比對它。**

**⇒ 站別數改了不會有東西出聲**,這正是票 51 那條判準要防的形狀,
而**它只被套用在 R 系列上,沒有被套用在站別上**(`F-148`:
一條紀律的範圍,大於守住它的那個機制的範圍)。

## 三、範圍與不做的事

**本票不裁「到底是幾站」。** 有三種可能,都要人決定:

| 可能 | 意思 |
|---|---|
| 名稱就是名稱 | 「六站」是產品名的一部分,不承諾等於 `id` 數 —— 那就**寫明它不是計數** |
| 數字該對上 | 那要決定 `idle` / `arch` 算不算站,並把數字改對 |
| 不寫數字 | 照票 51 D3 的先例,改成指向定義檔 |

**⚠ 文章沒錯。** 文章說「六站」與現行程式碼、README、CLAUDE.md **完全一致** ——
落差是 repo 自己的,**不影響發文**。

## 四、驗收

- [ ] 三種可能擇一,裁決寫進票面
- [ ] 若選「數字該對上」或「不寫數字」:五處散文同步
- [ ] **不論選哪一種,要有一條測試守住那個選擇** ——
      否則本票只是把一個沒人守的數字換成另一個沒人守的數字
