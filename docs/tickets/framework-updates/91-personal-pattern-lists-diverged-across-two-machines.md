# 91 — 兩台機器的個人 pattern 清單分歧,而已知項回歸對 K1 / K2 是紅的

**狀態**:**立案**(2026-08-28)。**上線前硬條件。**
**時鐘**:**8/29–8/30 解決,不排 8/31** —— 上線當天不做調查。
**⚠ 它不是票 84 那三件的第四件**,見「七、時鐘與清單邊界」。
**站別**:立案時 `implement`(票 89 收尾);動工前重設 `ticket_id`
**前置**:ADR `F-0015`「缺口二」與其裁決(C 必要 / B 並行 / A 排除)、
票 22(使用者層搬運)、票 28(機器相依的身分檔)、票 33(已知項登記)、
`docs/going-public-known-items.md` 的「K1 / K2 複驗」
**執行者**:**第三節是人(Jeff,在自己的終端機)**。agent 不碰 `~/.claude/`。
**本輪**:**只立案**。不改 pattern、不碰使用者層、不翻公開

> **票號取得時點:2026-08-28,動手當下重查 `docs/tickets/framework-updates/`
> 的最大號 90 加一。** 不提前占號(`F-118`)。

---

## 一、事實(2026-08-28 實測)

`docs/going-public-known-items.md` 登記 K1 / K2 為 `未清除`,期望「掃得到」。
實測 `leak_scan` 對 `22-machine-recovery-drill.md` 的
**12 個歷史版本 + 現樹全部 0 命中**。

三個替代解釋逐一排除:個人清單**有載入**(無警告)、K1 那一版**在**掃描範圍
(`f0c17fe` → `15ad81c`)、個人 pattern **只增未減**(16 → 18)。

**成因不是新的。** 登記表自己的 `### K1 的現況與已知偏差` 逐字寫著:

> **發現它的那條 pattern 在演練那台筆電上。**

ADR `F-0015`「缺口二」把形狀寫死了:

> **搬得過去,但兩台的清單各自長了,而沒有任何東西在比對它們。**
> `age!` 的 `!` 要求的正是人回頭看一眼,而那一眼**沒有攔住這次** ——
> 因為新 pattern 是在演練**之後**才加到筆電上的,匯出當下它還不存在。
> **單次搬運的檢查點,擋不住搬運之後才發生的分歧。**

## 二、本票是 ADR `F-0015` 裁決裡的 **B**,第一次執行

那份裁決已經定了三件事,本票不重新設計:

| | 內容 | 狀態 |
|---|---|---|
| **C** 已知項回歸 | 必要條件,掃不到就 refuse | **已在跑,而它正是這次出聲的那個** |
| **B** 清單對帳 | 兩台各出「條數 + 每條雜湊」(**不出原文**)比對,不一致就先合併 | **本票 = 第一次執行** |
| **A** 綁定單一機器 | **排除** | — |

> **C 沒有失效 —— 它正是這次把事情叫出來的東西。** 失效的是 B 從來沒被跑過。

## 三、🔴 給 Jeff 的極簡步驟 —— **約十分鐘,不需要跑完整的 26 步 machine-init**

**執行者:人。** 筆電只要能開機、能跑 python 就夠;
**不必**裝 hook、不必 clone、不必跑 `machine-init` 的任何一節。

### 3-1 兩台各出一份「指紋清單」(**不出原文**)

兩台各跑同一段(**輸出只有條數與雜湊前 10 碼,沒有任何 pattern 內容**)。
**10 碼不是 12 碼** —— 六之一記的三個期望指紋是 10 碼,長度不一致會產生
一組永遠對不起來的數字,而對帳的人會去找一個不存在的缺陷(`F-109`):

```
python -c "import hashlib,io,os; p=os.path.expanduser('~/.claude/leak-patterns.local.txt'); L=[l.split('#',1)[0].strip() for l in io.open(p,encoding='utf-8')]; L=[l for l in L if l]; print(len(L)); [print(hashlib.sha256(l.encode('utf-8')).hexdigest()[:10]) for l in sorted(L)]"
```

**實測結果(2026-08-28,裁決者側):桌機 18 條、筆電 19 條。**

> ### 🔴 **而差集是 5 條,不是 1 條。**
>
> `19 - 18 = 1` 會讓人以為「筆電多一條」。**實際是筆電獨有 3 條、桌機獨有 2 條。**
> **數字對得上不代表集合相同** —— 票 85:123 的硬條件,在這裡第二次兌現。
> 照「誰多誰少」去補,會補錯 3 條又漏掉 2 條,而**兩邊的條數看起來會變成一樣**。

### 3-2 比對 —— **不是看誰多,是看兩邊的差集**

把兩份指紋清單放一起,取**雙向差集**:

- 只在筆電有的 → **要合併回桌機**(這裡面就有 K1 / K2 那一條)
- 只在桌機有的 → **筆電缺的**,合併之後兩邊要一致

> ### ⚠ **合併不是覆蓋。**
> 桌機 18 條、筆電當時 16 條起算,而**兩邊各自長過**
> (票 84 §4-5 在桌機加了舊帳號那一條:35 → 43 行)。
> **照「還原」做會把桌機這 18 條裡的新增直接蓋掉,而那是無聲的。**

### 3-3 合併:**一行一次,用 `Add-Content` 追加**

> ### ⚠ **更正(2026-08-28):本節原本寫「走 age 匯出/匯入」。那是錯的。**
> 舊文保留在本票的「三之一」,照 `F-036`。

**做法(與已經發給 Jeff 的那份一致):**

對 3-2 算出來的「只在筆電有的」每一條,在**桌機**上各下一次:

```
Add-Content -Path "<家目錄>\.claude\leak-patterns.local.txt" -Encoding UTF8 -Value '<那一行>'
```

**一行一次,共三次。** 本次的差集是 **3 條**(見「六、合併後驗收」)。

- `-Encoding UTF8` 不是可選的 —— `Add-Content` 預設走系統 ANSI 碼頁,
  非 ASCII 的資料夾名寫進去會變成別的位元組,而 pattern 從此永遠打不中。
- 用**單引號**包 `-Value`:PowerShell 的雙引號會展開 `$`,而 regex 裡有 `$`。
- **追加,不是覆寫。** 桌機自己那 18 條一個字都不動。

### 3-4 為什麼**不**走 age(回答「有沒有不能取代的理由」:**沒有,而且它是錯的工具**)

| | age 匯出/匯入 | `Add-Content` 追加 |
|---|---|---|
| 語意 | `user_layer.import_` 的 docstring 逐字:「**把匯出的內容放進新機器的 `~/.claude/`**」—— **那是覆蓋** | **追加**,桌機既有的 18 條不受影響 |
| 對本題 | 🔴 **正是票 87 §七 剛警告的破壞性動作** —— 用筆電那份蓋掉桌機那份,而桌機獨有的 2 條會無聲消失 | 對 |
| 前置 | 要 age 公鑰 + **私鑰**,而私鑰是紙本(`machine-init.md` 硬條件二) | 無 |
| 保密面 | 明文不經螢幕 | 明文經過螢幕與剪貼簿 —— **但兩台都是 Jeff 自己的機器,那個字串本來就在兩台上** |

> **age 的價值在「搬一整份到一台沒有它的機器」,不在「把 3 行併進一份已經存在的清單」。**
> 票 39 裁決 2 鎖的是「**真實字串不進版控**」,而 `Add-Content` 沒有違反那一條 ——
> 它寫的是使用者層檔案,不是版控。

**⇒ 統一成 `Add-Content`。** 本節取代原本的 age 寫法。

### 3-5 合併完成之後(**照票 87 ④,不可略**)

- **雲端那份 `leak-patterns` 要重傳**(手動上傳,`machine-init.md` 硬條件一)
- **README 的 `sha256` 跟著更新** —— 不更新的話備份清單的核對步驟會誤紅
- **桌機獨有的那 2 條要搬去筆電** —— 見「七、登記不做」

## 三之一、~~舊文:走 age,不重打~~(2026-08-28 撤回,`F-036` 不刪)

~~`.agents/user-layer-manifest.txt:51` 標 `age!`;票 39 裁決 2 鎖死條款:
個人 pattern 只走 age 匯出。筆電端 `export --recipient`、桌機端 `import --identity`。~~

**撤回理由**:`import_` 是**覆蓋**不是合併(見 3-4),
而且我把票 39 裁決 2 的「真實字串不進版控」讀成了「搬運只能走 age」——
**那是把一條關於【目的地】的規矩,讀成一條關於【管道】的規矩。**

## 四、⚠ 這改變了票 87 的題目

見票 87 的併記:**這是備份第一次出現「正本不只一份」的情況。**
不只是「備份會過期」,還有「**備份可能只是其中一份**」——
而「還原」這個動作對後者是**破壞性的**。

## 五、本輪不做,但要登記的兩件

1. **`tests/test_known_items_regression.py` 的 4 個合成樣本全是 K3 形狀**
   (`OneDrive` 路徑)。所以那支「防過度擬合」的測試
   **偵測不到「pattern 根本不在」** —— 它只測得到在場的那些。
   **要等 pattern 回來才補得出 K1 / K2 形狀的樣本**,所以不在本輪修。
2. **行號錨與範圍欄過期**(登記表五之二):`:348` 不符合它自己的描述,
   blob 版本數 12 ≠ 表上的 7 / 8。**與 pattern 分歧是兩件事**,不要一起修。

## 六、🔴 合併完成之後要跑的驗收(**⚠ 合併目前尚未執行**)

| # | 驗什麼 | 期望 | 執行者 |
|---|---|---|---|
| **a** | **指紋比對** | 桌機 **21 條**,且含 `0a0d1878e3` / `8a4ef001a2` / `e600a01da3` | **人** |
| **b** | **正控** | 合成樣本 `exit=1` —— 確認掃描器仍活著 | agent |
| **c** | 🔴 **反控** | 一個**已知乾淨**的檔 `exit=0` —— 確認新加的三條**沒有把一切都變紅** | agent |
| **d** | **K1 複驗** | `22-machine-recovery-drill.md` **抓得到** | agent |
| **e** | **雲端重傳 + README `sha256` 更新** | 兩者一致 | **人** |

### 六之一、⚠ 指紋長度必須是 **10 碼**,不是 12

**本票 3-1 原本寫 `[:12]`,而 a 記的三個指紋是 10 碼**
(`0a0d1878e3` = 10 個十六進位字元)。
**兩種長度會產生一組永遠對不起來的數字**,而對帳的人會去找一個不存在的缺陷
(`F-109`)。**以 a 那三個為準:`[:10]`。**

### c 為什麼是硬條件

**新加三條 pattern 之後,「掃得到」不再是好消息的唯一解釋** ——
一條寫壞的 regex(例如不小心成為 `.*`)會讓**每個檔案都命中**,
而 d 照樣會綠。**只有 c 分得出「抓到它該抓的」與「什麼都抓」。**

> **正控證明它會叫,反控證明它不是一直在叫。** 少了後者,前者沒有資訊量。

### d 的判準:**這是量測,不是辨識**

抓到就代表「缺的那條在新加的三條之中」。
**不必辨認是哪一條** —— 辨認需要看 pattern 原文,而那是敏感資料,
且對本票的結論沒有增益。

### 收尾:改回登記表

d 抓到之後,`docs/going-public-known-items.md` 的 K1 / K2 期望欄
從 `應抓到 · 目前抓不到 · 已知缺口 · 票 91` **改回 `掃得到 ✅`**。

> **那不是「把紅燈洗綠」,是 `xfail(strict)` 的正常出口:**
> 缺口被補好 -> 回歸不合格 -> **有人被迫回來更新表**。
> 這一步發生了,才代表那個語意真的在運作。

## 七、時鐘與清單邊界

**時鐘:8/29–8/30。** 上線當天不做調查。

**🔴 8/31 若拿不到筆電**:照票 84「**任一件不過 = 順延。不是取消,也不是硬上**」
的同一條處理。判準逐字適用:**守的東西一旦漏出去就收不回來,而日期可以再挑一個。**

**⚠ 但它不是票 84 那三件的第四件。**
兩張清單各自的長度不寫在任何一張清單上,合起來數會出現
「到底幾件才算過」的歧義(`F-137`)。**本筆屬於 `going-public-known-items.md`,
不屬於票 84。** 正常情況下它在 8/29–8/30 結案,不會出現在 8/31 的勾選清單上。

## 七之二、登記不做:**桌機獨有的 2 條要搬去筆電**(時鐘 9/11)

3-2 的差集是**雙向**的:筆電缺 2 條(桌機獨有),桌機缺 3 條(筆電獨有)。
**本票只做「筆電 → 桌機」那 3 條**,因為它擋著 8/31。

**另外那 2 條掛筆電那一輪,時鐘 9/11**:

> **9/11 之後筆電是唯一在用的機器,而它現在抓不到舊帳號名**
> (票 84 §4-5 加的那一條在桌機上)。

**⚠ 不要把它讀成「不重要」** —— 它的時鐘只是比較晚。
現在不做的理由是**範圍**(本票的時鐘是 8/31,那 2 條不影響 8/31),
不是優先級。

## 八、本票不做

- **不改任何 pattern**、不碰 `~/.claude/` —— 第三節執行者是人
- **不修行號錨與範圍欄**(登記表五之二,另計)
- **不補 K1 / K2 形狀的合成樣本** —— 要等 pattern 回來(第五節)
- **不翻公開** —— 本票結案是翻公開的前置
