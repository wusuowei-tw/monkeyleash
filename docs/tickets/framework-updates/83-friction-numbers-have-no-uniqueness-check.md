# 83 — friction 號沒有唯一性檢查,而它已經撞過一次

**狀態**:**done**(2026-08-27)—— 11 條紅燈轉綠,全套 1043 passed,CI 綠。
~~**核准動工**(2026-08-26 裁決二)~~
**時鐘**:**8/28 開源** —— README 自稱 friction log 是「最可搬走的資產」,
而**撞號會被外人看到**。內部撞號是引用斷掉;開源之後撞號是**那份資產的可信度**。

**時鐘二(2026-08-26 裁決三):8/28 開源前 CI 必須是綠的。**

> 一個公開 repo 的第一印象是 CI badge,而**紅燈 commit 的用意是「還沒做完」,
> 不是「給陌生人看的狀態」**。本庫的紅燈先行慣例是**對內**的溝通方式,
> 它預設讀者知道那代表什麼 —— 開源之後那個預設不成立。

**若 8/27 收不完,8/28 之前二擇一**:

- **實作落地**(首選),或
- **把那 11 條標 `xfail`,並在票面寫明原因**

**標 `xfail` 要用 `strict=True`** —— 照票 04 那三條的先例:實作落地時它們會
XPASS,而 strict 讓 XPASS 算失敗,**強迫有人回來刪 marker**。
不加 strict 的話,11 條測試會安靜地留在那裡永遠不再有人看,
而那正是本庫已經記過的那個形狀(「一條長期紅的測試變成噪音」)。
**立案**:2026-08-26,`F-118` 預測的相撞實際發生
**站別**:動工時 `implement`,紅燈先行

---

## 事實(已經發生,不是假想)

```
721cb8f  (另一視窗)  F-122 票面裡的內容快照,是一份沒有人在守的副本
0b17cae  (本視窗)    F-122 R7 比 G1 第二級更寬 …          ← 同號
```

**兩則都經過 pre-commit,兩次都綠。**
權威層看得到檔案內容,但**不檢查 friction 號唯一性** —— 這一面從來沒有守衛。

處置照新訂的慣例(後到者改號):`F-122` → `F-123`,三處引用改指,原號留痕。
**而那是人做的,不是機器抓的** —— 抓到它靠的是我為了發下一個號去查最大號,
順手看到 `uniq -d` 回了兩個 122。**下一次沒有人去查最大號時,它不會被發現。**

## 為什麼這一條**適合**進權威層(理由要寫清楚)

**這句話會被拿去論證別的檢查也該進 pre-commit,所以判準要寫死:**

| 判準 | 本條 |
|---|---|
| **零誤報** | 兩個一模一樣的號碼就是撞號。**沒有灰色地帶** |
| **零判斷** | 不需要理解那兩則寫了什麼,只數字串 |
| **極便宜** | 一次 `grep` + 一個 set,對整份 log 是毫秒級 |

### 與 stale status 檢查的對照(這格是重點)

票 53 登記的「status 行要不要也有偵測器」**不該**用同一個理由進權威層:

> **那條誤報率高,所以只能當 lint;本條沒有灰色地帶。**

stale status 要判「票面說的與實際做的一不一致」,而「實際做了什麼」
本身要推論(commit 訊息?測試?檔案存在?)—— **推論會錯,而錯在權威層等於擋住做對事的人**,
那種規則最後會被整條關掉(票 24 的判準)。

**所以:進權威層的門檻不是「重要」,是「零誤報 + 零判斷 + 便宜」。**
重要但需要判斷的東西進 lint,不進權威層。

## 範圍:**只查重複,一條檢查一件事**

- ✅ **查**:同一份 `friction-log.md` 裡有沒有兩個相同的 `## F-NNN` 標題
- ❌ **不查連號**(缺號是正常的 —— 改號會留下空洞,見發號規則第 4 節)
- ❌ **不查格式**(標題長什麼樣不是本條的事)
- ❌ **不查跨 repo**(下游用自己的前綴,`TSI-001` 與 `F-001` 不衝突)

**一條檢查一件事。** 把連號與格式併進來的話,本條就不再是零誤報的
(缺號合法、格式會演化),而**混進一個會誤報的子判定,整條的可信度就跟著它走**。

## 實作註記

- 判定對象是**標題行**(`^## F-\d+`),不是內文提及 —— 內文引用重複是正常的
- **併記段不得誤判**:本檔已有一段 `## 併記於 F-118(…)`,
  它刻意**不**寫成 `## F-118 …` 正是為了不被本檢查判成撞號。
  這一格要有測試釘住(併記形態不算撞號)
- 下游 repo 的 `friction-local.md` 用自己的前綴,**判定要吃前綴不是寫死 `F-`**
- 失敗方向:讀不到檔案 → **fail-closed 當違規**(照家規)

## 驗收(兩方向)

- **方向 A**:人工造一份含兩個 `## F-999` 的 log → 擋,且訊息**點名是哪個號、哪兩行**
- **方向 B**:現行的 `friction-log.md` → 過(`uniq -d` 現在回空,已實測);
  且 `## 併記於 F-118` 那一段**不得**被判成撞號
- **負控**:缺號(如 `F-001` 跳到 `F-003`)**不得**觸發

## 本票不含

- stale status 偵測器(票 53 候選第二面,判準不同,見上)
- 票號 / ADR 號 / event id 的唯一性 —— **同一條判準適用,但一條檢查一件事**;
  要做另開票,理由見發號規則第 4 節那句「有效期到下一次有人寫入為止」
- 自動改號 —— **後到者改號是人的判斷**(誰先誰後要看 commit 順序,
  而機器擋下的時點是 commit 之前,那時順序還沒定)

---

## 落地(2026-08-27)

規則碼 **R9**,住在 `gate.py` 的 `check_friction_numbers()`,
接在 `mode_pre_commit()` 的 `check_legacy_list()` 之後。
**11 條紅燈全綠,全套 1043 passed / 3 skipped / 3 xfailed。**

### 判定用的正則,以及它為什麼長這樣

```python
_FRICTION_HEADING = re.compile(r"^##\s+([A-Za-z]+-\d+)(?:\s|$|[^\w-])")
```

**兩個條件一起,把「發一個號」與「提到一個號」分開**:

| 行 | 判 |
|---|---|
| `## F-118 甲` | **發號,算** |
| `## 併記於 F-118(…):乙` | 提到,不算(號碼不在 `## ` 之後的開頭) |
| `見 F-005 與 F-005 的討論` | 提到,不算(不是 `## ` 開頭) |

**本檔自己就有一段 `## 併記於 F-118(…)`** —— 它當初刻意寫成這樣,
正是為了不被本條誤判,而現在那個刻意有了機器在守。

### ⚠ 落地時被框架自己擋了兩次(這一格值得記)

加了 `R9` 之後,**兩條既有的元測試當場紅**:

```
FAILED tests/test_gate.py::TestRulesAreEnumeratedFromTheDefinition::test_every_rule_has_a_scenario_that_actually_triggers_it
    AssertionError: 這些規則沒有任何實測會觸發它們:['R9'] ——
    規則存在但沒被證明擋得住,跟沒有規則的差別只在讀碼的人心裡。

FAILED tests/test_claude_md.py::test_every_rule_that_exists_is_named_somewhere_in_the_canon
```

**我沒有去找它們,是它們來找我。** `rule_codes()` 從擋下訊息掃出規則碼,
所以 `R9` 一寫下去就自動進了清單,而那兩條測試各自問了一個問題:
**「它有沒有被實測證明擋得住」**與**「正典段有沒有提過它」**。

於是補了兩處(**不是為了讓測試變綠,是那兩件本來就該做**):

1. `verify_gates.py` 的 `SCENARIOS` 加 `scenario_r9`
2. `CLAUDE.md` 的規則表加 R9 那一列(含「為什麼這條在權威層而別的不在」)

> **這是「驗收條件會自己長大」的一次實證** —— 加一條規則,
> 驗收自動多兩項要求,而**不必有人記得**。
> 與本票自己的主題同構:兩者都是把「要記得」換成「會叫」。

### `scenario_r9` 的一個刻意選擇

它**不寫死 `F-001`**,而是先從目標 repo 的 friction log 讀出**第一個實際存在的號**
再去複製它。理由寫在該函式的 docstring:

> 每個安裝的 repo 用自己的前綴(`TSI-`、`TSA-`…),寫死上游的前綴會讓這個情境
> 在下游**永遠觸發不了**,而它仍然會印「擋下 OK」—— **那是最糟的一種綠。**

取不到號時 `SystemExit` 並明說「**這不是通過,是驗不到**」。

## 未做,登記

**`docs/agents/friction-local.md` 沒有被檢查。** 下游用它記專案層的號
(`TSI-`、`TSA-`),而它同樣會撞。

**本輪不做的理由是紀律不是遺漏**:那 11 條紅燈規定的是
`friction-log.md` 這一個檔,**加一個檔等於加一段沒有紅燈證明過的行為**。
要做的話先寫紅燈。**登記在此,不開票。**
