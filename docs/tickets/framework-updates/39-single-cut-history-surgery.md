# 39 — 單次開刀:歷史改寫

**狀態**:**票面已核可**(2026-08-15,一條修正:遠端 ref 驗收寫成類)。
**尚未執行** —— 阻塞於先決條件 P1/P2/P3/P5/P6。

**排程**:阻塞於 ADR F-0015 階二驗收通過。**不插隊,不搶跑。**
**性質**:**單次、不可逆**。人工執行為主,代理人只做量測與產出對照表。
**相關**:ADR F-0015(決定與驗收門檻)、`docs/going-public-known-items.md`(K1–K3)、
`docs/audits/2026-08-15-going-public-attack-surface.md`(委託書)

> **本票不是「怎麼跑 filter-repo」的教學。**
> 它要回答的是:**憑什麼可以動刀了**,以及**動完憑什麼相信動對了**。

---

## 執行門檻(照貼 Gemini 回件第 4 節,2026-08-15)

> **轉錄說明**:回件檔案抵達時編碼受損,本節為逐字轉錄。
> 一處(4.2 第 2 點的「提交測試證據」)原文字面模糊,依上下文還原,已標註。

### 4.1 約束 1:登記表作為「藏寶圖」的風險

- **現行緩解(公開前清除/泛化)是否足夠?**:**不足。**
- **分析**:Git 的核心是不可變性。即使檔案已修改,只要未利用 `filter-repo`
  將帶有洩漏的歷史 Commit 徹底剷除(包含 SHA),攻擊者仍可循索履查找舊物件。
  此外,若用新 Commit 覆蓋洩漏,該 Commit 的 Message(如 "Remove credentials")
  即為歷史路標。
- **要求**:登記表上的項目,必須在 `filter-repo` 執行、原 SHA 成為
  Dangling Object 並被 `git gc` 徹底清除後,方能視為安全。

### 4.2 約束 2:`filter-repo` 只跑一次的原則門檻

因「找齊」在原理上(見軸二)不可證明,為避免草率宣告完成,
執行 `filter-repo` 改寫全 SHA 前,必須滿足以下**硬性基準門檻**,方可動刀:

1. **多引擎交叉驗證(Two-Engine Consensus)**:
   禁止單點依賴 `agent-gates`。必須引入至少一套具備高熵值特徵庫檢測的
   業界標準掃描器(如 `Gitleaks` 或 `TruffleHog`)。
   透過雙引擎在歷史掃描的回報零(或全為已知豁免),方視為達標。
2. **入口面窮舉驗證**:
   必須提交測試證據〔字面模糊,依上下文還原〕,證明「軸一」所列之
   非常規入口(包含 `commit msg`、`author/committer`、`refs/stash` 等)
   已納入掃描器接管管線,無盲點遺漏。
3. **明確的妥協聲明**:
   必須於 ADR 中明確宣告:
   **「本機制不防禦刻意混淆(如 Base64 隱藏、字串分割拼接),
   僅防禦開發過程之意外洩漏。」**
   劃定此防線,以避免為追求無法達成的「100% 絕對涵蓋」而無限延宕整理作業。

### 本 repo 據此新增的執行要求

**4.1 的「要求」比原本的階段三多一步**:清除不等於 `filter-repo` 跑完 ——
舊 SHA 成為 dangling object 之後**還要 `git gc` 真的回收掉**。
已補進階段三與驗收(見下)。

**4.2 第 2 點與本票的 ★ 那一節同向**:窮舉要有**證據**,不是宣稱。
底本仍是**委託書的來源表**(裁決 1),不是回件的清單。

**涵蓋範圍**(裁決 5),妥協聲明的文字要涵蓋到:

- 編碼:**Base64 / URL 編碼 / Quoted-Printable / HTML entity**
- 跳脫:**shell · regex · YAML · Markdown**
- **字串分段**(被換行、串接、註解切開,重組後才是秘密)
- **擴充屬性**(xattr / ADS —— 不隨 git 傳輸,但要說出來)
- **同形字**(見下方「量到的更正」)—— 它**不在**已修好的那一格

#### 一處**已縮小**的範圍(裁決 1,候選 B)

個人 pattern 選了 B(分隔符可重複),所以**跳脫形式裡的一種**已被涵蓋:

| | 狀態 |
|---|---|
| **分隔符重複**(JSON 跳脫的 `\\`) | **已涵蓋**,不再屬於妥協聲明 |
| URL 編碼 / Base64 / QP / HTML entity | **仍不防** |
| 字串分段、串接 | **仍不防** |
| 同形字 | **仍不防** |
| shell · regex · YAML · Markdown 的其他跳脫語法 | **仍不防** |

**B 只涵蓋「分隔符重複」這一種寫法,不是「跳脫形式」整族。**
這句話要寫死 —— 「涵蓋了跳脫」與「涵蓋了跳脫的一種」差一個量詞,
而讀的人會自動把後者讀成前者。

#### 妥協聲明的**物理證據**(裁決 1 的配套)

原本釘在「跳脫形式不得命中」上的那顆釘子,**被 B 拆掉了** —— 它現在該命中。
釘子已**換位到 B 涵蓋範圍之外**:`tests/test_known_items_regression.py` 的
`test_url_encoded_forms_are_out_of_scope`(四種:`%5C` / `%2F` / 雙重編碼 / 小寫)。

> **妥協聲明的每一句都要有一條會為它出聲的測試,否則它只是散文。**

換位到 URL 編碼的理由:它是最容易被誤以為「順手也涵蓋了」的一種 ——
因為它看起來也只是「分隔符換個寫法」。**它不是。**
不一致的方向會是**聲明比實作保守**,而那種不一致沒有人會發現。

## ★ 階二「入口面窮舉」的驗收底本

> **底本是委託書的來源表,不是回件的清單。**(裁決 1)

`docs/audits/2026-08-15-going-public-attack-surface.md` 指定的三個來源 ——
**git 物件型別表、refs 命名空間、檔案系統中繼資料** —— 逐一走過才算窮舉。

理由是這條規矩最容易被倒過來用:拿回件的清單當底本,等於
**讓被審的那一方定義「審完了」的範圍**。回件答得好不好是另一回事;
**答漏的地方不會自己出現在回件裡**,只會出現在來源表上。

實例就在裁決 3:**大小寫不敏感比對是委託書列了、回件漏答的** ——
若以回件為底本,那一條會靜靜消失。

---

## 先決條件(全部要綠,缺一不動刀)

| # | 條件 | 現況 |
|---|---|---|
| P1 | ADR F-0015 **階二驗收**通過(分格版門檻) | 未跑 |
| P2 | 裁決 2/3/4 的掃描器改動**已落地**(見下) | 未做 |
| P3 | 雙引擎(Gitleaks + `leak_scan`)全歷史**先跑過**,結果併入登記表 | 未跑 |
| P4 | 執行門檻三條(回件 4.2) | **✅ 已到齊**(2026-08-15,阻塞解除) |
| P5 | 帶日期 zip 備份(ADR Q3 第一段③) | 未確認 |
| P6 | 下游 sync 凍結中(ADR Q3 第一段①) | ⚠ **窗已關**(2026-08-15 實測,見下) |

### P2 的內容(裁決 2/3/4)

| 來源 | 改動 |
|---|---|
| 裁決 3 | `leak_scan` **補三件,TDD 一批**:**NFKC 正規化**、**零寬字元過濾**、**大小寫不敏感比對** |
| 裁決 4 | **審查模式副檔名改白名單(deny-by-default)**;跳過清單照 Q5 **仍進報告** |
| 裁決 2 | 回歸驅動的新 pattern **須附同類異字變體測試**;個人 pattern **永遠只走 age 匯出** |

**第三件是委託書有列、回件漏答,自行補上的** —— 見上方 ★ 那一節。

#### ⚠ 量到的更正:**NFKC 折不動同形字**

回件第 3 節把「同形字 / 零寬字元」列在**同一列**,代價寫「低」,
處置寫「強制做 NFKC 正規化並濾除 ZWJ」。**那一列裡有兩種東西,而處置只蓋得住一種。**

P2 動手前實測(`unicodedata.normalize`):

| 形式 | NFKC | +濾零寬 | +casefold |
|---|---|---|---|
| 全形拉丁 `Ｃ` | ✅ | ✅ | ✅ |
| **西里爾 `С`(U+0421)** | ❌ | ❌ | ❌ |
| **希臘 `Ϲ`(U+03F9)** | ❌ | ❌ | ❌ |
| 零寬 ZWSP / ZWJ / ZWNJ / U+FEFF | ❌ | ✅ | ✅ |
| 全部大寫 | ❌ | ❌ | ✅ |

NFKC 折的是**相容性**差異(全形、連字),**不是視覺相似**。
同形字要另一套映射(Unicode TR39 的 confusables / skeleton)。

**所以:同形字歸妥協聲明,不歸「已修好」。**
把它放進已修好那一格,就是製造一格假涵蓋 ——
**而假涵蓋正是這整份 ADR 從頭到尾在打的東西。**
(是否值得為它引入 confusables 表,另案再議;本票不做。)

裁決 4 的方向要看清楚:**這是黑名單改白名單,而且方向是對的** ——
一般規則是「黑名單、fail-closed」(ADR 0003),但那裡的黑名單列的是
**不管的東西**;這裡的副檔名清單列的是**要跳過的東西**,
所以「白名單」在這裡等於 **deny-by-default**,同一個方向。
**別把它讀成 ADR 0003 的例外。**

裁決 2 的後半是**鎖死條款**:個人 pattern 的分歧問題,
**不得以「進版控」解決** —— 無論流程多卡。這一條寫在停止條件裡。

### P2 第五件(2026-08-15 核准加入):跳過清單的比對要帶邊界

**發現方式值得記**:它是**審查模式的「未內容掃描」清單**照出來的 ——
Q5 那條「清單必須是報告的一部分」在落地第一天就付了一次房租。

`SKIP_PARTS` 有一條裸的 `skills/`,為了跳過**鏡像**
(`.claude/skills/`、`skills/`,兩者 gitignore)而放,但比對是**子字串**,
而正典 `.agents/skills/` 的路徑裡剛好含有 `skills/`:

> **39 個進版控、會跟著公開走的正典檔,在兩種模式下都從來沒有被內容掃描過。**

#### 這一族在本 repo 早就被命名過

`g1_guard.py` 的 docstring 寫著:

> 前綴要帶邊界。`startswith("/tmp")` 會命中 `/tmpdata`、`/tmp_backup`
> —— **與 `.gitignore` 的 `skills/` 缺前導斜線同一族**。

**教訓學在一個地方,而隔壁的資料從來沒有回頭重掃** —— F-082 的形狀,
只是這次「隔壁」是同一個 repo 裡的另一個模組。
正確形狀也早就有:`gate.py:is_source_path` 取 `r.split("/")[0]`,根錨定 + 分段。

#### 家族掃(條件 1)找到的受害者不只一個

| 形狀 | 舊行為 |
|---|---|
| `.agents/skills/**`(正典) | **被吞** |
| `src/myskills/**`、`docs/skills-guide.md` | **被吞**(名字結尾剛好是標記) |
| `vendor/foo.git/**` | **被吞** |
| `a/my.cache/**` | **被吞** |

#### 修法:兩種錨定,不合成一種

- `SKIP_ROOTS = ("skills/", ".claude/skills/")` —— **根錨定**。
  鏡像的位置是構造決定的,不會浮動
- `SKIP_PARTS = (".git/", "__pycache__/", ".pytest_cache/", ".cache/")` ——
  **任何深度,但兩側帶 `/` 邊界**

判準:**這個東西是「某個固定位置的目錄」,還是「任何深度都算」?**
合成一種就是把兩個不同的問題用同一個式子答。

#### F-082 回掃(條件 3):以前犯過幾次

| 量到的 | 值 |
|---|---|
| 正典底下的歷史物件 | **39 blob + 27 tree** |
| 相異 blob 路徑 | **39**,每個路徑**恰好一個版本** |
| 只活在歷史裡的 blob 路徑 | **0** |
| 還原後仍被跳過的 | **0**(還原目錄自身不得命中跳過清單,否則回掃又是一輪假綠燈) |
| **命中** | **0** |

> 那 27 個是**目錄(tree)**,不是消失的檔案 —— 這一行原本我寫成
> 「只活在歷史裡的 27」,查證後更正。**寫錯數字比沒寫更糟**:
> 落地紀錄會被引用,而引用的人不會回頭重驗。

**結論:以前犯過 0 次。** 但那是運氣 —— 這 39 個檔從進版控起就沒被掃過,
乾淨是因為它們的內容剛好沒有秘密,不是因為有東西在守。

### 順序性要講白:P2 必須在 P3 之前

---

## P3 落地紀錄(2026-08-15)—— 雙引擎全歷史,**唯讀**

**未執行任何 `filter-repo` 指令,含 dry-run。** 兩引擎各自完整跑一次,分開記錄。

### 引擎一:`gitleaks` v8.30.1

實際指令(以絕對路徑呼叫,未改 PATH):

```
<家目錄>\tools\gitleaks\gitleaks.exe git <repo>
    --log-opts=--all
    --report-format json --report-path <scratch>\gitleaks-history.json
    --exit-code 0 --redact
```

`--log-opts=--all` 是關鍵:預設只走目前分支的 log,而遠端另有一條 wip 分支
—— 少了 `--all` 就少掃一整條 ref(正是自查撿到的第四條)。
`--redact` 讓報告本身不留明文(掃描器的輸出是外流面,F-066/067)。

| | |
|---|---|
| 掃描 | **80 commits**、~1,387,161 bytes(1.39 MB)、753 ms |
| **命中** | **0** |
| 退出碼 | 0(刻意用 `--exit-code 0`:命中靠報告內容表達,不靠退出碼) |

### 引擎二:自家 `leak_scan`,**審查模式**(`--review`)

| 掃描面 | 數量 |
|---|---|
| blob 版本 | **401** |
| commit 訊息(**不是 blob**) | **81** |
| tree 路徑名(Q4-A) | 153 條,彙整成 1 檔 |
| **送進掃描器** | **483 檔** |
| 實際內容掃描 | **478 檔** |
| 未內容掃描 | **5 檔** |

**命中 50,分類:**

| 類 | 數 |
|---|---|
| 掃描器自身(`leak_scan.py` / `leak-patterns.txt` 的歷史版本) | **50** |
| 已知項 K1 / K2 / K3 | **0** |
| **新發現** | **0** |

**未內容掃描的 5 檔逐筆**(Q5:只報「5 個」等於沒報):

```
.githooks/pre-commit  @2bc76c6   非白名單副檔名(無副檔名)
.githooks/pre-commit  @4b8c58d   非白名單副檔名(無副檔名)
.gitignore            @98a0eb4   跳過清單(類別豁免)
.gitignore            @99e3b3f   跳過清單(類別豁免)
.gitignore            @e7999cb   跳過清單(類別豁免)
```

**掃描器沒有入口的面**(另計,不混進命中數):身分欄位 3 個,
其中一個是真名 + 私人信箱。**兩個引擎都沒有它的入口。**

### 差集(條件 3)—— **這一段才是雙引擎的價值**

| 差集 | 數量 | 內容 |
|---|---|---|
| 自家 \ gitleaks | **50** | 全部是**掃描器掃到自己**,構造上的假陽性 |
| gitleaks \ 自家 | **0** | — |
| 兩者交集(真實命中) | **0** | — |

#### ⚠ 這個「0」不是共識,是兩個沉默

差集的價值來自「**一方有另一方沒有**」。而當**一方的結果集本身是空的**,
差集必然為空 —— **空差集在這裡不帶任何涵蓋資訊**。

更要緊的是對照現實:

> **兩個引擎都回報零,而 K1 / K2 / K3 三筆已知項確實存在**
> ——K2、K3 就在**現樹**裡,`git grep` 找得到。

所以回件 4.2 的**條件 1 單獨看,今天就會過**
(「雙引擎在歷史掃描的回報零(或全為已知豁免)方視為達標」——
自家那 50 筆全是已知豁免,gitleaks 是零)。
**而它會過的那一刻,repo 並不乾淨。**

### 已知項回歸(Q2-C)—— **它擋住了**

| 項 | 期望 | 實際 | 判定 |
|---|---|---|---|
| K1 | 掃得到 | **掃不到** | ✗ **refuse** |
| K2 | 掃得到 | **掃不到** | ✗ **refuse** |
| K3 | 掃得到 | **掃不到** | ✗ **refuse** |

**三條全紅,所以門檻不通過,刀不能動。**

這正是裁決把 Q2-C 定為**必要條件**的價值:
**條件 1 會放行,是 Q2-C 擋下來的。** 兩者不是重複,是互補 ——
條件 1 問「引擎有沒有找到東西」,Q2-C 問「引擎**找不找得到我們已經知道的東西**」。
前者的零可以來自乾淨,也可以來自瞎;**後者能分辨這兩件事。**

### ⚠ P6 實測(2026-08-15,唯讀,未跑 sync):**同步窗已關**

票面原本寫「本 repo `.dev/provenance.jsonl` 不存在,P6 的凍結就是為了讓這個
數字維持在 0」。**那個 0 從一開始就不成立** —— 本 repo 沒有 provenance,
是因為它是**上游**;要問的是**下游有沒有記著我們的 sha**,而它們有。

| repo | provenance 行數 | **相異檔案路徑** | 相異 upstream commit |
|---|---|---|---|
| 影音 | 23 | **23** | 1(`37f6bfa`) |
| 量化 | 298 | **95** | 4(`4a8ca4d` / `e51c8e3` / `eb47500` / `f19a418`) |
| **合計** | 321 | **118** | **5** |

**那 5 個 sha 在 agent-gates 全部解得開**(逐一 `git cat-file -e` 驗過,
均為 2026-08-13 的 commit)。所以:

> **`filter-repo` 一跑,這 5 個 sha 全部變成孤兒,
> 兩個下游 repo 合計 118 個檔案的 R3 provenance 豁免同時失效** ——
> 而失效的形狀是 `upstream_backed()` 落到「上游物件問不到」那一態,
> 下游從此對這些同步成品要求本地紅燈,**而那是做不到的**(F-074 開這條路的理由)。

量化那邊的分布值得單獨看:`.agents/skills` 佔 39 筆 ——
**正是票 39 P2 第五件那 39 個正典檔**。

#### 凍結仍然有意義,但意義變了

窗關在凍結**之前**,所以凍結**擋不住已經發生的 118 筆**,
它擋的是**這個數字繼續長**。原票面把凍結寫成「維持在 0」,
那句話已作廢(F-036:留痕不改寫)。

#### 這一項現在是開刀的**主要成本**,不是備註

開刀前必須先決定 118 筆怎麼辦。可見的方向(**不在此決定**):

- 開刀後在兩個下游**重跑 sync**,讓 provenance 記新的 sha
- 或在開刀時同步產出 **舊 sha → 新 sha 的對照**,由下游改寫 provenance
- 或接受下游那 118 檔暫時失去豁免,逐一走本地紅燈

三條路的成本差很多,而且**都要下游配合** —— 這件事本 repo 單方面做不完。

### P3 後續:補個人 pattern(2026-08-15,唯讀,未動歷史)

**新 pattern 條數:1**(個人清單 16 → 17 條。原文不記錄於版控 ——
個人 pattern 只走 age 匯出,裁決 2 鎖死條款)。

#### 為什麼只需要一條

盤點形狀時發現 **K1 與 K2 是同一類**:都是「OneDrive 底下的**真實資料夾名**」,
而**字串完全不同**。所以一條結構性的 pattern 同時打中兩者 ——
**這件事本身就是防過度擬合的實證,而且用的是真實資料不是合成資料。**

| 樣本 | 結果 |
|---|---|
| **真樣本** K1 | ✅ |
| **真樣本** K2 | ✅ |
| **真樣本** K3 | ❌(設計如此,見下) |
| 合成:換資料夾名 | ✅ |
| 合成:換資料夾名(非拉丁) | ✅ |
| 合成:換深度 | ✅ |
| 合成:換分隔符(正斜線) | ✅ |
| **反樣本**:角括號佔位符 | 不中 ✅ |
| **反樣本**:星號遮罩 | 不中 ✅ |
| **反樣本**:把 OneDrive 當產品名的散文 | 不中 ✅ |

反樣本那一組是刻意加的:**誤判率是涵蓋率的一部分,不是另一件事** ——
一條把每份提到 OneDrive 的文件都擋下來的 pattern,會被關掉。

**誤判面實測**:現樹命中 2 處、全歷史 8 處,**全部都是 K1/K2 自己的字串**,
零誤判。

合成樣本寫進 `tests/test_known_items_regression.py`(**不寫進 pattern 清單** ——
清單只放真正要守的東西);檔內**一個真實字串都沒有**,只有結構相同的假資料。
該檔在 portable-manifest 標 `skip`(它驗的是這台機器的個人清單)。

#### Q2-C 轉綠證據

```
K1  期望=掃得到  實際=掃得到  ✓
K2  期望=掃得到  實際=掃得到  ✓
K3  期望=掃得到  實際=掃不到  ✗
```

#### K3:待裁的一個字元

K3 的真樣本是 **JSON 跳脫形式**(路徑分隔符是連續兩個反斜線)。
照裁決 4,pattern 只打未跳脫形式 —— 所以 K3 維持紅,**這是設計不是漏**。

**要讓它綠,差別只有分隔符字元類上的一個 `+`。** 兩案都已量:

| | K1 | K2 | K3 | 現樹命中 | 全歷史命中 | 誤判 |
|---|---|---|---|---|---|---|
| **A 單分隔符**(已實作) | ✅ | ✅ | ❌ | 2 | 8 | **0** |
| **B 分隔符可重複** | ✅ | ✅ | ✅ | 3 | 38 | **0** |

B 多出來的 30 處**全部是 K3 自己的字串**在 friction-log 的歷史版本 ——
**沒有任何一處是誤判**。合成樣本與反樣本兩組在 A/B 之下結果相同。

所以 B 的成本是:**多 30 筆同一個已知項的重複命中,零誤判**;
收益是 K3 轉綠、且跳脫形式從妥協聲明裡移出一部分。
**不自行擴張範圍,回報待裁。**

#### K2 現樹遮蔽(裁決 2,已執行)

加了這條 pattern 之後,`test_the_shipped_tree_is_clean` 一度轉紅,
且**只紅在一處**:`22-machine-recovery-drill.md:291`,也就是 K2 本身。
裁決者明示授權後已依票 32 整行遮蔽形式處理、留記號,現樹恢復乾淨。

##### ★ 遮現樹 **≠** K2 已清除

> **歷史版本仍在,Q2-C 對 K2 的階段判準續盯歷史那一筆,
> 不得因現樹乾淨而翻到「已清除 → 須掃不到」那一邊。**

這一句寫在這裡是為了擋下一輪的誤讀,而誤讀的路徑很短:
回歸若拿 `HEAD` 比對,遮完現樹的那一刻判準會**自己翻邊** ——
於是回歸從此驗的是「遮乾淨了沒」,而**再也不驗掃描器的涵蓋**。
**涵蓋才是 Q2-C 的全部目的**,那一翻就把它整個換掉了,而且測試仍然全綠。

處置:登記表為 K2/K3 各記一個**歷史錨**(最新一個仍含該字串的 commit) ——
K2 = `da47808`(該檔 8 個版本中 5 個命中)、K3 = `5b58e85`(31 中 30)。
回歸拿歷史錨比對;現樹另有兩條**反控**(遮乾淨了沒),**並存而不取代**。

### 一個沒有歸因的數字

自家引擎的自身命中,票 33 起草時量到 **42**,這次是 **50**。
中間**同時**發生了兩件事:P2 加了大小寫不敏感那一輪,以及這段期間多了幾個
commit(`leak_scan.py` 因此多了幾個歷史版本)。
**我沒有把兩者拆開量**,所以不宣稱是哪一個造成的 —— 記為未歸因。

否則「找齊」這一步用的是**還沒修好的**掃描器 ——
而找齊只有一次機會(刀只能下一次),用舊掃描器找完就等於把
NFKC / 零寬 / 大小寫 / 白名單那四項的價值全部丟掉。

---

## 自查三條(裁決 7,已量 2026-08-15)—— 全部乾淨

一指令級,結果進落地紀錄。

| # | 項目 | 實測 | 對開刀的意義 |
|---|---|---|---|
| 甲 | **symlink(mode `120000`)** | **全歷史 0 筆**。全歷史 tree entry 模式只有 `100644`(394)與 `000000`(1,刪除標記);現樹 149 檔全 `100644` | symlink 的「內容」就是**目標路徑**,而目標路徑可以是絕對路徑 —— 這個入口在本 repo **從來沒被用過**,零工作量 |
| 乙 | **`.gitattributes`** | 存在,歷史上**只有 1 個 commit 動過**。內容純粹是行尾政策(`text=auto`、`*.sh`/`*.py`/`.githooks/*` 強制 LF、字型與 png 標 binary) | **無 `export-ignore`、無 filter / diff driver** —— 不改變 push 帶走什麼,也不含任何路徑或身分資訊 |
| 丙 | **GPGsig / `refs/replace` / submodule** | 簽章:76 個 commit **全部 `N`(未簽名)**;`refs/replace`:**0 筆**;`.gitmodules`:現樹無、**歷史上也從未出現**;`submodule status`:0 行 | 改寫**不毀簽章**、無 replace 物件會指向孤兒、無子模組指標要跟著改。**三項皆零成本** |

> **三條全乾淨的意義不是「不用做」,是「做了、寫下來了」** ——
> 與 Q5 的跳過清單同一條規矩:**「查過是空的」與「沒查」在報告上必須分得開。**

### 量測時撿到第四條 ⚠(不在裁決指定的三條裡)

**遠端存在第二條分支**:`refs/heads/wip/f062-installer-defaults`

該分支相對 `master` **有 0 個獨有 commit**(它是 master 的祖先),
所以它**不影響「要清什麼」**。但它影響**「清乾淨了沒有」**:

> `filter-repo` 改寫本機所有 ref,**遠端那條 wip 分支不會自己消失**。
> 它留在遠端,就把**改寫前的整段物件圖釘在那裡** ——
> 於是 force-push 了 master,舊歷史仍然可達,而**看起來像是清乾淨了**。

處置(裁決:**寫成類,不寫成那一條分支**):

> **開刀後以 `git ls-remote` 列出遠端全部 refs,逐一確認每一條都指向
> 改寫後的新物件圖,且沒有任何 ref 可達舊物件。**
>
> `wip/f062-installer-defaults` 是**本類的已知實例**,不是這條驗收的全部。

寫成類的理由與 F-085 同:**修的時候人看到的是那一條分支**,
而驗收要問的是「**遠端還有幾條 ref**」。今天是一條 wip,
明天可能是一條 tag、一條 PR ref、一條別台機器推上去的分支 ——
**逐一列出**才擋得住那些,點名一條擋不住。

### 其他已量的面(補完自查的底)

| 面 | 實測 |
|---|---|
| `stash` | **0** |
| `.gitattributes` 的 `export-ignore` | **0 條**(見自查乙) |
| 本機 ref 種類 | `refs/heads/master` + 三條 `refs/remotes/origin/*` |
| 遠端實際分支 | `master`、`wip/f062-installer-defaults` |

---

## 工具(2026-08-15 已裝,**只驗版本,未執行任何改寫**)

| 工具 | 版本 | 來源 | 位置 |
|---|---|---|---|
| `git-filter-repo` | **2.47.0**(PyPI) | `pip install git-filter-repo` | Python `Scripts/`(**本來就在 PATH**,未改 PATH) |
| `gitleaks` | **v8.30.1** | GitHub 官方 release(URL 見下) | `<家目錄>\tools\gitleaks\gitleaks.exe` —— **repo 外固定資料夾,絕對路徑呼叫,未改 PATH** |

```
https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_windows_x64.zip
sha256 d29144deff3a68aa93ced33dddf84b7fdc26070add4aa0f4513094c8332afc4e
```

雜湊**與官方 `gitleaks_8.30.1_checksums.txt` 逐字比對相符**才解壓;
腳本在對不上、找不到 checksums 檔、或目標已存在時一律**中止**,不覆寫。

**版本號一處說明**:`git filter-repo --version` 印的是內部識別碼
(`a40bce548d2c`),不是語意版本;**以 PyPI 的 `2.47.0` 為準**,
兩者都記,免得將來對帳時以為裝錯了。

### ⚠ 防毒曾攔截 gitleaks,裁決者放行,**系統沒有留下攔截紀錄**

**這件事本身要進紀錄。** 沒有紀錄的放行不能只活在記憶裡 ——
半年後看到這支 exe 的人,不會知道它曾經被攔過、被誰放行、憑什麼放行。

處置:**不重裝、不換來源**,改做事後驗證鏈。

#### 驗證鏈(2026-08-15,四環全通)

`checksums.txt` 列的是**壓縮檔**的雜湊,不是裡面 exe 的雜湊 ——
所以要重抓一次官方 zip(**抓到記憶體,不落地、不覆寫任何東西**),
先驗 zip、再從 zip 取出 exe 算雜湊,才有東西可以跟磁碟上那支比。

| 環 | 值 |
|---|---|
| 官方 `gitleaks_8.30.1_checksums.txt` 記的 zip | `d29144deff3a68aa93ced33dddf84b7fdc26070add4aa0f4513094c8332afc4e` |
| 本次重抓 zip 實算 | `d29144deff3a68aa93ced33dddf84b7fdc26070add4aa0f4513094c8332afc4e` |
| 官方 zip 內的 `gitleaks.exe` | `17157e2ee8b76fc8b1d8bee607a250e34b8a8023c8bc81822d4b5ee4d78fcb7c` |
| **磁碟上那一份** | `17157e2ee8b76fc8b1d8bee607a250e34b8a8023c8bc81822d4b5ee4d78fcb7c` |

**逐字元相符**,大小同為 22,575,104 bytes。
結論:**防毒沒有改動那支執行檔**;它還是官方那一支。

判準:**「它還在原地」不等於「它沒被動過」。** 隔離後還原是常見流程,
而還原的東西不保證位元組相同 —— 所以驗的是內容,不是存在。

#### `git-filter-repo` 是否也被攔

**沒有證據顯示被攔,但這是「沒看到」不是「沒發生」** ——
代理人看不到防毒的事件記錄,只能報可觀察的:

- `pip install` 全程正常完成,無中斷、無錯誤輸出
- wheel 的 `RECORD` **逐檔驗雜湊:11 檔全驗、0 不符、0 缺檔**
  (`RECORD` 記的是安裝當下的內容雜湊 —— 不符代表**裝完之後被動過**)

所以能斷言的是**安裝後未被改動**,不能斷言**安裝過程未被攔**。
兩者的差別要寫出來,否則下一個讀的人會把前者讀成後者。

安裝要留痕(版本號進妥協聲明)—— 理由與 ADR Q7 同:
**驗收要說得出用的是哪個版本的哪個引擎**,否則「雙引擎共識」是一句沒有內容的話。

### 開刀前備份(護欄 1,已完成)

```
<家目錄>\OneDrive\<備份資料夾>\agent-gates-pre-surgery-2026-08-15.zip
1058 項 / 壓縮後 4.9 MB(5,102,396 bytes)
sha256 e0b2eb805b306d9503529840dcf797d81cea00982e33fc32cfa7b7d8515e7675
testzip OK;含 .git/(905 項)
```

**含 `.git/` 是重點** —— 開刀改寫的就是歷史,少了它的備份在單刀那天等於沒有備份。
只排除快取目錄。備份腳本在目標檔已存在時**中止不覆寫**
(備份 = 讀取 + 複製;涉及覆蓋就停,票 22 R5)。

### 活標本:本節第一版被 leak_scan 擋下,而它同時證實了 K3

第一版把兩個路徑寫成完整絕對路徑,`leak_scan` 命中**個人 pattern** 擋下 commit。
擋得對,已改成 `<家目錄>` 佔位符 —— **沒有加豁免**。

真正的收穫是對照組:

| 形式 | 出處 | 掃描結果 |
|---|---|---|
| `C:\Users\…`(未跳脫) | 本節第一版 | **命中,擋下** |
| `C:\\Users\\…`(JSON 跳脫) | K3(`friction-log.md:803`) | **零命中** |

**同一個字串、同一組 pattern、同一支掃描器,差別只在跳脫。**
這就是 K3 為什麼活到現在,也是為什麼跳脫形式必須寫進妥協聲明 ——
P2 收的 NFKC / 零寬 / 大小寫**都不碰跳脫**。

---

## 執行順序

**#1–#7 對應 ADR F-0015「開刀前要量的清單」。** 順序不可交換。

### 階段零:K3 的現樹遮蔽(裁決 6,**可先做**)

K3(`docs/agents/friction-log.md:803`)**裁決:遮**。

- 形式照**票 32 的整行遮蔽**,**留記號** —— 不是刪掉那一行
- **裁決者明示授權**動這段引用的證據原文(TSI-002 的例外,逐次授權)
- **現樹遮蔽先做;歷史那一份在階段三連同 K1/K2 一起清**

分兩個時點的理由:現樹遮蔽是可逆的、隨時能做;歷史清除只有一次機會。
**能先做的先做,不要把可逆的動作綁進不可逆的那一刀。**

### 階段一:找齊(刀還沒下)

1. 裝 `git-filter-repo` 與 `gitleaks`,記版本
2. **雙引擎全歷史掃描**(P3):兩個引擎各跑一次全歷史,
   **取聯集不是交集** —— 交集是「兩個都認得的」,那比任何單一引擎都窄
3. 結果併入 `going-public-known-items.md`;**新命中一律先定性再登記**
4. 對每一筆登記項確認**期望值方向**(未清除→掃得到;已清除→掃不到)
5. **入口面窮舉**照 ★ 那一節:走**委託書的三個來源表**,逐項留紀錄

### 階段二:量參照(刀還沒下)

6. **#1** `.agents/legacy-no-redlight.txt` 的 `# go-live: 5180678` ——
   **9 條目**。開刀後該 sha 消失,`gate.py:1098` 會讓 **9 條全部變違規** →
   R6 擋掉所有 commit。該檔 **gitignored,不會自動更新**。
   **產出**:舊 sha → 新 sha 的對照,以及重新指向的步驟
7. **#2** 下游 provenance —— **已量到,不是零**(2026-08-15,見下方 P6 專節)
8. **#3/#4** 追蹤檔裡的 sha 形狀字串(**15 檔**)—— 多為散文引用。
   測試裡的是 `deadbeef` 假資料(安全)。**產出**:要改的引用清單
9. **#5** `.dev/gate-exemptions.jsonl` —— 已確認 `at_commit` 是**布林值不是 sha**,
   **不受影響**。本項結案,不必重量
10. **#6** 登記表自己的 sha(K1 = `3a265f2`)—— 開刀後失效,**必須同步改**
11. **#7** 遠端與既有 clone —— 列出遠端**所有** ref(含自查撿到的 wip 分支)

### 階段三:動刀(**只跑一次**)

12. 備份確認(P5),**異地副本先於手術**(具名原則:順序決定安全性)
13. `filter-repo` 一次跑完**全部**改寫:身分欄位 → `noreply`、
    **K1 / K2 / K3 三筆的歷史內容清除或泛化**。**不分兩次**
14. **`git gc` 回收 dangling object**(回件 4.1 的「要求」)——
    `filter-repo` 跑完只是讓舊 SHA 變成孤兒,**孤兒還在 `.git/objects` 裡**,
    而 clone 有可能帶走它。**沒 gc 就不算清除**
15. 重寫 `#1` 的 go-live sha、`#6` 的登記表、`#3/#4` 的散文引用
16. force-push `master`;**處理遠端全部 refs**(見上方「寫成類」那條)

### 階段四:驗收

17. 雙引擎全歷史**重跑**:登記表逐筆比對**期望值**
    (已清除的要**掃不到**)
18. 全套測試綠;R6 不再報違規
19. **`git ls-remote` 列出遠端全部 refs**,逐一確認指向改寫後的新物件圖,
    **無任何 ref 可達舊物件**
20. **確認 dangling object 已回收** —— 回件 4.1 的判準是
    「原 SHA 成為 dangling 並被 `git gc` 徹底清除後,方能視為安全」
21. **自查三條重跑**(裁決 7)—— 開刀會產生新的物件,
    「開刀前乾淨」不蘊含「開刀後乾淨」
22. 妥協聲明定稿:**回件 4.2 第 3 點的原句 + 裁決 5 的涵蓋清單
    + 同形字(量到的更正)+ 引擎版本 + 明文排除項**

---

## 驗收範圍:**「push 會帶走的」有限清單**

照 ADR F-0015 階二門檻(分格版)。**範圍是列舉的,不是「所有東西」** ——
無限的範圍等於沒有範圍。

**在範圍內**:檔案模式位元、GPG 簽章、`refs/replace`、tag、notes、
submodule 指標、時間戳、以及**所有遠端 ref**。

**明文排除,理由寫在驗收報告上**:

| 排除項 | 理由 |
|---|---|
| `reflog` | **push 不傳輸** |
| `stash` | **push 不傳輸** |
| `.git/` 底下的本機檔(`config`、hooks…) | **push 不傳輸** |

> 這三項歸**本機衛生**,不是公開審查的對象。
> 明文寫出來,是為了讓「沒查」與「查過沒事」在報告上分得開 ——
> 與 Q5 的跳過清單同一條規矩。

**時間戳的時區**:**聲明接受**。它可推斷作息與大致地理位置,
但改寫時間戳會讓整段歷史失去時序意義,代價大於收益。**寫進妥協聲明。**

---

## 停止條件

- 執行門檻三條(回件 4.2)任一條未滿足 → **停**
- 出現「把個人 pattern 進版控就解決了」的念頭 → **停**。
  裁決 2 是**鎖死條款**:無論流程多卡,一律走 age 匯出
- 掃描器改動(裁決 2/3/4)未落地 → **停**,不得先跑 P3
- 雙引擎結果不一致而無法定性 → **停**,登記為未決項,不自行判斷「應該沒事」
- 任何一筆登記項的期望值**說不出來** → **停**(ADR:說不出期望值本身就是 refuse 的理由)
- 下游 sync 凍結已被打破(P6 不成立)→ **停**,先重量 `#2`

## 明確不做

- 不改寫時間戳 / 時區(已聲明接受)
- 不處理 `reflog` / `stash` / `.git` 本機檔(明文排除)
- 不把個人 pattern 清單改成進版控 —— **任何流程鎖死都不得以此解決**(裁決 2)
- 不由代理人執行 force-push 與遠端分支刪除

## 待議

- 原型驅動程式(`history_scan_probe` 系列,目前在 scratch)要不要出貨成正式工具。
  併入本票再議 —— 判準是:**階段四的驗收要重跑一次,那它就不是一次性腳本了**
