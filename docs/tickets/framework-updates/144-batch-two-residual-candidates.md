# 票 144 —— 票 133 批二的剩餘候選(**合併追蹤,逐項保留獨立識別**)

**狀態**:**candidate。只登記,本輪不修。**
**優先度**:**未定** —— 各項的〈時鐘〉逐項寫,**沒有統一的時鐘**。
**立案**:2026-09-24,票 133 批二端對端驗收完成、準備結案前的候選移交。
**來源**:票 133(批二 #8–#14 與第 15 格)。**票 133 那幾列改成「轉出 → 票 144」。**

---

## ⚠ 這張票不是一個問題,是**九個各自獨立的候選**

**合併的理由只有一個**:它們原本**只活在票 133 裡**,而票 133 要結案 ⇒
不搬出來就會隨著結案變成沒有人追蹤的東西(`F-086` 的形狀:
**判準寫在一個即將關閉的地方,等於沒有寫**)。

**合併不等於它們同類。** 所以:

- **每一項保留自己的編號、證據、與限制措辭**;
- **原文照搬**,票 133 那邊**不刪**(`F-036`:舊紀錄不刪),只加「轉出」指標;
- **本票不做任何修復,也不排序** —— 轉出是**換追蹤者**,不是修好。

**對照表(票 133 ↔ 本票)**

| 票 133 的位置 | 本票 |
|---|---|
| 九項盤點第 3 項(#12 的候選記錄) | **§A1 / §A2 / §A3** |
| 九項盤點第 4 項(#11 的候選記錄) | **§B** |
| 九項盤點第 5 項(#13 的候選記錄) | **§C1 / §C2** |
| 九項盤點第 6 項(受測分支可達性) | **§D** |
| 九項盤點第 9 項(`prov_why` 從未被讀) | **§E** |
| 〈未證明 / 已知邊界〉第 1 條(`load_stage_defs` 診斷路徑) | **§F** |
| (本輪新發現,不在票 133) | **§G**(腳本裡的 `read_upstream_root` 副本) |

---

## 〇、先查過既有票(**不重複立案**)

| 票 | 狀態 | 查的結果 |
|---|---|---|
| **116**(帳本與讀它的三個東西格式不一致) | **done**(2026-09-08) | 它修的是 **B-6 / B-7 / B-8 的格式歧異**;**沒有**涵蓋 §A1(票↔檔案綁定)、§A2(`outcome` 欄位)、§A3(歷史證據)。⇒ **不算已追蹤** |
| **122**(code-review 豁免對帳散文的前提) | **candidate** | 對象是 `.agents/skills/code-review/SKILL.md` 的**散文**,不是 `gate.py` 的 `logged_exemption_backed`。⚠ **同一族(都走 `declared_in` 回查票)但不同範圍** ⇒ 引為相關,**不併入** |
| **141**(go-live 基準身分未被持續驗證) | **candidate** | 對象是 **go-live 基準**;§C1 的對象是 **`upstream_commit` / `upstream_path` / 上游 repo 身分**。⚠ **相關家族**(141 §一逐字區分「身分」與「可達性」),**不等同已確認是重複問題** ⇒ 引為相關,**不併入** |
| **142**(站別定義檔的修改授權) | **candidate** | 對象是**誰可以改站別定義檔**;§F 的對象是**錯誤訊息印的路徑與實際讀取路徑不一致**。不同範圍 |
| **143**(偵測規則刪除降低涵蓋) | **candidate** | 第 15 格的規則清單削弱,已另立。與本票各項無重疊 |
| 全庫搜尋 | —— | `prov_why` 在 `docs/tickets/` **零命中**;「受測分支可達性 / R8 搶先」在 141 / 84 的命中都是別的語境 |

⇒ **九項沒有任何一項已被既有票追蹤**,故合併立本票取號 **144**。

---

## A、#12(R3 已記錄的票宣告豁免)的候選記錄

**原文照搬票 133〈#12 的候選記錄(只記錄,不修、不另開票)〉。**
**符號**:`gate.py` 的 `logged_exemption_backed()` / `committed_declaration()`。

### A1 —— 回查票時只確認模組名,未驗證「票 ↔ 檔案」的綁定

帳本會匹配目標 `file` / `module`,**但回查票時僅以【模組名】確認宣告**,
**未驗證該宣告與目標檔案路徑或 feature 的綁定**。⇒ 同名檔案借用宣告的風險。

⚠ **靜態判讀,未另行量測。**
⚠ **不簡化成「完全不比對檔案」** —— `rec.file` 是有比對的,
沒比對的是「**票 ↔ 檔案**」那一段。

**時鐘**:說不出來。

### A2 —— 讀取端不查 `outcome`

#12 **不查 `outcome` 等欄位**:在其餘輸入相同時,
讀取端**未區分 `blocked` / `granted`**。

⚠ **記為語意待確認**,**不預先宣稱所有 `blocked` 紀錄都不應被接受**。

**時鐘**:說不出來。

### A3 —— 歷史使用證據不足

引用票 133 那段搜尋範圍(**58 個版本 / 272 筆**,`ticket-declared` 零筆)。

⚠ **不把零紀錄等同零執行。**

**時鐘**:說不出來。

---

## B、#11(R3 純套件標記豁免)的判準邊界

**原文照搬票 133〈#11 的候選記錄(不修、未開票)〉。**
**符號**:`gate.py` 的 `is_bare_package_marker()`。

**判準邊界:「未匹配 `^\s*(def|class)\s`」≠「沒有程式碼」。**

**實測表**(對 `is_bare_package_marker("p/__init__.py", <內容>)` 直接餵入):

| 內容 | 判為純標記 | 種類 |
|---|---|---|
| 空字串 | **True** | 實測 |
| `import os` | **True** | 實測 |
| `__all__ = ['a']`(頂層賦值) | **True** | 實測 |
| `f = lambda: 1` | **True** | 實測 |
| `async def f(): ...` | **True** | 實測 |
| 行首 `def` | False | 實測 |
| **縮排的** `def` | False | 實測 |
| 行首 `class` | False | 實測 |

⚠ **頂層「呼叫」(例如 `setup()`、`register()`)本輪【未量測】** ——
依判準**靜態判讀**應同樣被判為純標記,但**沒有實際餵進去量過**,
**不混入上面的實測表**。

⇒ **這是判準問題,不是來源問題**;**不屬票 133 的修法範圍**。
票 133 的 #11 只改了「提交時讀哪一份」,**判準的正規表示式一個字沒動**。

**時鐘**:說不出來。

---

## C、#13(R3 上游成品豁免)的候選記錄

**原文照搬票 133〈#13 的候選記錄(不修、未開票)〉。**
**符號**:`gate.py` 的 `upstream_backed()`。

### C1 —— `upstream_backed` 本身未驗證三件事

`upstream_commit` 的**核准身分**、`upstream_path` 與目標路徑的**授權關係**、
指標所指 repo 的**上游身分**。

⚠ **引票 141 為相關家族**(go-live 基準身分未被持續驗證),
**不等同已確認是重複問題**。
⚠ **指標檔的外部保護本輪未重查** —— **不寫成「任何人都能改」**。
⚠ **路徑不同也不直接等同違規**,`upstream_path` 的**授權語意待確認**。

**時鐘**:說不出來。

### C2 —— 寫入時點比對磁碟內容,未使用本次傳入的 `content`

`check()` 寫入時呼叫 `upstream_backed(r)`,**不傳 `raw`**。

⚠ **靜態判讀** —— 「寫入前內容」的說法**須對照入口時序**才算數,本輪**未量**。
**不修。**

**時鐘**:說不出來。

---

## D、受測分支可達性(R8 搶先 `fail-closed`)

**原文照搬票 133〈受測分支可達性的候選(不修、未開票)〉。**

**R8 對不在 index 的目標會搶先 `fail-closed`,使後續格子不可達。**

**已發生的事故**:**#13 當輪實際發生 R8 搶先返回,包含一格假綠**
(`test_an_index_that_cannot_be_read_does_not_grant` 當時是綠的,
但擋它的是 R8 而不是 #13)。
**#11 先前已有相同可達性限制的記錄與防範**(票 133 ⑧ 那一格的 docstring)。
⚠ **目前資料不足以把 #11 也算成一次已發生的測試事故。**

**候選方向**:**建立「受測分支可達性」的前提檢查／插樁** ——
讓測試能證明它要測的那一格**真的被評估過**。

⚠ `research/` 路徑與 `assert "R8" not in msg` 是**當次的手法**,
**不是所有測試都應套用的通用規格**;
而且**沒有 R8 訊息,也不能單獨證明 #13 被評估** ——
當輪是另外用插樁記錄呼叫序列才確認的。

> **2026-09-24 併記**:端對端 A 輪的 `13-reject` 案例也帶了
> `assert "[R8" not in msg`,而它通過了。
> ⚠ **那只證明那一次沒有被 R8 搶先**,**不是**這個可達性缺口被關掉了 ——
> 缺的仍然是一個「證明目標那一格真的被評估過」的通用機制。

**時鐘**:說不出來。

---

## E、`prov_why` 賦值後從未被讀

**原文照搬票 133〈#13 / #14 的裁決與落地〉裡的那一則。**

`gate.py` 的 #13 呼叫點:

```python
if at_commit:
    _staged = staged_blob(r)
    if _staged is None:
        prov_ok, prov_why = False, (
            "無法取得 %s 的 index 內容 —— 判不出它是不是同步成品。" % r)
    else:
        prov_ok, prov_why = upstream_backed(r, raw=_staged)
else:
    prov_ok, prov_why = upstream_backed(r)
```

⚠ **`prov_why` 目前【沒有】出現在任何最終訊息裡**(全檔只有賦值、沒有讀取)。
⇒ 那句「無法取得 index 內容」**寫了但沒有人看得到**。

⚠ **這不是判定錯誤** —— #13 是豁免,取不到就不授予,判定方向正確。
缺的是**訊息**:被擋的人看到的是一般的 R3 訊息,
**不知道 #13 曾經因為「取不到 index」而沒有授予**。
(同 `F-031` 那一族的反面:一個**不存在的**訊號同樣不會有人來查。)

**時鐘**:說不出來。

---

## F、`load_stage_defs` 的診斷路徑與實際讀取路徑可能不同

**原文照搬票 133〈未證明 / 已知邊界〉第 1 條。**

**情境**:`STAGES_DEF` 被**單獨替換**(patch `STAGES_DEF` 而不動 `ROOT`)時,
「定義檔不存在」那則訊息印的是 `STAGES_DEF_REL`(即
`.agents/pipeline-stages.yaml`),而實際去讀的是被替換後的那個路徑。

**符號**:`gate.py` 的 `load_stage_defs()` —— `rel_def = STAGES_DEF_REL`,
而 worktree 分支讀的是 `STAGES_DEF`(**引名不引行號**)。

**由來**:2026-09-22 B3 落地時,`rel_def` 由
`os.path.relpath(STAGES_DEF, ROOT)` 改成直接取 `STAGES_DEF_REL`
(改的理由是 index 分支要把相對路徑交給 `git show :<path>`,不得反算)。

**證據來源**:票 99 探針的丙格 —— 指定 `tmp/no-such-file.yaml`,
而 err 印 `定義檔不存在:.agents/pipeline-stages.yaml`;
該格仍正確回 err、`stages == []`,斷言未受影響。

⚠ **目前觀測是【診斷指向不準】,不宣稱判定失效** ——
沒有任何一格因此判錯,也沒有量過它會不會在別的情境造成判錯。
⚠ **只記錄,不修。**

**時鐘**:說不出來。

---

## G、`read_upstream_root()` 有兩個可寫的位置(**本輪新發現**)

**不在票 133 裡** —— 2026-09-24 寫端對端腳本時造出來的,原地登記。

`scripts/e2e_authority_layer.py` 裡有一份 `read_upstream_root()` 的**副本**
(腳本**不 import** `gate.py`,那會把 `gate.py` 的 import 期副作用帶進腳本)。

**已做的核對**(2026-09-23):兩份對九種輸入**逐格一致** ——
2 註解+1 賦值 / 只有賦值行 / 註解+空行+賦值 / 行尾註解 / 帶 BOM / 兩行賦值 /
零行賦值 / 認不得的行 / 路徑含 `#`。

⚠ **但這是「同一個事實有兩個可寫的位置」那個形狀**:
`gate.py` 的解析哪天改了,副本不會跟著改,
**而且不會有東西出聲** —— 那正是 `F-086` 那一族。

**候選方向**(**只列不選**):
(a) 搬到共用位置,兩邊 import 同一份;
(b) 加一條測試釘住兩份行為一致;
(c) 腳本改成 import `gate.py`(⚠ 會帶進 import 期副作用,要先評估)。

⚠ **本票不選方向。**

**時鐘**:**有觸發條件** —— **下一次有人改 `gate.py` 的 `read_upstream_root()` 時。**
⚠ 而那個觸發條件**沒有機器在管**(這正是它被登記在這裡的理由)。

---

## 附、已經有落點、**不進本票**的兩件

| 項目 | 落點 | 核對 |
|---|---|---|
| **「REAL 層材料須由 Jeff 本人或 CI 產生,agent 自跑不算」** | **`docs/agents/commander.md` §六〈只有 Jeff 能做的〉→〈REAL 層材料的來源限制〉**(commit `eeb2d00`) | ✅ **已進 repo 文件**。來源逐字:「本輪裁決確認;裁決者轉述此要求源於 2026-09-03,本次未取得該日原始裁決文件。」**不得引述成已查證的歷史條文** |
| **真 hook 端對端的操作說明** | **`docs/agents/authority-layer-e2e.md`** + `scripts/e2e_authority_layer.py` | ✅ 已進 repo;REAL 證據持久化於 `docs/audits/2026-09-24-authority-layer-e2e-round-a.md` |

---

## 時鐘(**整票**)

**沒有統一的時鐘** —— 九項各自的時鐘寫在各節,**八項答不出日期**,
只有 **§G** 有觸發條件(而那個條件沒有機器在管)。

照 `docs/agents/issue-tracker.md`:規矩要的不是每張票都有期限,
是**每一項都被問過這個問題**。**九項都問過了。**

---

## 相關

- **票 133** —— 本票的來源。九項的原文留在那裡,不刪。
- **票 116**(done)—— 帳本格式歧異;**不涵蓋 §A**。
- **票 122**(candidate)—— code-review 豁免對帳散文;與 §A **同族不同範圍**。
- **票 141**(candidate)—— go-live 基準身分;與 §C1 **同族不同對象**。
- **票 142 / 143**(candidate)—— 票 133 另外兩個轉出。
- **`F-036`** —— 舊紀錄不刪(票 133 那邊保留原文的依據)。
- **`F-086`** —— 判準寫在別處不算機制(**合併立本票的理由**,以及 §G 的形狀)。
