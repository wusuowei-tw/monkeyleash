# 57 — 批一:文件與訊息(F-099 立案 / F-097 補欄 / R7 heredoc 訊息)

**狀態**:**完成**(2026-08-18,全套 808 passed / 3 skipped / 3 xfailed)
**立案**:2026-08-18 唯讀盤點輪的裁決
**落地**:`af2624e` 立案+卷首 → `e6db960` 票 51 ⑥(六處活的字)
→ `7558e60` ⑦ R7 訊息+測試+帳本 → `5baeb11` 帳本(提交時點那一筆,單獨成筆)
→ `e38c1a5` friction 三則 → 本筆收尾
**批次**:本輪分兩批。**批二(執行 `F-065:1115`)不在本張,而且尚未立案** ——
票號在立案當下自查,**不預留**(票 46 的規矩:號碼要自查、不裸猜、斷號不重用)。
**⑤ 不在本張** —— 裁決明文「併進票 51」,本張只記它被搬走,不重述內容。

---

## 卷首:傳遞層也在做「基礎設施 = 特殊處理」(第五次)

**這一段不是本張的工作項,是本輪所有票共用的前提。** 批二立案時引用本節,不重述。

`sync.py` **完全不碰權威層**:

| 檔案 | 標記 | 後果 |
|---|---|---|
| `.githooks/pre-commit` | `skip`(`.agents/portable-manifest.txt:33`) | sync 不搬 |
| `bootstrap.sh` | `skip`(`:32`) | sync 不搬 |
| `.git/hooks/pre-commit` | 不進版控(git 設計) | 任何工具都搬不了 |

判定點在 `sync.py:463`:

```python
        mark = mark_for(rel, marks)
        if mark != "copy":
```

**所以同步再多次,下游的權威層都不會被修好、也不會被裝上。**

而本輪十項裡有三項 —— **①(install.py 不認識 hooksPath)、⑤(層數計數)、⑧(CI 守衛測試不可攜)** ——
要修的東西**全部住在 `skip` 桶**,而 `skip` 的語意就是「同步不管它」。

> **修好上游不會讓下游變好,而綠燈長得一樣。**

**這是「基礎設施 = 特殊處理」這個習慣的第五次,而這一次它在傳遞層。**
CLAUDE.md 的常駐檢查項記著前面幾次(`.claude` 整個在非原始碼清單、
`find_implementation` 跳過點開頭目錄、`scripts/` 在非原始碼清單),
共同根因寫得很清楚:

> **「基礎設施 = 特殊處理」這個習慣** —— 它在多數情境是對的(跳過 `.git` 省時間),
> 所以會被無意識套用;而閘門、hook、安裝器恰好全住在那些位置。

**前四次都在「判定層」:某條規則跳過了某類路徑。這一次在「傳遞層」:
某條運送管道跳過了某類路徑。** 形狀相同,受害者不同 ——
前四次的後果是「這個 repo 的規則有洞」,這一次的後果是
**「所有下游 repo 的規則永遠不會被修好」**,而且它不會出聲。

**每一筆 `skip` 都是一次「這個我不管」的宣告。** 那些宣告逐筆看都對
(每個 repo 的 CI 是那個 repo 的事實、每個 repo 的 hook 是那個 repo 的事實),
**合起來就是「框架管不到框架自己的傳遞」**。
與 F-066 同一個形狀:每一步都對,合起來是錯的。

### 這一節不主張任何修法

**故意的。** 正解要另外設計(參見 F-098 那條判準:
顯而易見的修法 —— 把 `skip` 通通改成 `copy` —— 是錯的,
`.github/` 與 `bootstrap.sh` 的內容**確實**是各 repo 的事實)。
本節的職責是讓這件事**有一個可以被引用的位置**,不是解決它。

> **一個沒有位置的觀察,下一輪就得重新發現一次。**

---

## 本張範圍(三項,逐項對應盤點編號)

| 盤點編號 | 內容 | 產出 |
|---|---|---|
| **④** | 「防誤讀的文字自己被誤讀」落成通用層 friction | `F-099` |
| **③** | `F-097` 補「成因分類」欄 + 七個資料點 | `F-097` 改寫 |
| **⑦** | R7 擋下訊息:heredoc 內文也被切,而「逐段比對」那句根本不會印 | `gate.py` 訊息 |

### 不在本張

- **⑤** → 併進**票 51**(裁決 2026-08-18)
- **①** → **批二**(執行 `F-065:1115`),尚未立案
- **②** → 本輪只做量測,結果記在下方「附:② 的量測結果」,**不改任何程式碼**
- **⑥⑨⑩** → 登記不做(⑥ 併票 48、⑨ 待排、⑩ 屬量化)
- **⑧** → 登記不做,但**它是卷首那件事的第三個實例**,見上

---

## ⑦ 的現況證據(本張唯一動到程式碼的一項)

### 一、`_r7_head` 的「逐段」那句掛在 `if mixed:` 底下

`gate.py:679-685`:

```python
    if mixed:
        head += ("     **許可是逐段比對的**(`&&` / `;` / `||` 各算一段),"
                 "而這幾段不在清單裡:%s\n"
```

而 `mixed` 的定義在 `gate.py:708`:

```python
    mixed = any(seg_ok) and offenders
```

**`any(seg_ok)` 要求「至少有一段是許可的」。** heredoc 那條路徑上一段都不許可
(`python - <<'PY'` 的 `python` 不在 `BASH_ALLOWED_CMDS` 裡 —— 清單裡只有
`python -m pytest` / `python -m pip` / `python .claude\`),於是 `mixed` 為假,
**那句解釋一個字都不會印。**

F-097 記下的實測擋下訊息原文就是這個樣子:

```
[六站閘門/前哨] [R7][enforce] 這個 Bash 指令會寫到沒有被許可的位置((引號或跳脫使目標無法可靠切分))。
     請改用 Write / Edit。
```

**兩行,沒有「逐段」二字。**

### 二、訊息說的「各算一段」與實作切的東西不是同一件事

`gate.py:702`:

```python
    segments = [s.strip() for s in re.split(r"&&|\|\||;", cmd) if s.strip()]
```

`re.split` **對引號、heredoc 內文、跳脫一律無視**。訊息寫的
「`&&` / `;` / `||` 各算一段」讀起來像在講 shell 的指令分隔符,
而實作切的是**整條字串裡的每一個分隔字元**,包含資料裡的。

> **照訊息推論會得到「整條以 `git` 開頭就安全」,而實測不是。**

### 三、歸屬:票 13 那族,而票 13 修過的是另一條分支

票 13 的落地表(`13-r7-has-no-deletion-exit.md:168`)寫著
`| 1 | R7 逐段許可 | **A 修好** |`。**那次修的是 `mixed` 分支。**
heredoc 走的是 `not mixed` 那條,**沒被涵蓋** ——
所以這不是回歸,是**同一張票的涵蓋缺口**。

判準原文(`13-r7-has-no-deletion-exit.md:55`):

> **fail-closed 的訊息必須說出是哪一個前提沒滿足。**

### 四、修法邊界:只改訊息(票 13:70 的定調照樣適用)

> `- 不改判定邏輯,只改訊息。逐段比對維持原樣(它是對的)。`

**本張動的是訊息的「印不印」與「印什麼」,不動 `bad` 的算法、
不動 `segments` 的切法、不動任何回傳值。**

`if mixed:` 改成 `if offenders:` **是訊息的發出條件,不是判定** ——
`bash_write_violation()` 對同一條指令的擋/放結論在改前改後完全相同。
**這一句要寫在這裡,否則下一個讀 diff 的人會以為判定邏輯被動過。**

---

## 怎樣算做完

- [x] `F-099` 落地,內容含「為什麼防誤讀的文字出錯代價更高」的完整判準
- [x] `F-097` 有「成因分類」欄,七個資料點逐筆入表,量化那句原話逐字帶進
- [x] R7 訊息在**沒有任何一段許可**時也印得出「逐段比對」的解釋
- [x] R7 訊息說出**引號與 heredoc 內文一樣會被切**
- [x] R7 訊息指出 heredoc 的**真實出口**(訊息內容寫檔 → `git commit -F <路徑>`)
- [x] `bash_write_violation()` 的擋/放結論在改前改後逐條相同(負控:票 13 既有的四條參數化案例)
- [x] 卷首那一節可被批二引用

## ⑦ 的落地紀錄(2026-08-18)

**紅燈先行**:`tests/test_bash_write.py::TestTheMessageCoversTheNoSegmentQualifiedPath`
先寫、先跑,**4 failed / 11 passed**,而那 4 條紅的正是本票宣稱的四件事。
擋下訊息與 F-097 記載的**逐字相同**(兩行,沒有「逐段」二字),
所以那一則記的東西在今天仍然重現得出來。

### 改了什麼(全部在 `_r7_head` 與兩支新 helper 內)

| 改動 | 性質 |
|---|---|
| `if mixed:` → `if offenders:` | **訊息的發出條件** |
| 「一段都沒有」與「某一段」分開措辭 | 新訊息 |
| `_may_carry_an_inline_body()` + 引號/heredoc 那一段 | 新訊息 |
| `_clip_segment()` | 新訊息(印出來的字,不是比對的字) |
| 內文出口印出時,**不再補一句「請改用 Write / Edit」** | 新訊息 |
| `shown = [b for b in bad if b and b.strip()]` | 新訊息(見下) |

**判定一個字都沒動**:`segments` 的切法、`seg_ok`、`mixed` 的算式、
`WRITE_CONSTRUCT`、`unallowed_write_targets`、`bad`、所有回傳值,全部原樣。
`test_the_verdict_is_untouched` 用 7 條參數化案例(票 13 的四條原案 + 三條放行側)釘住。

### ⚠ 改完的訊息當天就擋下了作者自己,而且讀對了

C1 之前備份票 57 用的是 `cd … && cp …`,**被 R7 擋下**。
擋下訊息(節錄)正是本輪新加的三段:

```
[六站閘門/前哨] [R7][enforce] 這個 Bash 指令會寫到沒有被許可的位置(docs/tickets/…/57-….md、(引號或跳脫使目標無法可靠切分))。
     **許可是逐段比對的**(`&&` / `;` / `||` 各算一段)。
     而**沒有任何一段在清單裡** —— 不是某一段的問題,整條要換寫法。
     **切分不認引號,也不認 heredoc 內文**:…
     出口:**內文用 Write 寫成檔案**,再 `git commit -F <路徑>` …
```

**改前那條指令得到的會是「請改用 Write / Edit」兩行**,而 Write 做不到複製。
處置:停,不繞過,改走它指名的出口(本輪六筆 commit 訊息全部走 `-F <路徑>` ——
因為 `Co-Authored-By: … <noreply@anthropic.com>` 的角括號正是 F-097 的 **M2**)。

> **一條訊息在寫下它的同一天,就把它的作者從錯誤的修法上擋開。**
> 這比任何測試更能說明它改對了什麼 —— 測試證明字串出現了,
> 這一次證明**那些字真的改變了下一步的動作**。

### 順手收掉的殘留:目標欄的空元素

改完之後印訊息複驗,看到:

```
[R7] 這個 Bash 指令會寫到沒有被許可的位置((引號或跳脫使目標無法可靠切分)、)。
```

**結尾多一個 `、` 與一個沒有名字的目標。**

票 13 的落地表第 3 項寫著
`| 3 | 目標欄空括號 | ✅ **closed by 票 36**(\`if not saw_target\` 保證 \`bad\` 非空)—— **不重做** |`。

> **票 36 保證的是「`bad` 不是空 list」,不是「`bad` 裡每一項都有內容」。**
> **「清單非空」與「清單裡每一項都有內容」是兩件事**,而驗收當時把前者讀成了後者。

處置:只過濾**印出來的**那一份(`shown`),`bad` 本身不動 —— 判定不受影響。
釘住的測試:`test_the_target_list_never_shows_an_empty_entry`。已落成 **F-100**。

**這不是票 13 沒修好,是它的驗收條件比它的宣稱弱一階** —— 與 R4
「讀起來在守、實際只守一部分」同一族(票 27 用過同一句話)。

### ⚠ F-097 資料點 3 在本 repo 重現不出來

`git commit -m 'fix: a; b'` —— **放行**(實測)。
因為分號雖然切出了不許可的段落,但整條**沒有任何寫入構造**,
`WRITE_CONSTRUCT.search(cmd)` 回 None 就直接放行了(`gate.py:710-711`)。

**真正的觸發要兩個條件同時成立**:分隔字元切出不許可的段落 **且** 有寫入構造。
資料點 3 與資料點 5 的寫入構造是同一個:`Co-Authored-By: X <a@b.c>` 結尾那個 `>`。
實測 `git commit -m 'fix: a; b\n\nCo-Authored-By: X <a@b.c>'` **確實被擋**,
訊息點名 `` `b Co-Authored-By: X <a@b.c>'` `` 那一段。

**已回填進 F-097 的資料點表** —— 分類不變(仍是規則邊界),
補的是機制:**「訊息裡有分號」本身無害,是它與角括號的組合才觸發。**
不補的話,下一個照 F-097 去重現的人會得到「放行」,然後以為那一則是假的。

---

## 附:② 的量測結果(2026-08-18,唯讀,不改任何程式碼)

裁決:「這一輪只做量測那一半……看到數字再決定改不改。」

**量法**:一支唯讀腳本 `import` `gate.py` 本體(**不自帶判定副本** —— ADR 0003),
對 `legacy_no_redlight()` 的每一筆各呼叫兩次 `redlight_missing()`,
一次不傳 `impl_rel`(現況)、一次傳(補完之後)。

**精確答案:3 條。**

| 路徑 | 現況 | 補 `impl_rel` 後 | |
|---|---|---|---|
| `.claude/hooks/gate.py` | 不合格 | **合格** | **轉紅** |
| `.claude/hooks/redlight.py` | 不合格 | 不合格 | |
| `.claude/patches/apply_patches.py` | 不合格 | 不合格 | |
| `.claude/portable/claude_md.py` | 不合格 | 不合格 | |
| `.claude/portable/g1_verify.py` | 不合格 | 不合格 | |
| `.claude/portable/install.py` | 不合格 | 不合格 | |
| `.claude/portable/leak_scan.py` | 不合格 | **合格** | **轉紅** |
| `.claude/portable/manifest.py` | 不合格 | **合格** | **轉紅** |
| `.claude/portable/verify_gates.py` | 不合格 | 不合格 | |

- `test_no_entry_still_holds_a_qualifying_redlight_record`:現況 stale **0 條** → 補完 **3 條**
- `test_the_list_is_what_the_generator_would_produce`:`undrained` **改前改後都是 0 條,不受影響**
  (`expected - entries` 只有一筆 `.claude/portable/g1_guard.py`,而它兩種呼叫法都合格 ——
  那是票 25 排水留下的紀錄,見 `legacy-no-redlight.txt:15-17`)

**盤點時我給的上界是 6(曾有紅燈紀錄的 stem 數),實測 3。**
差在另外三個(`redlight` / `install` / `verify_gates`)的紅燈紀錄
`impl_hash` 對不上今天的 HEAD —— 紅燈之後那些檔案又被改過。

> **上界不是答案。** 「最多 6」與「就是 3」對「要不要現在改」是兩種決定 ——
> 6 條要排一輪審查,3 條可以逐條看完。
> **而兩者的差別只有跑一次才知道**,推不出來。

---

## 附:`ticket_id` 對齊買到的東西不只是票號

本輪開工前 `.dev/pipeline.json` 的 `ticket_id` 是 `"55"`,而工作是票 57。
`gate.py` 的 R2 自我修改豁免會把當下票號蓋進 `.dev/gate-exemptions.jsonl`
(`gate.py:1537` `stage, ticket = load_stage()`),**而那本帳進版控**
(`git ls-files .dev` → `.dev/gate-exemptions.jsonl`;`git check-ignore` 無命中)。

所以工作停下來等裁決者把 `ticket_id` 改成 `57`。**結果是五筆,不是四筆:**

| # | `tool` | `at_commit` | `content_hash` → `result_hash` |
|---|---|---|---|
| 1 | `Edit` | `false` | `f4182bd9…` → `cce33799…` |
| 2 | `Edit` | `false` | `cce33799…` → `38b2b707…` |
| 3 | `Edit` | `false` | `38b2b707…` → `93dd7202…` |
| 4 | `Edit` | `false` | `93dd7202…` → `54dabea0…` |
| 5 | **`pre-commit`** | **`true`** | `54dabea0…` → `null`(評估,不是寫入) |

**第 5 筆是 `7558e60` 的 pre-commit 自己產生的**,而它**接得上前四筆的鏈**。

**四件事,而只有第一件是當初停下來的理由:**

| | 買到的 |
|---|---|
| 1 | **票號歸屬正確** —— 這是預期的 |
| 2 | **五筆首尾相接**:`f4182bd9 → cce33799 → 38b2b707 → 93dd7202 → 54dabea0`,而第 5 筆看到的正是 `54dabea0`。**四次編輯之間沒有第三方寫過 `gate.py`** |
| 3 | **鏈的起點 `f4182bd9…` 正是 ② 量測時獨立印出的 `gate.py` HEAD 雜湊**。兩次目的完全不同的量測對得上 |
| 4 | **鏈跨過了「編輯」與「提交」的接縫** —— 提交時看到的內容,逐位元組等於四次編輯產出的內容 |

> **一本記對了票號的帳,順便就是一條可驗的鏈。**
> 記錯票號不只是「那一格填錯」—— 鏈還在,但它掛在別人名下,
> **而掛錯名字的證據在對帳時看起來與正確的證據一模一樣。**

**這一條同時是 `.dev/gate-exemptions.jsonl` 前四筆必須與 `gate.py` 同一筆 commit 的理由:**
那條鏈記錄的正是那一次編輯,**拆開就等於把證據與它證明的東西分到兩個 commit**。
第 5 筆走另一條規矩(**證據檔單獨成筆**,先例是票 22 的 `test-runs.jsonl`)——
它記的是**提交事件**,不是編輯事件,所以歸在自己那一筆(`5baeb11`)才對。

**不用 `--amend` 把它補回 `7558e60`:** amend 會再跑一次 pre-commit、再長一筆,
**遞迴不收斂。** 收斂性已實測:`C1/C2/C4/C5` 與 `5baeb11` 自己都不 stage `gate.py`,
帳本零成長 —— 其中 C2 是最強的一格,**它 stage 了 `manifest.py`(legacy 豁免)而帳本沒動**,
那件事本身登記在票 49。

---

## 附:本輪順帶產出的登記(不在本張範圍,但出自本輪)

| 去處 | 內容 | 來源 |
|---|---|---|
| **票 49** | `頭條一之二`:legacy 豁免**放行卻不記帳**(9 個檔案,每次繞過 R3 都沒有紀錄);附帶問題「為什麼 `GATE_SELF` 記帳而 legacy 不記」 | ⑦ 落地後查帳本時實測發現,C2 提交時再次證實 |
| **票 53** | `上游自己也漂了`:`CLAUDE.md` 常駐檢查項的「三次」已落後(裁決者認定為第五次);外加動工時的設計建議 | 卷首那一節的連帶 |
| **`F-100`** | 「一張標 ✅ closed 的票,它的驗收條件可能比它的宣稱弱一階」——**登記,凍結期不做** | 空目標欄殘留(票 13 第 3 項) |
| **`F-097`** | 機制表:8 筆撞擊只有**四種機制**,其中 M1 已裁定不動 → **可評估的是三個點** | 裁決者 2026-08-18 |

**這四筆都是「不寫下來就會蒸發」的東西。** 本輪已經咬過三次同一件事
(F-098 的「命名了卻沒往外走」、F-099 的「引用了變掉的事實」、
以及卷首那條「一個沒有位置的觀察,下一輪就得重新發現一次」)。
