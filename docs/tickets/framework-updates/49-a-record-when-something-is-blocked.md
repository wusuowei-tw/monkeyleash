# 49 — 判定紀錄機制:每次**被擋**產生一筆

**狀態**:**candidate**
**立案**:票 46(2026-08-16)
**特性**:**一張票同時解掉清冊的兩條頭條結論。**

---

## 來源

`docs/audits/2026-08-16-rule-inventory.md`:

- **頭條一**(沒有攔截帳本)
- **頭條二**(連「當時規則是什麼設定」也沒有紀錄)
- **登記四**(族,9 處)+ **登記四的範圍收斂**

## 現況證據

### 頭條一:沒有通用的攔截帳本

全 repo **唯一**一筆有紀錄的實際攔截是 **R3**
(`.dev/gate-exemptions.jsonl`,`outcome=blocked`,`blocked_by=R3`)。

所以清冊的第 4 欄(「擋過沒有」)對多數規則**不可回答** ——
而原因是「**沒有東西在記**」,不是「查過沒有」。這兩者在表上長得一樣。

### 頭條一之二:**被批准的豁免也有一半沒有紀錄**(2026-08-18 加入,票 57 實測)

前一段講「攔截沒帳本」。這一段講**同一本帳裡,兩條豁免待遇不同** ——
而少的那一條是**放行**的那一條。

**實測**(票 57 ⑦ 那一輪,對 `gate.py` 與 `manifest.py` 各做了編輯):

| 豁免 | 觸發的檔案 | 進帳本? | 程式位置 |
|---|---|---|---|
| **R2 自我修改**(`GATE_SELF`) | `.claude/hooks/gate.py` | **是** —— 4 筆,`reason: gate-self-modification`、`outcome: granted` | 記帳走 `log_exemptions` |
| **R3 legacy 清單** | `.claude/portable/manifest.py` | **否 —— 一筆都沒有** | `gate.py:1742-1743` `if r in legacy_no_redlight(): return None` |

那兩行直接 `return None`,**不進 `bucket`**,所以 `log_exemptions()` 收不到它。

> **擋下來的事你會看到(commit 失敗),放行的事你不會。**

`.agents/legacy-no-redlight.txt` 現在有 **9 個檔案**靠它繞過 R3 **整條**
(紅燈紀錄 + 測試檔存在,兩半都免)。**每一次繞過都沒有留下任何紀錄。**
問「這半年 legacy 豁免被用了幾次、用在哪些檔案上」——**無法回答**,
而原因與頭條一完全相同:**沒有東西在記**,不是查過沒有。

#### 附帶問題(本張要回答,不是順帶提一句)

**為什麼 `GATE_SELF` 記帳而 legacy 不記?**

兩條都是「這個檔案免受某條規則管」,性質同類,待遇不同。
**最可能的解釋是設計時只想到前者** —— `GATE_SELF` 是寫 ADR 0004 時
連同帳本一起設計的,legacy 清單是票 26 後來補的,而補的時候
**沒有人問「這條豁免也要記嗎」**。

若屬實,那不是「legacy 漏了記帳」這一個缺口,而是
**「新增一條豁免時,沒有任何東西提醒它要接上帳本」** ——
下一條豁免會用同樣的方式漏掉。**本張要處理的是後者。**

### 頭條二:當時的設定也沒有紀錄

F-083 掃出 **9 處**「決定規則行為卻不進版控」的檔案。

> **每一次判定,都是「一份不進版控的設定」對「一份不進版控的輸入」做出的,
> 而結果也不進版控。三樣缺一就無法複現,而現在三樣都缺。**

唯一例外是 `.dev/gate-exemptions.jsonl` —— **它進版控,而它剛好就是唯一留得下證據的那一個。**
兩件事是同一件事的兩面。

### 為什麼範圍是「被擋」而不是「每一次判定」

輸入**不是均勻缺失的**:

| 判定結果 | 輸入後來去了哪 |
|---|---|
| 通過 | 變成 commit —— **留得下來** |
| 被擋(pre-commit) | **從未落地** |
| 被擋(前哨) | **那筆寫入根本沒發生過** |

> **輸入唯一留不下來的情況,正是最想事後複驗的那一種。**

通過的已經有紀錄(commit 本身),被擋的一筆都沒有 —— **而它們是全部的爭議來源。**

### 那筆紀錄要帶三樣

**設定指紋 + 被拒的輸入 + 擋下的理由。**

### 先例

量化 `dc-01`:「**內容不進版控、指紋進版控**」。
理由是**可複現性**,不是完整性 —— 少了它結論無從證偽。

指紋綁的是**那一次判定**,不是某個時間點;於是設定 / 輸入 / 結果同處一筆,
而那九個檔**不需要**進版控。

## 為什麼不在原票修

票 45 唯讀。且清冊自己寫明「**設計不在本票**」——
這件事的形狀是**新機制**,不是修一個壞掉的判斷,規模與盤點票不相稱。

## 已知代價

- 第 4 欄**永遠不可回答**,而下一份清冊會再一次得到同樣的空格
- 爭議發生時(「它為什麼擋我」)沒有任何原始資料,只能靠重現 —— 而**重現需要當時的設定,那正是缺的那一樣**
- 頭條一與頭條二會**各自被當成獨立問題**分別處置,而它們是同一個缺口的兩面

## ← 反向指標:票 54 有一條測試在等本票(2026-08-16)

票 54(裝 CI)把 `test_gate.py::TestLegacyNoRedlightList::test_the_list_is_what_the_generator_would_produce`
**排除在 CI 之外**,原因正好是本票的題目:

該測試要求每個「已從 legacy 清單排水」的路徑都拿得出**合格紅燈紀錄**,
而紀錄住在 `.dev/test-runs.jsonl`(2923 行,不進版控)—— 乾淨 clone 上不存在。

> **排水的理由進了版控(清單註解),排水的證據沒有。**

**排除是時機問題不是優劣問題**:證據該不該進版控、用什麼格式,就是本票要決定的事。
票 54 當時臨時設計一個,本票開工時很可能推翻重來,**還會多出一個沒人記得的舊格式**。

**→ 本票定案後,回票 54 重評那一條排除。** 兩張票互指,避免任一邊以為對方處理了。

## 本票不含

**解法設計不在本票。** 上面的「三樣」與「先例」是**範圍**,不是設計。
