# 票 140 —— 第六站 2026-09-13 輪盤點:A 桶 7 項 + B 桶 2 項(登記)

**狀態**:candidate。**只登記,不修、不動工、不排優先序。**
**原始來源**:`docs/audits/2026-09-13-arch-round-3-raw.md`(本輪收錄的逐字原文)
**原報告日期 / 基準**:**2026-09-13** / **`59d9e2c`** + 當時工作樹
**本輪核對基準**:`18b0b55a8b98ce3a8815df7e20a95fb4016eba58`(本地 = 遠端)
**登記日期**:2026-09-14

---

## ⚠ 五句必讀(全部適用於本票每一項)

1. **本輪為靜態呼叫鏈判讀,未實測繞過。** 原報告自己也逐字寫著
   「以下新問題是**靜態呼叫鏈判讀**,不是實測繞過成功」。
   **沒有任何一項做過繞過實驗。**
2. **「新增」指【該輪新辨識】,不表示最近才引入。** 原報告逐字:
   「下列『新增』指本輪新辨識,不表示都是最近才引入。」
3. **這批不是窮盡盤點**,是該輪確認到的範圍。原報告逐字:
   「這些是本輪確認到的範圍,不是宣稱剩餘漏洞已被全部窮盡。」
4. **原始來源與現版結論屬不同時點,不得混寫。**
   原文是 `59d9e2c` 的判讀;本票每一項的〈現版證據〉是 `18b0b55` 的核對。
   **兩者在本票分欄呈現,不合併。**
5. **原始報告從未進版控** —— `docs/audits/2026-09-13-arch-round-3-raw.md`
   是它的**唯一版控載體**。
   ⚠ **8/24 那輪 19 條清單已因同一形狀佚失**(票 76 / 77 / 78 仍引用它)——
   **這是 `docs/audits/2026-09-07-arch-round-2.md` 的既有記載,本輪未重新調查。**

> **A / B 是【原報告的分類】** —— A 為原報告判為較高關注的可能漏擋,
> B 為原報告判為次要缺陷。
> **本票保存該分類,不據此宣告立即修復,也不決定優先序。**
> **每一項是否成立、如何修、是否排入工作,留待後續裁決。**

**證據一律寫【檔案 + 符號名】,不寫死行號** —— 行號會隨落地推移
(票 136 那一族在票 133 內已現身七次)。

---

## A 桶(原報告分類:較高關注的可能漏擋)

### A-1 —— 兩段權威層都漏 rename

| 欄位 | 內容 |
|---|---|
| **原編號 / 分類** | A-1 / **A** |
| **原報告描述** | staged 清單只收 `ACM`,Git 判為 `R` 的目的路徑不進判定。把研究碼改名移進生產目錄、或把原本豁免的檔案改名到受管位置,可跳過目的地應有的檢查。**內容來源即使修成 index,也救不了根本沒列入的檔案。** |
| **現版證據(符號)** | `gate.py` → `staged_paths()` 內的 `git diff --cached -z --name-only --diff-filter=ACM`;`scanner.py` → 其 staged 列舉處同樣使用 `--diff-filter=ACM` |
| **現版核對** | **a. 現版仍有靜態證據支持** —— 兩處的 `--diff-filter=ACM` 都在,`R` 未被納入 |
| **可達的漏擋方向(靜態)** | 以 rename 進入受管位置的檔案不會出現在 per-file 迴圈的清單裡 ⇒ 該檔的 R2/R3/R8 等**根本不被評估** |
| **已有相關修改** | 票 132 已把 `scanner` 的**被掃內容**改讀 index;**但那是「掃什麼內容」,不是「列舉哪些路徑」** |
| **既有票** | **無**(票 132 / 133 都是「讀哪一份內容」,與「哪些路徑進清單」不同軸) |
| **仍待確認** | 未實測 rename 是否真能繞過;原報告另附唯讀佐證(`8dbd594` 的兩筆 R100 在加上 `--diff-filter=ACM` 後消失),**該佐證本輪未複驗** |

### A-2 —— R7 的分段沒有換行邊界

| 欄位 | 內容 |
|---|---|
| **原編號 / 分類** | A-2 / **A** |
| **原報告描述** | `git status` 後接真正換行、再接 `tee pkg/x.py`,會被視為同一段;整段以許可的 `git` 開頭而提前放行。 |
| **現版證據(符號)** | `gate.py` → `SEGMENT_SPLIT_RE = re.compile(r"&&\|\|\|\|;\|\|")`(**只切 `&&` / `\|\|` / `;` / `\|`,沒有換行**);`_allowed_cmd_prefix()`;`inline_interpreter_hit()` 亦以同一個 `SEGMENT_SPLIT_RE` 切段 |
| **現版核對** | **a. 現版仍有靜態證據支持** —— 該正則現版逐字如上,**不含 `\n`** |
| **可達的漏擋方向(靜態)** | 含換行的多行指令只被視為一段;若首段以許可前綴開頭,**後續行的寫入動詞不會被單獨檢查** |
| **已有相關修改** | 該正則的註解記著票 76 A2 的修法(**兩份切段定義合一**)——**合的是「兩份要一致」,不是「加上換行」** |
| **既有票** | 票 76(無邊界比對)為同族但**不是同一條**;**本項無專屬票** |
| **仍待確認** | 未實測;`SEGMENT_SPLIT_RE` 是否刻意排除換行(有無既有裁決)**未查** |

### A-3 —— 某段抽到許可目標,替另一段的抽取失敗背書

| 欄位 | 內容 |
|---|---|
| **原編號 / 分類** | A-3 / **A** |
| **原報告描述** | `echo x > .cache/probe; sed -i s/a/b/ pkg/x.py`:第一段把全域 `saw_target` 設為 True;第二段的 `sed` 寫入被偵測但沒有抽取分支,最後不再補「解析不出」的拒絕。 |
| **現版證據(符號)** | `gate.py` → `_bash_write_violation()` 區段內的 **`saw_target`** 單一旗標(初始 `False`,多處 `saw_target = True`),以及結尾的 **`if not saw_target:`** 補拒絕分支 |
| **現版核對** | **a. 現版仍有靜態證據支持** —— `saw_target` 是**整條指令共用一個布林**,非 per-segment |
| **可達的漏擋方向(靜態)** | 任一段成功抽到目標即設旗標 ⇒ 另一段「偵測到寫入但抽不出目標」時,**結尾的 fail-closed 補刀不會觸發** |
| **已有相關修改** | 程式內註解逐字「closed by 票 36 —— `if not saw_target` 保證 `bad` 不是空 list」⇒ **票 36 修的是「抽取失敗必須拒絕」,而本項指的是該旗標的粒度** |
| **既有票** | **票 36**(`extraction-failure-must-refuse`)相關但**不同**:票 36 建立了那條補刀,本項質疑它的**作用域** |
| **仍待確認** | 未實測;旗標是否刻意設計為整條共用,**未查** |

### A-4 —— 內嵌直譯器守衛仍只認部分 token 寫法

| 欄位 | 內容 |
|---|---|
| **原編號 / 分類** | A-4 / **A** |
| **原報告描述** | `env -i python -c …` 會把 `-i` 當成指令名;`python "-c" …` 則不認被引號包住的旗標。兩者仍是同一個「傳入程式碼」概念,卻可漏過新增守衛。 |
| **現版證據(符號)** | `gate.py` → `inline_interpreter_hit()`(跳過 `WRAPPERS` 與 `VAR=val` 之後取 `toks[i]` 當指令名);`WRAPPERS = {"sudo", "env", "time", "nohup", "xargs", "command"}`;`_inline_code_flag()`;旗標比對的守門條件 **`if tok.startswith("-")`** |
| **現版核對** | **a. 現版仍有靜態證據支持** —— 兩條路徑都成立:<br>• `env` 在 `WRAPPERS` 裡 ⇒ 跳過後 `toks[i]` 變成 `-i`,`basename("-i")` 不在 `INLINE_INTERPRETERS` ⇒ **continue**<br>• `tok = '"-c"'` 的 `startswith("-")` 為 **False** ⇒ **不進 `_inline_code_flag()`** |
| **可達的漏擋方向(靜態)** | 以 `env -i` 前綴、或把旗標加引號,可讓內嵌直譯器守衛不觸發 |
| **已有相關修改** | 票 112 建立了這個守衛(`inline_interpreter_violation`);本項是**該守衛的邊界** |
| **既有票** | **票 112**(`r7-inline-interpreter-is-undecidable`)—— **本項應引用它,不另開**;票 126 為訊息面,不同 |
| **仍待確認** | 未實測;`env -i` 的語意(清空環境)是否另有阻礙,**未查** |

### A-5 —— 「純套件標記」判準不等於沒有可測行為

| 欄位 | 內容 |
|---|---|
| **原編號 / 分類** | A-5 / **A** |
| **原報告描述** | `__init__.py` 裡的 `async def`、頂層呼叫等都可能沒有符合正則的 `def`/`class` 行,卻含真正邏輯,仍直接豁免 R3。**即使工作樹與 index 完全相同也成立**,所以不是票 133 第 11 格的來源問題。 |
| **現版證據(符號)** | `gate.py` → `is_bare_package_marker()`,判準為 **`not re.search(r"^\s*(def\|class)\s", body, re.M)`**;消費端在 `check()` 的 `if is_bare_package_marker(r, content): return None` |
| **現版核對** | **a. 現版仍有靜態證據支持** —— 該正則要求行首(容許前導空白)緊接 `def`/`class`;`async def …` 的行首是 `async` ⇒ **不匹配** ⇒ 判為純標記 |
| **可達的漏擋方向(靜態)** | 含 `async def`、頂層函式呼叫、lambda 指派等邏輯的 `__init__.py` 可整條豁免 R3 |
| **已有相關修改** | **無**(該函式現版與原報告時點同形) |
| **既有票** | **票 133 第 11 格**追蹤的是**來源**(工作樹 vs index);**本項是判準本身**,原報告已明確區分 ⇒ **不併入 #11** |
| **仍待確認** | 未實測;是否有既有裁決認為「只擋 `def`/`class`」已足夠,**未查** |

### A-6 —— R3 的測試存在判定仍看磁碟

| 欄位 | 內容 |
|---|---|
| **原編號 / 分類** | A-6 / **A** |
| **原報告描述** | 有合格紅燈紀錄及本機測試檔,就可能提交實作而不提交測試;把測試 staged 刪除但保留磁碟副本,同樣能滿足存在判定。**這格不吃檔案內容,故不在票 133 的內容家族計數內。** |
| **現版證據(符號)** | `gate.py` → `check()` 內的 `cands = [...]` 三個候選路徑,判定為 **`any(os.path.exists(c) for c in cands)`**(出現於 R3 兩半各一處) |
| **現版核對** | **a. 現版仍有靜態證據支持** —— 兩處都是 `os.path.exists`,**判的是檔案系統,不是 index** |
| **可達的漏擋方向(靜態)** | 測試檔存在於工作樹但不在 index 時,R3 的「測試檔須存在」仍成立 ⇒ 實作可進歷史而測試不進 |
| **已有相關修改** | 票 133 批一把 R1/R6/R8/R9/R5 的**內容來源**改為 index;**存在性判定不在那批範圍內** |
| **既有票** | **無專屬票**。⚠ 與票 133 **同主題但不同軸**(內容 vs 存在性),原報告已明說不計入該家族 |
| **仍待確認** | 未實測;是否應改判 index 涉及語意裁決(參照票 133 ⑦ R4 的先例:**在提交時執行,不代表每一項都必須來自暫存區**)—— **本票不裁** |

### A-7 —— 使用者層匯入把「有分類」當成「准許自動覆寫」

| 欄位 | 內容 |
|---|---|
| **原編號 / 分類** | A-7 / **A** |
| **原報告描述** | `import_()` 只拒絕未分類,後續對所有 incoming 項寫入;**`human` 沒有被排除**。因此匯入包若帶 `g1-protected.txt` 或 `shadow-clamp.txt`,腳本仍會覆寫本應人工處理的守衛控制檔。 |
| **現版證據(符號)** | `.claude/portable/user_layer.py` → `import_()`:`unclassified` 檢查(未分類整批拒絕)、`age_incoming` 檢查(無解密器整批拒絕),而其後 **`for rel in incoming:` 迴圈對每一項 `io.open(out, "wb").write(raw)`**,**迴圈內沒有 `HUMAN` 的排除分支**;分類常數 `EXPORT, AGE, HUMAN, NEVER`;分類表 `.agents/user-layer-manifest.txt` |
| **現版核對** | **a. 現版仍有靜態證據支持** —— 寫入迴圈以 `incoming` 為來源,未依 `HUMAN` 過濾 |
| **可達的漏擋方向(靜態)** | 標為 `human`(需逐條人工覆核)的守衛控制檔,在 `--apply` 匯入時會被腳本直接覆寫 |
| **已有相關修改** | `user_layer.py` 內另有 `pending` 訊息提醒「機器相依」與「人工複製 + sha 核對(R4:腳本不碰)」⇒ **提醒存在,但未阻止寫入** |
| **既有票** | **票 30**(`import-side-is-incomplete`)為同一支程式的既有票 ⇒ **本項應先比對票 30 的範圍再決定引用或新開**;**本輪未逐段比對票 30** |
| **仍待確認** | 未實測;`pending` 與 `written` 是否在 `--apply` 下互斥,**未查** |

---

## B 桶(原報告分類:次要缺陷)

### B-1 —— 秘密遮罩先截斷、後替換

| 欄位 | 內容 |
|---|---|
| **原編號 / 分類** | B-1 / **B** |
| **原報告描述** | 命中跨過第 100 字時,完整命中字串不在 snippet 裡,`replace` 換不到,可能把秘密前段印進錯誤報告。**commit 仍被擋,因此不是放行漏洞。** |
| **現版證據(符號)** | `.claude/portable/scanner.py` → 掃描迴圈內 **`snippet = line.strip()[:100]`**,其後 `redact(snippet, m.group(0))`;`redact()` 的實作為 `text.replace(matched, "***已遮罩 %d 字***" % len(matched))` |
| **現版核對** | **a. 現版仍有靜態證據支持** —— 截斷在前、替換在後;`redact` 只做 `replace`,換不到即原樣返回 |
| **可達的漏擋方向(靜態)** | **不是放行方向** —— 是**輸出面**:遮罩失效時 snippet 原樣印出 |
| **已有相關修改** | 同一段已有 `unmappable` 分支處理「命中只在正規化後可見」的情形(整行遮罩),註解引票 32 ⇒ **該分支判的是「對不回原文」,不是「被截斷截掉了」** |
| **既有票** | 票 32(多重 pattern 遮罩可被重組)相關;**本項的成因不同(截斷),未見專屬票** |
| **仍待確認** | 未實測;`unmappable` 是否已涵蓋截斷情形(`m.group(0) not in line` 用的是 `line` 而非 `snippet`)—— **需逐字再核,本輪未做** |

### B-2 —— 帳本輪替可以在摘要不完整時刪原檔

| 欄位 | 內容 |
|---|---|
| **原編號 / 分類** | B-2 / **B** |
| **原報告描述** | 壞 JSON 行被跳過;讀取失敗回空清單,呼叫端仍往 `os.remove` 走。**這不是既有的「摘要刻意不留全文」,而是連摘要計數也可能遺失。** |
| **現版證據(符號)** | `gate.py` → `roll_intercepts()` 的迴圈:`if month not in done: for rec in _summarise_month(...): _append_jsonl(...)`,**其後無條件 `os.remove(path)`**;`_summarise_month()` 內 `except ValueError: continue`(壞行跳過)與 `except (IOError, OSError): return []`(讀取失敗回空) |
| **現版核對** | **a. 現版仍有靜態證據支持** —— `_summarise_month` 回 `[]` 時不寫任何摘要行,而 `os.remove(path)` 仍執行 |
| **可達的漏擋方向(靜態)** | **不是放行方向** —— 是**證據保存面**:月檔被刪而摘要為空 |
| **已有相關修改** | `roll_intercepts()` 的 docstring 已明寫「滾動會丟掉個別紀錄的 `message` 全文與 `cmd_sha256`」⇒ **那是【刻意】的取捨;本項指的是連計數也可能沒寫成** |
| **既有票** | **無專屬票** |
| **仍待確認** | 未實測;實際觸發需要壞 JSON 行或讀取失敗,**發生機率未評估** |

---

## ⚠ 原報告的其他內容(**不計入本批九項**)

原報告另有三個區塊,**本票只記其存在,不逐項核對**:

| 區塊 | 原報告怎麼說 | 本票處置 |
|---|---|---|
| **修復交互缺口**(R6 讀 index / R3 讀工作樹) | 原報告明寫「**歸票 133,不另加一項**」 | **已在票 133 批二 #10 完成四情境量測**(2026-09-13)。⚠ 原報告稱其為「票 133 分批修復造成的交互缺口」,而**票 133 #10 的量測明寫:未做變更前後對照,不主張因果** |
| **C 桶:查過後不需另修**(3 項) | R5 前哨分流接對了、影子套用卡的 `startswith`、R7 無 commit 第二道 | **不計入本批**,本輪未核對 |
| **D 桶:仍需查證**(2 項) | 票 133 第 15 格(另含一個票面前提更正:`leak-patterns.txt` 的 `.txt` 已列非原始碼會提前返回)、R4 的實際部署效果 | **不計入本批**,本輪未核對。⚠ 其中「票 133 第 15 格的票面前提可能有誤」是**尚未處理的更正**,登記於此 |

> **⚠ 更新(2026-09-23)—— D 桶【第一項】的票面前提更正已處理。**
> 實測:`is_source_path('.claude/portable/leak-patterns.txt')` = **False**,
> `check()` 兩個時點 `trace` **都是空的** ⇒ **R2/R3 的職責範圍沒有被進入**;
> 而且讓它提前返回的是**副檔名**那一條
> (`'.txt' in NON_SOURCE_EXT` = True,`'.claude' in NON_SOURCE_DIRS` = **False**)。
> **本 D 桶的登記得到實測支持**,更正已寫進票 133。
> ⚠ **只處理了「前提更正」這一件事** —— 第 15 格的**修復**轉由**票 143** 追蹤;
> **D 桶的另一項(R4 的實際部署效果)未動**,本行不涵蓋它。

---

## ⚠ 附帶登記:報告保存流程的風險(**不排在九項之前**)

**本輪不調查、不修復。**

原報告只存在於一個 GPT 對話裡,靠人工轉貼才進到版控 ——
而**這次轉貼經歷了三輪才成功**(前兩次原文未隨訊息送達)。

⚠ **不宣稱目前完全沒有版控落點** —— `docs/audits/` 就是落點,
9/07 那份與本輪這份都在裡面。**缺的是「從對話到落點」這一步有沒有機制。**

**留待後續裁決要不要處理。本項不排在九項之前。**

---

## 時鐘

**說不出時鐘。** 九項皆為靜態判讀、皆未實測,**沒有一項有可到期的東西**。

照 `docs/agents/issue-tracker.md` 的規矩:說不出時鐘 ⇒ **一律停在 `candidate`,
不排進任何順序**。**本票被問過了,答案是沒有。**

---

## 相關

- **原文**:`docs/audits/2026-09-13-arch-round-3-raw.md`(唯一版控載體)
- **前一輪**:`docs/audits/2026-09-07-arch-round-2.md`(A 桶 4 條 / B 桶 9 條,基準 `7df755a`)
- **票 112**(A-4 的既有票)、**票 36**(A-3 相關)、**票 30**(A-7 相關)、**票 32**(B-1 相關)
- **票 133**(交互缺口歸屬;第 15 格與 R4 部署效果見 D 桶)
