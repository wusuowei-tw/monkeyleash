# 87 — 控制檔備份沒有「什麼時候該重做」的觸發條件

**狀態**:**立案**(2026-08-28)
**時鐘**:**9/11**(桌機海運當天 —— 備份過期的代價在那一刻才會兌現)
**站別**:立案時 `implement`;動工前由 Jeff 改 `ticket_id`
**前置**:票 22(備份先於手術)、票 49(第 10 項)、`F-110`、`F-083`
**本輪**:**只立案偵察,不實作**

> **票號取得時點:2026-08-28,動手當下重查 `docs/tickets/framework-updates/`
> 的最大號 86 加一。** 不提前占號(`F-118`:兩個視窗各自查最大號、各自發同一個號)。

---

## 零、**第一個實例 —— 而且發生在本票立案之前**(2026-08-28)

**本票預測的是控制檔會過期。實際上先過期的是備份清單自己(`README.md`),
而且在本票立案之前就已經發生了。**

時序:

```
1. agent 產出暫存夾(含 README.md,乙組標「待裁 / 不想上傳就不要上傳那個子夾」)
2. Jeff 手動上傳             ← 雲端拿到的是這一版
3. 裁決:乙組「上傳」
4. agent 改寫 README 乙節     ← 本機這一版與雲端那一版從這裡開始分歧
```

> **雲端那份 `README.md` 現在寫著一句已經被推翻的話**
> ——「不想上傳就不要上傳那個子資料夾」。
> 而**它看起來完全正常**:檔在、開得起來、內容是通順的中文。

### 為什麼這一格放在第一節

**已發生的事實比預測強。** 本票原本第一節舉的是兩個「會變 / 即將變」的例子
(`leak-patterns.local.txt` 35→43 行、`g1-protected.txt` 即將 +1 行),
那些是**論證**;這一格是**已經發生的事**,而且它示範了一件論證沒說到的事:

> **最先過期的不是被備份的東西,是備份清單自己。**
>
> 清單是「哪些檔、什麼指紋」的**當下快照**,而它同時也是**還原時唯一的權威**。
> 它過期的時候,不只少了一項資訊 —— **它會拿一個舊答案去回答一個新問題,
> 而且答得很有自信**(有表格、有 sha256、有取樣時間)。

**這也是 `F-122` 的形狀**(票面裡的內容快照是一份沒有人在守的副本)
換到備份清單上:F-122 講的是**排版帶來的權威**,而一份逐檔列了 `sha256`
的清單,權威感比一段散文更強。

### 對範圍的影響:**清單自己也要進被檢查的集合**

原本第二節寫「把**十項**掛進一條檢查」。**十一項** ——
`README.md`(或它的後繼者)必須檢查它自己,否則
**檢查的基準本身可以過期而不出聲**,而那是一個會自我拆除的機制。

---

## 一、問題:**一次性備份會安靜地變成過期快照**

`docs/machine-init.md` 的控制檔清單有十項,判準是「決定閘門行為、卻不進版控」。
2026-08-28 產出了第一份備份暫存夾。**而那一份從產出的下一秒就開始過期。**

**這不是假設,是同一天已經發生兩次的事:**

| 檔 | 什麼時候變的 | 幅度 |
|---|---|---|
| `~/.claude/leak-patterns.local.txt` | **2026-08-27**(票 84 加 pattern) | **35 → 43 行** |
| `~/.claude/g1-protected.txt` | **即將**(裁決 2026-08-28:加 `C:\projects\_ticket84_backup`) | +1 行 |

> **兩項都是控制檔,兩項都在同一週改,而備份是 2026-08-28 做的一次性動作。**

### 為什麼它不會被自己發現

備份的產物是**一份清單 + 一堆檔案**,而它們**不會因為來源改了就長得不一樣** ——
雲端上那一份看起來永遠是完好的:檔名在、大小合理、開得起來。

> **一份過期的備份與一份最新的備份,在雲端硬碟的檔案列表上長得一模一樣。**
> 而差別只在**還原的那一天**才會顯現 —— 那天你沒有第二次機會。

**`F-110` 逐字**:「要留存」是一個祈使句,而**祈使句沒有主詞**。
「記得定期重做備份」也是一個祈使句。

## 二、最便宜的版本(**這是範圍,不是設計**)

**把十項掛進一條檢查:任一項的 `sha256` 與最近一次備份清單不符就出聲。**

暫存夾的 `README.md` 已經逐檔記了 `sha256` —— **那份清單就是基準**,
不必另造一個資料結構。

```
讀 <最近一次備份清單>  →  逐項重算現況 sha256  →  不符 / 缺項 / 多項  →  出聲
```

### 三種不符要分開講,不要合成一句「備份過期」

| 情況 | 意思 | 該不該叫 |
|---|---|---|
| **sha 不同** | 控制檔改了,備份是舊的 | **叫** |
| **來源缺項** | 檔不見了(或本來就刻意不存在,如 `shadow.json`) | **看情況** —— 見下 |
| **來源多項** | 清單有新的第 11 項,而備份清單不知道 | **叫**(這條最容易被漏掉) |

**⚠ `shadow.json` 那一項必須能表達「刻意不存在」** ——
否則這條檢查每次都會為它叫一聲,而**一條每次都叫的檢查會被關掉**(`F-031`)。
這是本票最容易做錯的一格。

## 三、要偵察的(本輪不回答,只列)

1. **這條檢查住在哪裡?** 候選:pre-commit(但控制檔多半不在版控裡,
   改它不會觸發 commit)、測試套件的一條、或一支手動跑的腳本。
   **判準同票 86:它在沒有人記得的時候會不會叫。**
2. **它的觸發時機是什麼?** 「改了就叫」做不到(那要監看檔案);
   「每次跑測試時叫」可行但可能吵;「換機器前叫一次」最準但最晚。
3. **`~/.claude/` 那四項怎麼算?** 它們在 repo 之外,而檢查住在 repo 裡 ——
   跨界讀取要不要?讀不到時 fail 往哪邊倒?
4. **第 11 項出現時怎麼被發現?** 清單本身會長,而
   **`F-137`:一份清單不會帶著它自己的長度。**

5. **🔴 跟誰比?** —— **這一問不是第 2 問的一部分,不要併進去。**

   第 1–4 問問的都是**「來源有沒有變」**。但這條檢查
   **證得了「來源沒變」,證不了「雲端那份存在而且相同」** ——

   > **兩者中間隔著一個手動上傳動作,而那個動作沒有回執。**

   ### ★ **回執找到了(2026-08-28)**

   > ## **不是「上傳成功」,是「下載回來還是它」。**
   >
   > 上傳之後把它**下載回來,逐位元組比對 `sha256`**。

   **這一句就是第五問要的那個回執**,而它是被 Google 雲端的轉檔逼出來的
   (第八節):`.txt` 被轉成 Google 文件、下載回來是 `.docx`,
   **而「上傳成功」那個訊號完全正常**。
   
   **為什麼它答得了第五問**:它不去比對「來源有沒有變」,
   它比對的是**雲端那一份實際交還給你的東西** ——
   把「跟誰比」的那個「誰」變成一個**可以拿在手上的檔案**,而不是一個假設。
   
   同一句已寫進 `docs/machine-init.md` 的硬條件四。**兩邊互指,不各寫各的。**

   **零那一節的 `README.md` 就是從這個縫掉下去的**:來源(本機)確實變了,
   而即使有一條檢查在看來源,它也只會說「本機改了」——
   **它說不出「雲端那份還停在上一版」**,因為它從來沒看過雲端那一側。

   **第 2 問問的是「什麼時候比」,本問問的是「跟誰比」。**
   合成一問的話,做完之後會得到一條**準時去比對一個空位**的檢查 ——
   而它會準時回報綠燈。

   **方向只點名,不設計**(照裁決):上傳後把**雲端側的檔案清單與大小抄回一份
   當回執**,讓「我上傳了」從一個記憶變成一筆資料。
   **回執機制不在本輪設計範圍。**

## 四、這是 `F-110` 形狀的修法

> **機器出聲,不是人記得。**

本票不是「要更常做備份」,那句話沒有主詞。本票是
**「讓備份過期這件事變成一個會出聲的狀態」** ——
出聲之後要不要重做仍然是人決定的,但**「不知道它過期了」這個狀態被消掉**。

**與票 86 的關係**:兩張票同一個判準句(「它在沒有人記得的時候會不會叫」),
不同的對象 —— 票 86 管的是**前哨不在場**,本票管的是**備份過期**。
**兩張票都不要用「記得做某件事」收尾。**

## 五、已知代價(不做的話)

- 9/11 海運當天帶走的是一份 **8/28 的快照**,而其後每一次控制檔改動都不在裡面
- 而且**還原時不會有人發現** —— 還原成功了,只是還原成兩週前的設定
- `leak-patterns.local.txt` 尤其嚴重:它是 `leak_scan` **一半的涵蓋**,
  還原成舊版等於**悄悄放寬了洩漏偵測**,而那正好發生在剛開源之後

## 六、本輪不做

- **不實作。** 只立案 + 偵察清單。
- 不改 `docs/machine-init.md` 的清單內容(第 11 項若要加是另一件事)。

---

## 🔴 七、題目變了:**不只是「備份會過期」,還有「備份可能只是其中一份」**(2026-08-28)

**來源**:票 91(兩台機器的個人 pattern 清單分歧)。

### 事實

`~/.claude/leak-patterns.local.txt` 有**兩份正本,而它們的內容不同**:

| 位置 | 條數 | 各自長了什麼 |
|---|---|---|
| **桌機** | **18 條**(2026-08-28 實測) | 票 84 §4-5 在這裡加了舊帳號那一條(35 → 43 行) |
| **筆電** | 16 條起算 | **K1 / K2 那一條 pattern 在這裡**,而它從未到過桌機 |
| **雲端備份** | 某一次匯出的快照 | 它是**其中一份的某個時點**,不是聯集 |

### 為什麼這改變本票的題目

本票原本問的是**時間**:「這份備份是什麼時候做的、什麼時候該重做」。
而本則問的是**完整性**:

> ### **這份備份是「那個檔案」,還是「那個檔案的其中一份」?**

**兩個問題的失效方式相反:**

| | 過期的備份 | **只是其中一份的備份** |
|---|---|---|
| 還原之後 | 回到一個**較舊但真實存在過**的狀態 | 回到一個**從未存在過**的狀態 —— 另一台的增修被抹掉 |
| 會不會發現 | 對帳日期就看得出來 | **看不出來。** 條數對、sha256 對、清單全綠 |
| 修法 | 重做備份 | **不是備份問題,是「還原」這個動作本身錯了** |

### 🔴 因此:「還原」對這一類檔案是**破壞性動作**

`docs/machine-init.md` 與 26 步筆電劇本的第 09 步原本寫「**還原**取回
`leak-patterns.local.txt`」。**照那樣做會把桌機這 18 條裡的新增直接蓋掉,而且無聲。**

**⇒ 已改成「合併」**(劇本第 09 步,2026-08-28;同時在三選一圖例裡新增
**第四個處置類別:合併**)。**正本 `machine-init.md` 要不要跟著補 —— 待裁**,
理由與票 88 §八之三(四)同一格:**它牽涉使用者層控制檔,執行者是人。**

### 判準(可搬走)

> **「還原」預設了正本只有一份。**
> 一個檔案只要在兩台機器上各自被編輯過,**「還原」就從恢復變成刪除** ——
> 而刪掉的是**另一台上的那些**,所以在這一台上看不出任何異常。

**同族但方向相反**:`F-108`(把 A 併進 B 要驗兩個方向 ——
「A 的都到了嗎」證明你做了事,「**B 的都還在嗎**」證明你**只**做了那件事)。
本則是那條判準的**前一步**:先問「**B 有沒有東西是 A 沒有的**」,
沒問就直接用 A 覆蓋,方向 B 連驗的機會都沒有。

### 對本票驗收條件的影響

本票的驗收原本會是「備份有沒有過期的觸發條件」。**現在要加一問**:

> **每一份控制檔備份,要能說出它是【聯集】還是【某一台的快照】。**

`user-layer-manifest.txt` 的四個桶(`export` / `age` / `human` / `never`)
**表達的是「怎麼搬」,不是「有幾份正本」** —— 那正是票 28 的標題句
(「檔案是好的 ≠ 它保護的是這台機器」)再往前一步。**登記,本票不擴充範圍。**

---

## 🔴 八、Google 雲端硬碟會**把純文字換掉**,而下載的當下看不出來(2026-08-28,時鐘 8/29)

**發現者:Jeff(裁決者側),實測。**

> ### **上傳 `.txt`,雲端把它轉成 Google 文件;下載回來是 `.docx`。**

**逐字證據(裁決者提供)**:

```
桌機收到的檔:前四位元組 80, 75, 3, 4   <- PK,zip 標頭(docx 是壓縮包)
              6870 bytes
而那三行純文字只需要一百多 bytes。
筆電上的原檔是好的 —— 被換掉的是【中間那一段】。
```

### 為什麼它比「檔案傳錯了」嚴重

**被換掉的不是檔名,是內容,而且是在你沒有動它的時候。**
`docs/machine-init.md` 的備份硬條件一逐字寫過這個形狀:

> 一個會自動同步的資料夾,它的內容**在你沒有動它的時候會改變** ——
> 而本清單裡每一項的價值都建立在「它是我放進去的那一份」。

**硬條件一防的是「自動同步」,而這一次的動作是「手動上傳」——
它完全符合硬條件一,然後內容還是被換掉了。**

> ### **「我親手放進去的」不等於「那裡現在放著的是它」。**
> 手動上傳把「誰動的」這一格填成了人,而**那一格從來不是真正的保證** ——
> 真正的保證是**下載回來逐位元組相同**,而那件事沒有人驗過。

### 🔴 範圍:**11 / 11 全中招**(2026-08-28 實測,只看副檔名不讀內容)

```
_gdrive_upload_2026-08-28/
  control-files/     8 個:.json / .jsonl / .txt
  shadow-baselines/  3 個:.jsonl
  README.md          1 個
備份檔總數(不含 README):11;其中純文字副檔名:11  -> **11 / 11**
```

**⇒ 若轉檔真的發生在全部檔案上,下載回來的 `sha256` 與 README 記的會【全部】對不上。**
那時 README 那張表不是「過期」,是**指向一份不可還原的備份**。

### ⚠ 這正是票 87 在問的那件事,而答案比預期更早發生

本票第零節記的第一個實例是**備份清單自己過期**;
第七節記的是**備份可能只是其中一份**;
**本節是第三個,而且它比前兩個都根本:備份可能根本不是那個檔案。**

| # | 失效方式 | 還原之後 |
|---|---|---|
| 一(§零) | 清單過期 | 回到一個舊但真實的狀態 |
| 二(§七) | 只是其中一份 | 回到一個**從未存在過**的狀態 |
| **三(本節)** | **內容被平台換掉** | **還原不了** —— 拿到的是 docx,不是原檔 |

### 🔴 硬條件四(裁決 2026-08-28):**現階段一律用隨身碟**

**⚠ 而這一條與備份硬條件三相撞,需要裁決:**

`docs/machine-init.md` 硬條件三:**backup zip 要上雲必須先加密**,
而**加密方案至今未設計**(該節自承「加密方案不在本輪設計」)。

| 路線 | 狀態 |
|---|---|
| **壓成 zip 上雲** | 🔴 觸發硬條件三 ⇒ **要先加密** ⇒ **方案未定,現在走不通** |
| **隨身碟** | 不經雲端,不觸發硬條件三。**目前唯一可走的** |
| 維持現狀(純文字上雲) | **已證實會被換掉**,不可 |

**⇒ 裁決(2026-08-28):現階段一律用隨身碟。**

**規則要寫成三句,不是一句** —— 只留「用隨身碟」而不說另一條為什麼失效的話,
下一個人會覺得 zip 比較方便,然後在不知道加密那一格的情況下走回去:

> 1. **純文字走雲端,一律先壓成 zip**(避開轉檔)
> 2. **而 zip 上雲需先加密**(硬條件三),**加密方案未設計**
> 3. **⇒ 現階段一律用隨身碟**

**在加密方案落地之前,雲端那份備份不得被當成「可還原的備份」使用。**

### 驗收(不論走哪條路,缺一不可)

> **上傳之後,把它下載回來,逐位元組比對 `sha256`。**

**不是「上傳成功」,是「下載回來還是它」。**
這一步在本節之前**從來沒有被要求過** —— 而它正是唯一會抓到本則的檢查。

---

## 🔴 九、範圍調整:**「過期」有兩個獨立來源,驗收要寫成兩句**(2026-08-29,`F-147`)

**來源**:2026-08-29 重做暫存夾那一輪。**登記於 `F-147`,本節是它在票面上的落點。**

08-28 那一份備份**同時**壞了兩次,而**兩次的方向相反**:

| | **甲:內容被換掉** | **乙:內容沒跟上** |
|---|---|---|
| 記在哪 | 本票 §八(Google 轉檔) | 本票 §零 / §一(一次性快照) |
| 權威在哪一端 | **備份側** | **來源側** |
| 修法 | **換路線** | **重做備份** |

> ### **它們不是同一條軸上的兩個刻度,是兩個獨立的來源 —— 而檢查要往相反的方向讀。**

### 對 §二「最便宜的版本」的影響

§二 那條檢查是 **`讀來源 → 重算 → 對清單`**。**它結構上只看得到乙。**
甲發生的時候它會說「本機沒變」,然後**回報綠燈** ——
而 08-28 真正讓那份備份不可還原的,正是甲。

**⇒ 本票的驗收條件從一句變成兩句(缺一不可):**

1. **來源側**:控制檔現況 `sha256` 是否仍與最近一次備份清單相符(§二 已涵蓋)
2. **備份側**:**把備份取回來**逐檔重算,是否仍與該清單相符(§八 的驗收,**§二 尚未涵蓋**)

**這也是 §三 第 5 問(「跟誰比?」)的另一半答案**:
不只是「跟誰比」,是**要比兩次,而且方向相反**。

### 為什麼不能併成一句「備份過期了」

**併成一句的代價是具體的**:一列對不上的時候,
**你分不出該重做備份、還是該換一條傳輸路線** ——
而這兩件事的成本差了一個數量級(重複製 vs 重新設計傳輸路徑 + 加密方案,見 §八 硬條件三)。

**同一個形狀已經記過**:§二 自己就寫著「三種不符要分開講,不要合成一句」。
**本節是那句話往上一層** —— 連「不符」這件事本身也有兩個來源,而 §二 只讀得到一個。

### 本輪已落地的部分(不是本票的實作)

暫存夾 `_gdrive_upload_2026-08-28/README.md`:

- 新增「**這份備份的驗收方式**」一節 —— 寫成**常設條款**(每一次重傳都要跑),
  含 08-28 的失敗原因,以及**為什麼「改設定」不算解決**(設定看不見、跟帳號綁、被改回去不會有人說)
- 指紋表新增**快照格**(重算時點 + repo HEAD)——
  **沒有那一格,一張表分不出「沒變」與「沒重算」**,而那正是甲與乙在表面上長得一樣的原因
- 八個控制檔**全部重新複製、整張表逐檔重算**(不是只補變動的那幾列,`F-137`)

**本票本體仍未實作**(§六 不變):那條會出聲的檢查還是不存在。

---

## ✅ 十、備份清單自己的指紋 —— **§零 那一格關了**(2026-08-29)

§零 的結論是「**清單自己也要進被檢查的集合**」(十項 → 十一項),
而當時卡在一個看起來像死結的地方:**一份檔案放不進自己的雜湊**。

> ### **解法不是放棄,是放到【別人】身上。**
> 一份檔案放不進自己的雜湊,但**可以放進別人的**。

### 值

| | |
|---|---|
| **對象** | `C:\projects\_gdrive_upload_2026-08-28\README.md`(備份清單本體) |
| **時點** | **2026-08-31 (+0800)**(**第 5 版**) |
| **大小** | **35,175 B** |
| **sha256** | **`ca35795c08b495917ae48cf6a7f9de72ba915056413889c39fff9acaac7234b6`** |
| ⚠ 同日更正 | 本版一度算出 `de3e5cb3…` —— 那一版把日期誤寫成 `2026-09-01`(指令標題誤寫,`F-154` 實例 #5)。**同日改回 `2026-08-31`,長度不變(35,175 B)而雜湊改變** —— **這正是「只看大小會過關」的形狀**:改動與檔案長度無關時,只有雜湊看得見它。`de3e5cb3…` 作廢,勿用。 |
| 前四位元組 | `35, 32, 228, 184`(`# ` + `上` 的 UTF-8 首兩碼)—— 純 markdown、無 BOM |
| **第 5 版改了什麼** | 只動「**還原時**」一節:與筆電劇本建立**互指**,並修掉兩處已經對不上的引用 —— ① `leak-patterns.local.txt` 的處置寫的是「一定要用**還原**」,而劇本第 09 步 2026-08-28 已改判為「**合併**」(票 91 / 本票 §七),**兩份正本互相矛盾**;② 指到「劇本第 07、**10** 步」,而第 10 步是 `settings.json` 的掛載,`upstream-roots.txt` 是**第 11 步**。另補兩條:四個控制檔的 **BOM 建檔/驗收**,以及 `g1-protected` / `upstream-roots` 是**重新決定**(不是還原,`sha256` 對得上反而是紅旗)。**指紋表的十二列一列都沒動。** |
| 前四版(**已作廢,勿用**) | 24,408 B(第一次上傳的那一版)、26,039 B / `5f6b757b…`、30,890 B / `0571e5bf…`、31,633 B / `c4668d2e…`(**雲端上現在放著的仍是這一版**)、**33,079 B / `05eef6d3…`(第 4 版)** |
| 🔴 **本輪狀態(2026-08-31)** | **本機比雲端新【兩】版**(雲端 = 第 3 版 31,633 B;本機 = 第 5 版)。需要一次**整包重傳重驗** —— 而**這一輪不動筆電、不上傳**,所以這一格是**開著的**,不是關掉的。<br>⚠ 第 4 版當時寫著「這是本輪最後一次編輯」,而 **8/31 這一輪又改了一次** —— 那句話管的是**那一輪**,不是這個檔的一生。**§十二那道閘門要在【每一次上傳前】跑,不是「已經跑過了」。** |

### 為什麼記在票面,而不是暫存夾裡另開一個一行檔

一個放在**同一個資料夾裡**的一行指紋檔,**跟被它驗的那份東西一起旅行** ——
上雲會被同一個平台轉檔、隨身碟會被同一次複製失敗帶壞、
**而它自己也沒有指紋**。那只是把同一個問題往下挪一格。

**票面不一樣:它進版控。** 有 commit、有 diff、有歷史,
**被改動的時候留得下痕跡**,而暫存夾不進版控。

> **把基準放進一個有守衛的地方,才叫關掉這一格。**

### 規矩(與「加行與限制同一輪落地」同一個形狀)

> ### **`README.md` 改一次 ⇒ 同一輪更新本節的值。**

**只改其一比都不改更糟**:都不改的話「這一格是空的」是誠實的;
只改 README 的話**票面上有一個看起來權威的雜湊,而它指向一份已經不存在的內容** ——
**而讀票面的人不會知道**。

### 還原時的順序(不能反)

1. **先**用本節的值驗 `README.md` 自己
2. **再**用 `README.md` 的指紋表驗其餘 11 個檔

**先驗基準,再用基準驗別人。** 反過來的話,一份被換掉的清單會給出一整張全綠的表。

---

## ✅ 十一、**驗收第一次真的被跑過,而備份本體成立**(2026-08-29)

§八 定的那條驗收(**不是「上傳成功」,是「下載回來還是它」**)
**在本節之前從來沒有被執行過**。2026-08-29 第一次跑完。

### 結果

| 項 | 結果 |
|---|---|
| 下載回來逐檔比 `sha256` | **OK = 11 · MISMATCH = 1 · MISSING = 0 · 多出 = 0** |
| 唯一不符 | **`README.md`**(下載端 24,408 B = 舊版;本機 26,039 B)—— **競態,非轉檔**,見 §十二 |
| 下載端 `README.md` 前四位元組 | **`35, 32, 228, 184`** = `#`、空白、`上` 的 UTF-8 首兩碼 |

### ✅ 第二輪(整包重傳重驗)—— **備份第一次被證明可還原**

| 項 | 第一輪(12:0x) | **第二輪(12:27)** |
|---|---|---|
| 結果 | OK=11 · MISMATCH=1 · MISSING=0 · 多出=0 | **OK = 12 · MISMATCH = 0 · MISSING = 0 · 多出 = 0** |
| 不符項 | `README.md`(競態,見 §十二) | **無** |
| 下載端 `README.md` | 24,408 B(舊版) | **31,633 B,與本機逐位元組相同** |

**agent 獨立複驗過兩個下載夾**(`_gdrive_verify_2026-08-29` / `…-29b`),數字一致。

> ### 🔴 **2026-08-28 那次的「完成」是假的,而差別就是這組數據。**
>
> 08-28 收工時,備份的狀態是**「檔案都放進去了」**——
> 資料夾在、11 個檔在、README 有一張逐檔列了 `sha256` 的表。
> **那一天沒有任何一個位元組被取回來比對過。**
>
> **「上傳完成」是一個動作的結束,「可還原」是一個性質。**
> 兩者之間隔著一次下載,而那一次下載在 08-28 從來沒有發生。
> **現在它發生了,所以「備份成立」這四個字第一次有依據。**

### ⚠ 這一輪是整包重傳,不是只補那一個檔 —— **而那是裁決者改判的**

agent 原本建議「**只重傳 `README.md` 一個檔**」。**裁決者改判為整包重傳重驗**,
理由兩條,兩條都成立:

1. 它**牴觸 agent 自己寫的常設條款**(「不因為只換了一個檔就只驗那一個檔」)
2. **整包驗同時驗了下載路徑**,而**單檔驗收證明不了路徑** ——
   而路徑正是 08-28 出事的地方(平台對「純文字檔」這一類的處理,不是對某一個檔)

**成本:兩分鐘。** 登記於 **`F-150`**(一條常設條款的第一個例外,
由寫下它的人在同一輪提出,而理由是「這次成本比較低」)。

### 🔴 §八 那一格可以關了

08-28 的證據是 `80, 75, 3, 4`(**PK,zip 標頭 —— docx**)、6,870 B。
08-29 的證據是 `35, 32, 228, 184`(**純 markdown**)、大小相符。

> ### **同一個位置、同一種量法、相反的答案。**
> **Google 轉檔那件已解決,而「已解決」這三個字現在有一筆可以拿在手上的資料。**

**⚠ 但硬條件四(現階段一律用隨身碟)不因此自動解除** ——
本次驗收證明的是「**這一次沒有被轉檔**」,不是「**以後不會**」。
**那正是「這份備份的驗收方式」被寫成常設條款的理由**:設定看不見、跟帳號綁、
被改回去不會有人說。**路線要不要改回雲端,是另一個裁決。**

### 對本票的意義

§二 那條檢查仍然不存在(**§六 不變**)。
**但本票第一次有了一組【真實的驗收數據】** ——
在此之前,「備份成不成立」只有推論,沒有量測。

---

## 🔴 十二、**上傳閘門:上傳必須在所有編輯之後**(2026-08-29,`F-147` 的執行面)

### 時序(**不要只記規則**)

```
1. agent 重算指紋表、改寫 README                      (11:41–11:5x)
2. Jeff 上傳暫存夾                                    ← 雲端拿到的是【這一刻】的 README
3. agent 繼續改 README(自己的指紋那一節、④ 的合併…)  ← 本機從這裡開始與雲端分歧
4. Jeff 下載驗收 → 11 個檔全對,README 不對
5. 算術對得上:24,408 B → 26,039 B,方向是「本機比較新」
   ⇒ **競態,不是轉檔,也不是壞掉**
```

> **兩邊都沒有做錯事。** Jeff 上傳的是當時的正本,agent 改的是被交辦的內容。
> **錯的是:沒有任何一個地方規定誰先誰後。**

### 閘門(已寫進暫存夾 `README.md` 的驗收那一節)

> ### **上傳必須在所有編輯之後,而 `README.md` 必須是最後一個被改的。**
> **上傳前跑一條便宜的檢查:暫存夾 `README.md` 的 `sha256` == 本票 §十 的值。
> 不相等就不要上傳。**

**為什麼盯 `README.md` 一個檔就夠**:它是其餘 11 個檔的基準 ——
**任何一個檔改了,`README.md` 都必須跟著改**(表要重算)。
**一條檢查涵蓋十二個檔。**

### ★ 它是哪一族:**人的手動動作 vs agent 的編輯**

**與票 88 §八那條「同一 repo 同時只留一個活著的視窗」同族,但【不同軸】:**

| | 票 88 §八那條 | **本條** |
|---|---|---|
| 管的是誰 | **兩個 agent 視窗** | **人與 agent** |
| 過期的是什麼 | 睡著的視窗手上那份地圖 | **雲端上那一份檔案** |
| 有沒有機器在守 | **沒有**(該票自承) | **有 —— 就是上面那條 sha256 比對** |

**而人與 agent 那一軸更難管:**

> ### **兩個 agent 視窗至少在同一台機器上;
> 而「Jeff 按下上傳」這個動作,agent 完全看不到,也不會留下任何本機痕跡。**

**⇒ 所以修法不能是「協調」或「講好順序」**(那是祈使句,`F-110`),
**只能是一條【上傳前自己會失敗的檢查】** —— 而它已經落地。

### ⚠ 誠實標註:**本條有基準,但沒有觸發器**

**不得把它記成「有機制」。** 它給的是一個**可以跑的檢查**加一個**存在別處的基準**,
而**沒有任何東西會在上傳前叫人跑它**。

> **「不跑會有什麼東西叫?」—— 沒有。** 這正是 `F-110` 的問法,而本條的答案是這個。

**與票 88 那條的差別因此要說準**:

| | 票 88 §八(視窗) | **本條** |
|---|---|---|
| 觸發器 | **沒有** | **沒有** |
| 失效之後 | **事後也查不出來**(誰也不知道當時開了幾個視窗) | **事後查得出來** —— 比一次 `sha256` 就知道 |

> ### **這是往前一步,不是到站:從「查不出來」變成「查得出來」,而不是從「沒守」變成「守住」。**

**要它變成機制,得有人回答:上傳這個動作,agent 這一側從哪裡看得到?**
**那不是本票的題目,登記,不在此開票。**

### 🔴 補:**收工前必須整包重傳重驗一次 —— 而它沒有前置條件**(裁決 2026-08-29)

> ### **「今天收工前跑一次」不因為待辦沒清光而延後。**

**由來**:本輪之後還會再動檔案,而「等都改完再傳」聽起來合理 ——
**它就是 §十二 那個競態的另一個形狀**:一個沒有終點的等待,
最後留下的是**一份雲端比本機舊、而沒有人知道舊多少**的備份。

| 寫法 | 問題 |
|---|---|
| 「等待辦清光再傳」 | **待辦清光沒有時鐘**,而備份於是也沒有 |
| **「收工前傳一次」** | **有時鐘,而且與待辦無關** |

**這一條與 `F-110` 的差別**:它不是「記得要備份」那種祈使句 ——
**它綁在一個每天必然發生的事件上(收工)**,而不是綁在一個可能不發生的狀態上(待辦清光)。

> **一個以【狀態】為條件的規矩,它的觸發時機由那個狀態決定 ——
> 而沒有人管那個狀態什麼時候到。以【事件】為條件的規矩才有時鐘。**

**⚠ 仍然沒有觸發器**(同 §十二 的誠實標註):沒有東西會在收工時叫。
**這一條把「什麼時候」從不確定變成確定,沒有把「誰來叫」從無變成有。**


---

## ✅ 十三、**路線裁決:走隨身碟**(2026-08-31,Jeff)

> ### **硬條件四(現階段一律用隨身碟)沒有被推翻過。**
> 8/29 走雲端是**例外**,而本票 §八 自己寫著
> 「證明的是**這一次**沒被轉檔,不是**以後不會**」。
> 隨身碟這條路**沒有轉檔那個失效面** —— **少一個失效模式,
> 而驗收腳本兩條路都適用。**

**⇒ 本次重傳走隨身碟。** 雲端那一份維持在第 3 版(31,633 B),**不再更新** ——
它不是現況,而本票 §十 的「本輪狀態」那一格在隨身碟這一輪跑完之前**保持開著**。

### 十三之一、Jeff 照著做:**隨身碟版重傳**(四步)

**對象**:`C:\projects\_gdrive_upload_2026-08-28\`(**12 個檔**:
`README.md` + `control-files\` 8 個 + `shadow-baselines\` 3 個)。

#### 第 0 步 —— 上傳閘門(不相等就停,不要往下走)

```powershell
(Get-FileHash -LiteralPath 'C:\projects\_gdrive_upload_2026-08-28\README.md' -Algorithm SHA256).Hash
# 期望:CA35795C08B495917AE48CF6A7F9DE72BA915056413889C39FFF9ACAAC7234B6
# 與本票 §十 的值比對。不相等 => 有人又改了 README,先查清楚。
```

#### 第 1 步 —— **整包 12 檔**複製到隨身碟(不是只有 `README.md`)

```powershell
Copy-Item -LiteralPath 'C:\projects\_gdrive_upload_2026-08-28' -Destination '<隨身碟>:\' -Recurse
```

> **`F-150`:「只傳一個」必然變成「只驗一個」,因為【沒有傳上去的檔驗不了】。**
> 條款是**整包傳、整包驗**,兩個動詞都要。

#### 第 2 步 —— 從隨身碟**複製回一個乾淨的新資料夾**

```powershell
Copy-Item -LiteralPath '<隨身碟>:\_gdrive_upload_2026-08-28' -Destination 'C:\projects\_gdrive_verify_2026-08-31' -Recurse
```

**要複製回來,不能直接對隨身碟上那份跑驗收** —— 那樣量到的是「寫進去的」,
不是「讀得回來的」。**§十一 那句「上傳完成是一個動作的結束,可還原是一個性質」
在隨身碟上一樣成立。**

#### 第 3 步 —— 驗收(唯讀,只印表)

```powershell
$src = 'C:\projects\_gdrive_upload_2026-08-28'       # 本機正本
$dl  = 'C:\projects\_gdrive_verify_2026-08-31'       # 剛複製回來的

$srcFiles = Get-ChildItem -LiteralPath $src -Recurse -File
$rows = foreach ($f in $srcFiles) {
    $rel = $f.FullName.Substring($src.Length + 1)
    $d   = Join-Path $dl $rel
    if (-not (Test-Path -LiteralPath $d)) {
        [pscustomobject]@{ 檔=$rel; 大小=''; 前四位元組=''; 結果='MISSING' }
        continue
    }
    $b4 = (Get-Content -LiteralPath $d -Encoding Byte -TotalCount 4) -join ','
    $hs = (Get-FileHash -LiteralPath $f.FullName -Algorithm SHA256).Hash
    $hd = (Get-FileHash -LiteralPath $d          -Algorithm SHA256).Hash
    $r  = if ($b4 -eq '80,75,3,4') { 'CONVERTED(docx)' }
          elseif ($hs -eq $hd)     { 'OK' }
          else                     { 'MISMATCH' }
    [pscustomobject]@{ 檔=$rel; 大小=(Get-Item -LiteralPath $d).Length; 前四位元組=$b4; 結果=$r }
}
$srcRel = $srcFiles | ForEach-Object { $_.FullName.Substring($src.Length + 1) }
$extra  = Get-ChildItem -LiteralPath $dl -Recurse -File |
          ForEach-Object { $_.FullName.Substring($dl.Length + 1) } |
          Where-Object { $srcRel -notcontains $_ }

$rows | Format-Table -AutoSize
'OK        {0}' -f ($rows | Where-Object 結果 -eq 'OK').Count
'MISMATCH  {0}' -f ($rows | Where-Object 結果 -eq 'MISMATCH').Count
'MISSING   {0}' -f ($rows | Where-Object 結果 -eq 'MISSING').Count
'CONVERTED {0}' -f ($rows | Where-Object 結果 -eq 'CONVERTED(docx)').Count
'多出      {0}' -f $extra.Count
```

**通過條件:`OK = 12`,其餘四格全 `0`。**

> ### ⚠ **`CONVERTED` 那一格在隨身碟上永遠是 0 —— 而它【留在腳本裡】。**
> 拿掉的話,**將來有人改走雲端就少了一道**,而那個人不會知道曾經有過這一格。
> **一格恆為 0 的檢查,成本是零;而把它拿掉的成本要等到路線改變的那一天才付。**
> (它抓的是 §八 那件:下載端前四位元組 `80,75,3,4` = zip 標頭 = docx。)

### 十三之二、⚠ 為什麼驗收腳本**寫在票面裡**,而不是新增一個 `.ps1`

`0d87011`(2026-08-30)那次 CI 紅的原因逐字:**「新腳本沒有可攜標記 ——
而本機綠是因為它當時還沒被追蹤」**。**在上線當天為了一份四步操作而新增一個
進版控的腳本,會把一個已知會踩的坑再踩一次。**

> **票面進版控、有 diff、有歷史,而且不會被任何一道守衛當成新的可攜物件。**
> 這與 §十 把 `README.md` 的指紋放在票面上,是同一個理由。

**跑完之後**:回本票 §十 把「本輪狀態」那一格關掉,並記下驗收日與 `OK = 12`。
