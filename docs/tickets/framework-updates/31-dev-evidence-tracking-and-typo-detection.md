# 31 — `.dev/` 的版控矛盾,以及目錄名打錯時沒有偵測管道

**排程**:穿插。**#8 部分已裁決並隨開票批次落地**,本票留給 #10 與後續。

**來源**:換機器演練卡點 **#8(待驗證,已驗)** + **#10**。

## #8 —— 三處說法不一致(已驗證,已裁決)

| 位置 | 說法 |
|---|---|
| `gate.py` 狀態檔分類註解 | 「證據…放 `.dev/`,**進版控**」 |
| `redlight.py` 開頭 | 「紀錄是**證據**…放 `.dev/` **並進版控**」 |
| `.gitignore` | `/.dev/`,理由:「本 repo 是**發布來源,不是工作 repo**」 |

實測 `git ls-files .dev` = **0**。

**那個理由曾經自洽,現在不成立。** agent-gates 有自己的 44+ commit、
900+ 筆 `test-runs.jsonl`、自己的豁免帳本 —— **它就是工作 repo**。
票 26 更證明它**從來沒被 `install.py` 裝過**,所以它從頭到尾只是工作 repo。

而下游反過來是對的:台股資訊收集與影音**都把 `.dev/` 進版控**。
**上游對自己的紀律低於下游** —— 與票 24 的 `.scratch/` 完全同型(F-0014 家族)。

### 裁決(2026-08-14):分軌

| 檔案 | 處置 | 理由 |
|---|---|---|
| `gate-exemptions.jsonl` | **進版控** | 豁免史必須可審計 —— 票 24 的精神:豁免的正當性建立在「被記錄、可逐筆對帳」上,而不進版控的帳本對不了帳 |
| `provenance.jsonl` | **進版控** | 同上(本 repo 目前尚未產生,先立規則) |
| `test-runs.jsonl` | **續忽略** | 高流量(每跑一次測試就長),進版控會讓每次測試都髒工作樹。**存續歸 R5**(週級 zip 異地備份,已有第一份) |
| `pipeline.json` | 續忽略 | 執行期狀態,每台機器不同 |

`.gitignore` 的分軌**已隨開票批次落地**,完整理由寫在該檔內。
**兩處程式碼註解(`gate.py` 的狀態檔分類、`redlight.py` 的開頭)留給本票**,
照 **F-036** 改寫:舊文保留,加註分軌理由。
**「證據要進版控」不再是一句無條件的話,而是「可審計性」與「流量」的取捨** ——
而取捨的兩邊各自有一條存續管道(版控 / R5 備份)。

### 證據註記:自我豁免會讓同義註解對分岔

開票那一批原本想把兩處註解一起改。實際發生的是:

- `gate.py` 的註解**改成功了** —— 它有 R2 自我修改豁免(「閘門自身,不受站別限制」)
- `redlight.py` 的註解**被 R2 擋下** —— 當時 `current_stage='tickets'`,而它沒有豁免

**於是一次修改變成半套:兩處說同一件事的註解,一處新一處舊。**

`redlight.py` 是 `gate.py` 的相依(R3 的紅燈判定靠它),兩者的註解描述同一個約定,
**而豁免邊界只及 `gate.py` 一支**。ADR 0004 給那個豁免的理由是
「閘門把站別卡住時,修法需要改本檔」—— 那個理由對 `redlight.py` **同樣成立**
(它也可能把人鎖在外面),但豁免沒有涵蓋它。

**所以這不只是「改註解」的小事:豁免的顆粒度是單檔,而約定的顆粒度是一組檔案。**
兩者不一致時,能改的那一半會先走,另一半留在原地 —— 而那個中間狀態
**看起來像是有人只改了一處忘了另一處**,不像是規則造成的。

本票不解決豁免顆粒度(那是 ADR 0004 的範圍),但**要把這個觀測寫下來** ——
它是「兩處實作/兩處註解會分岔」那一族的新形狀:
**這次讓它們分岔的不是疏忽,是守衛自己。**

## #10 —— 狀態目錄被 gitignore,於是打錯目錄名失去偵測管道

`.dev` 打成 `.dve` 時:

- **`git status` 看不見**(整個目錄被 ignore,新目錄同樣被 ignore)
- fail-closed 訊息只說「**讀不到流程狀態**」,**不會說「你建在隔壁」**

於是使用者對著一個存在的、名字差一個字母的目錄,收到一個「檔案不見了」的訊息。

> 這與票 26 的形狀相同:**訊息說的是真話,但它指向的排查方向是錯的**
> (那次是「讀不到清單」而清單讀得到)。差別在這次沒有說謊,只是**說得不夠具體**。

**修法方向**:fail-closed 訊息印出它**實際找過的絕對路徑**。
打錯字時那條路徑本身就是證據 —— 人一眼看到 `...\.dve\pipeline.json` 就知道了。
成本近乎零,而且對**所有**「檔案讀不到」的訊息都適用,不只這一條。

## 紅燈計畫(#10)

| # | 紅燈 | 守什麼 |
|---|---|---|
| 1 | `pipeline.json` 讀不到時,訊息含**實際查找的絕對路徑**(現行只有相對檔名) | 核心 |
| 2 | 同樣的處理套到其他 fail-closed 讀取點(清單、pattern 檔) | 收了一個入口就問同類入口(F-083) |
| 3 | **反控**:訊息不得因此洩漏使用者名稱到會進版控的地方 | 絕對路徑含家目錄 —— 這條訊息只進 stderr,不進檔案,但要確認 |

第 3 條是本票唯一的風險:**印絕對路徑等於印使用者名稱**。
stderr 不進版控,但對話紀錄與 CI log 是外流面(F-067 的同一句話)。
要先確認這條訊息不會被寫進任何持久檔案,才能放心印。

## 怎樣算做完

- `.gitignore` 分軌(**開票批次已落地**)
- **兩處程式碼註解 F-036 改寫,同一個 commit** ——
  分開改就是重演上面那個半套狀態
- #10 上表三條各有測試,1–2 先紅
- 第 3 條先於 1–2 確認 —— 順序不能反
- 「自我豁免使同義註解對分岔」那則觀測留在票裡,**不隨本票關閉而刪** ——
  它要等 ADR 0004 那邊處理豁免顆粒度時被引用
