# 92 — G1 用 `utf-8` 讀保護清單,而同族的其他三個讀取端都已改用 `utf-8-sig`

**狀態**:**立案**(2026-08-29)
**時鐘**:**9/11**(**不在上線前修** —— 那是動守衛,見第五節)
**站別**:立案時 `implement`;動工前由 Jeff 改 `ticket_id`
**前置**:`F-139`(本票是它的第二個實例)、`F-146`(BOM 那一族)、`ADR 0009`、票 88
**本輪**:**只立案。不修、不改編碼、不動 `KNOWN_GAPS`**

> **票號取得時點:2026-08-29 13:00,動手當下重查 `docs/tickets/framework-updates/`
> 的最大號 91 加一。** 不提前占號(`F-118`)。

> ## 為什麼要有這張票
>
> `.claude/portable/g1_verify.py` 的 `KNOWN_GAPS` 註解逐字:
>
> > **⚠ 進這一桶的門檻:必須有票號。沒有票的缺口不叫已知缺口,叫沒人管的洞。**
>
> **本票的第一個用途就是讓這個缺口不再是「沒人管的洞」。**
> ⚠ **但本輪不把它放進 `KNOWN_GAPS`** —— 見第六節。

---

## 一、事實(2026-08-29 唯讀實查)

四個使用者層控制檔的讀取端,**三個用 `utf-8-sig`,一個用 `utf-8`**:

| 檔 | 讀取端 | 編碼 | 有 BOM 時 |
|---|---|---|---|
| **`g1-protected.txt`** | **`g1_guard.protected_entries()`** | **`utf-8`** | 🔴 **BOM 黏進第一條路徑** |
| `shadow-clamp.txt` | `gate.read_shadow_clamp()` | `utf-8-sig` | ✅ 免疫 |
| `leak-patterns.local.txt` | `leak_scan._read_patterns()` | `utf-8-sig` | ✅ 免疫 |
| `upstream-roots.txt` | `gate.read_upstream_root()` | `utf-8-sig` | ✅ 免疫 |

**那三個不是碰巧** —— 它們的 docstring 逐字寫著理由:

> **用 `utf-8-sig`:PowerShell 的 `Set-Content -Encoding utf8` 寫的是帶 BOM 的 UTF-8,
> BOM 黏上鍵名就解析失敗 —— 而 fail-closed 系統的故障是隱形的,
> 輸入端的坑要在進門前排掉。**

## 二、🔴 現況:**目前沒有 BOM。這是理論風險,不是現況**

**實測(2026-08-29,裁決者在自己的終端機)**:

```
g1-protected.txt 前三位元組 = 35, 32, 71    ( "# G" )
              **不是** 239, 187, 191
```

**⇒ 這一條現在沒有在發作。** 票面不得寫成「保護清單目前有洞」——
那會讓一個理論風險穿上事故的衣服(`F-149`:嚴重度不要跑在證據前面)。

**它會在什麼時候變成現況**:`g1-protected.txt` 是**新機器上第一次建檔**的四份之一,
而 PowerShell 最自然的兩種寫法(`Set-Content -Encoding UTF8`、
`Add-Content -Encoding UTF8` 對不存在的檔)**都會寫 BOM**(`F-146` 實測)。
**入口已先擋住**:`docs/machine-init.md` §一之零 指定建檔方式與三位元組驗收。
**但那是紀律,不是這一條的修法。**

## 三、發作時會發生什麼(**這一格是本票的分量所在**)

BOM 黏在第一條路徑前面 ⇒ 那條路徑**再也不會被命中**。而現場長這樣:

| 觀察點 | 看到的 |
|---|---|
| `cat` 清單 | 那一行**在** |
| 行數 | **正確** |
| `ls` | **正常** |
| 那條路徑的保護 | **沒有了** |
| 有沒有東西叫 | **沒有** |

> ### **這是 `F-139` 的第二個實例:沒跟上慣例的那一個,正好是守自己的那一個。**
>
> `F-139`:第一級擋不住 `~` 寫法,**而它保護的第一個對象就是自己**。
> 本票:四個讀取端只有一個沒改用 `utf-8-sig`,**而它讀的正是保護清單**。
>
> **兩次都不是隨機落點。** 保護清單是唯一一個「守衛自己是它的讀者」的檔案,
> 而**守衛自己那一份程式碼,是唯一沒有別人在替它跑同一套慣例的地方**。

### 🔴 失去保護的會是哪一條 —— **實測,不是慣例**(修正 2026-08-29)

**本票初稿寫**:「清單第一行**慣例上**寫清單自己(`machine-init.md` §一.2 的範例就是這樣),
所以最可能失去保護的是 `ADR 0009` 的地基」。

> ### **那句話在這台機器上不成立。**

**實測(2026-08-29,裁決者在自己的終端機看)**:

| | 這台機器的實際情形 |
|---|---|
| **第一段第一條** | **OneDrive 備份資料夾** |
| **清單自己** | 在**第 3 段**,不是第一條 |

**⇒ BOM 若發生,失去保護的是【備份資料夾】,不是 `ADR 0009` 的地基。**

**仍然嚴重,但嚴重的方式不同**:那是**備份**——
而票 87 整條線(9/11 海運、控制檔備份、`_ticket84_backup` 不刪不改名)
都站在那顆資料夾上。**失去保護的是那個。**

> ### ⚠ **初稿那一格本身就是 `F-152` 的實例(退化情形):
> 我用【慣例】推導出一個關於【這台機器】的結論,而沒有量它。**
>
> 「用通則推導個案而不量它」是量測獨立性的極端情形:**獨立性是零** ——
> 那個結論百分之百來自我對那個對象的模型。
> **而它比一個壞掉的量測更難察覺,因為它連一個數字都沒有留下,只有一句聽起來合理的話。**

**⇒ 本節的依據是【實測】。** 清單內容不抄進票面(`F-122`:票面裡的內容快照
是一份沒有人在守的副本),只記**結構事實**:第一條不是清單自己。

## 四、修法(**一行,但不在本輪**)

`g1_guard.protected_entries()` 的 `encoding="utf-8"` 改成 `"utf-8-sig"`,
與其他三個讀取端一致。

**驗收(正反控,缺一不可)**:

| 方向 | 要驗什麼 |
|---|---|
| **正控** | 一份**帶 BOM** 的清單:第一條路徑**仍然被擋** |
| **反控** | 一份**不帶 BOM** 的清單:行為與現在**完全相同**(沒有多擋、沒有少擋) |

**少了反控**,一個「把整份清單讀壞」的改動也會讓正控全綠。

## 五、為什麼**不排上線前**

**它是動守衛,而守衛週一公開。** 與票 88 第五節同一條理由:

> **上線前改守衛,等於在沒有人手的那一週,把最後一道防線換掉。**

而本票的急迫度比票 88 更低,因為 **第二節:現在沒有 BOM**。
**入口已由 `machine-init.md` §一之零 擋住(紀律),真正的修法排 9/11。**

## 六、⚠ 本輪**不**放進 `KNOWN_GAPS`

`KNOWN_GAPS` 的語意是「**放行 → 印已知缺口並通過;擋下 → 記為不合格**」。
**而這一條現在不會放行任何東西** —— 沒有 BOM,清單讀得好好的。

> **把一個沒有在發作的東西放進 `KNOWN_GAPS`,會讓那一桶從「已知漏擋」
> 變成「已知的擔憂」,而那兩件事的驗收語意相反。**

**⇒ 有票號(本票)但不進那一桶。** 這兩件事不是同一件,
本票的存在已經滿足「不是沒人管的洞」那個門檻。

## ✅ 八、落地(2026-08-31)

**站別 `implement`,紅燈先行。** 與票 88 同一支檔(`g1_guard.py`),**分兩刀**:
票 88 那一刀動的是 `_canon()` / `variants()`(比對面),本刀動的是
`protected_entries()`(讀取面)——**兩個缺口不在同一個函式上,commit 分得開。**

### 八之一、🔴 今天的旁證:**同一批檔案裡已經有一個帶 BOM 的**

第二節寫著「目前沒有 BOM,這是理論風險」。**那句話今天仍然成立,而旁證變強了。**

2026-08-31 量備份暫存夾裡的四個使用者層控制檔複本(**量的是複本,
不是 `~/.claude/`** —— 票 88 §八之一 界線 A):

| 檔 | 前四位元組 | BOM |
|---|---|---|
| `userlayer--shadow-clamp.txt` | **`239,187,191,35`** | 🔴 **有** |
| `userlayer--g1-protected.txt` | `35,32,71,49`(`# G1`) | 無 |
| `userlayer--leak-patterns.local.txt` | `35,32,229,128` | 無 |
| `userlayer--upstream-roots.txt` | `35,32,228,184` | 無 |

> ### **這台機器上真的有人用「會寫 BOM 的方式」建過控制檔 ——
> ### 只是剛好建的是那個免疫的。**

`shadow-clamp.txt` 的讀取端是 `utf-8-sig`,所以那個 BOM 無害、
而且**從來沒有人發現它在那裡**。換句話說:寫 BOM 的那個習慣**已經在這台機器上發生過**,
它沒有出事只是因為**中招的檔剛好不是保護清單**。

**而新機器上第一次建 `g1-protected.txt`(筆電劇本第 07 步)正是同一種寫法。**
⇒ 這不是「有一天可能會」,是「**同一個動作已經做過,只是落在別的檔上**」。

### 八之二、修法:**一個關鍵字**

```python
io.open(PROTECTED_LIST, encoding="utf-8")      ->  encoding="utf-8-sig"
```

同族的另外三個讀取端早就這樣做,理由逐字是
「fail-closed 系統的故障是隱形的,輸入端的坑要在進門前排掉」。
**沒跟上那個慣例的是 G1 自己,而它讀的正是保護清單。**

### 八之三、測試(紅燈先行,5 條)

`TestABomOnTheListDoesNotSilentlyDropTheFirstEntry` ——
**造一份真的帶 BOM 的清單**(前三位元組 `239,187,191`),
monkeypatch `PROTECTED_LIST` 過去。

| 條 | 驗什麼 | 紅/綠 |
|---|---|---|
| `test_the_fixture_really_carries_a_bom` | **先證明 fixture 本身是對的** —— 否則後面測的是別的東西 | 綠(前提) |
| `test_the_first_entry_is_still_protected` | 第一條路徑仍被 `level1_hit` 命中 | 🔴 **紅** |
| `test_no_entry_carries_the_bom_character` | 回來的條目不含 `\ufeff` | 🔴 **紅** |
| `test_the_second_entry_was_never_affected` | **對照** —— 只有第一條會中 | 綠 |
| `test_a_list_without_a_bom_behaves_identically` | **反控** —— 無 BOM 的行為一個字都不變 | 綠 |

> ### ⚠ **不得用「現在沒有 BOM 所以沒事」當通過。**
> 現況無 BOM 是一個**會變的事實**,而測試要釘的是**變了以後仍然守得住**。

**紅燈**:`2 failed, 3 passed`,訊息說的是後果:

```
**保護清單的第一條路徑靜默失去保護** —— BOM 黏在第一行上,
清單上有那一行、行數正確、g1_verify 照樣全綠,
而那條路徑不再被擋,沒有任何東西會說
```

**綠燈**:`103 passed, 3 xfailed`(`tests/test_g1_guard.py`)/
全套 **1123 passed / 3 skipped / 3 xfailed**(`F-109`:基準與數字一起寫,基底見收刀 commit)。

---

## 🔴 九、AC —— **本票【尚未】滿足**

### ☐ AC-1:Jeff 手動覆蓋(**驗收條件,不是備註**)

> ### **AC 未滿足,直到 Jeff 完成 `~/.claude/hooks/g1_guard.py` 的手動覆蓋
> ### (ADR 0009 四步)並跑過 `g1_verify` 全綠。**

**改 repo 內的 `.claude/portable/g1_guard.py` 不會讓任何一台機器變安全** ——
實際在守的是 `~/.claude/hooks/g1_guard.py`,三個 repo 共用,
而 agent 結構上碰不到它。**前五次都是這樣,沒有一次例外。**

**⚠ 今天不做 cp。**

> **⚠ 這一格是 `g1_guard.py` 專有的,票 82 沒有。** `gate.py` 走的是安裝與 sync,
> **不走 `~/.claude/hooks/` 那條人工路徑** —— sync 那一輪不要把三張票當成同一種。

### ☐ AC-2:copy 桶的下游同步(**三張票共用**)

`.claude/hooks/` 與 `.claude/portable/` 都是 copy 桶,
`tests/test_gate.py` 與 `tests/test_g1_guard.py` 也是 ——
**收刀前要 sync 兩個下游,兩支測試一併帶過去。⚠ 今天不做 sync。**

### ☐ AC-3:`KNOWN_GAPS` —— **出的門檻(裁決 2026-08-31)**

第六節裁定本輪不放進 `KNOWN_GAPS`。而票 88 那一刀補完之後,
**`KNOWN_GAPS` 裡票 88 那一筆該不該移掉**成了同一格的問題 ——
末行現在印「已知缺口 2 項」(票 80 UNC + 票 88),
**而票 88 那個缺口在 repo 內已經補好了。**

> ### **裁決:出的門檻 =「AC-1 勾了才移」**,綁在 `g1_verify` 全綠那一刻。

**理由**:

> **此刻移掉,末行會宣稱一個「在這台機器上還沒成立」的事實。
> 那個數字現在往悲觀方向說謊(算著一個已補的缺口),
> 移得太早會變成往樂觀說謊 —— 兩種都不準,但後者更貴。**

repo 內補好了,而**三個 repo 實際在守的那一份還是舊的**(AC-1 未勾),
所以那個數字現在說的是「守衛還有兩個缺口」——**對這台機器而言那是真的**。

### ★ 本格的產出不是那個數字,是那條門檻

> **進的門檻寫過**(`g1_verify.py` 的註解逐字:「必須有票號。
> 沒有票的缺口不叫已知缺口,叫沒人管的洞」),**而出的門檻沒有人寫過** ——
> 而**一個只有進沒有出的桶,最後會裝著一堆已經修好的東西**,
> 然後它的計數就不再有人相信。

**⇒ 門檻已寫進 `KNOWN_GAPS` 自己旁邊的註解**(2026-08-31)——
**進的門檻寫在那裡,出的門檻也該寫在那裡**,不是只寫在票面上。

**⚠ 今天不移。AC-1 未勾。**

### ☑ AC-4:紅燈先行 + 綠燈 + 全套(**已完成 2026-08-31**)

---

## 七、本票不做

- **不改編碼、不動 `KNOWN_GAPS`、不碰 `~/.claude/`**(界線 A,票 88 §八之一)
- 不處理其他三個讀取端(它們已經對了)
- 不重新設計「四份檔的建檔方式」——那已在 `docs/machine-init.md` §一之零
