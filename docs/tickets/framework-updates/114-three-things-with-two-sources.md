# 114 — 三件事各有兩份實作:收成單一來源(B-4 → B-3 → B-2)

**狀態**:**candidate**(立案,不動工)
**立案**:2026-09-07,第六站(arch)第二次診斷 → **B 桶 B-2 + B-3 + B-4**
**來源**:`docs/audits/2026-09-07-arch-round-2.md` 的 **B-2 / B-3 / B-4**
**站別**:立案時 `idle`;動工前由裁決者改 `current_stage` 與 `ticket_id`
**估工**:**小 ×3(三刀)** · **裝新下游前**:**要**(三件都要)

> **順序是刻上的:B-4 → B-3 → B-2。**
> B-4 是**唯一一件現在就承載判定**的(票 10 的 R2 豁免與 sync 發證要對同一件事給同一個答案),
> B-3 需要先量一件事才動得了,B-2 純粹是收尾。

---

## 共同判準

> **同一件事只留一個實作,而「兩邊要一致」要是構造,不是註解。**

這句話 `gate.py` 的 `POSIX_WRITE_COMMANDS` 自己寫過(票 29),
`SEGMENT_SPLIT_RE` 收單一來源時又寫過一次(票 76 A2)。**這張票是同一句話的第三、四、五次。**

每一件都要一條 **「兩處是同一個物件」的測試** —— 不是「兩份內容相等」:

```
內容相等的斷言:改了一份、忘了另一份 -> 紅(好)
                但**兩份都改了、改得不一樣**也可能碰巧相等 -> 綠(不好)
同一物件的斷言:`a is b` -> 構造上不可能分岔
```

---

## 刀一 —— B-4:行尾正規化兩個獨立實作

**受害符號**:`redlight.content_hash` / `sync._norm`
(`gate` 那一側經 `gate._hash_bytes` 重用 redlight 那份)。

**為什麼先做**:票 10 的 R2 內容豁免問「staged 位元組與上游物件是否逐位元組相同」,
而 `sync.write_provenance` 決定「要不要發證」問的是**同一件事**。
兩邊各有一份正規化實作,**現在碰巧一致** ——
一份改了另一份不會紅,而分岔的後果是「發了證但豁免不成立」或反過來。

> `CLAUDE.md` 的常駐檢查項:**說「構造保證」之前,先問這個保證是從方法推出來的,
> 還是從這次的資料看出來的。** 這一件現在是後者。

**修法**:`sync` 改用 `redlight.content_hash`。
`sync` 是 `copy` 桶、`redlight` 也是,兩者同進同出,所以下游不會出現「只有一半」的狀態。

**紅燈形狀(一句)**:斷言 `sync` 用的正規化函式**就是** `redlight.content_hash` 那一個物件;
改掉 `redlight.content_hash` 的行為(例如多正規化一種行尾),`sync` 的雜湊要跟著變。

---

## 刀二 —— B-3:兩份「非原始碼目錄」清單

**受害符號**:`gate.NON_SOURCE_DIRS` / `redlight._SEARCH_SKIP`。

**分岔實測(2026-09-07)**:`scripts` 在 `gate` 那份裡、**不在** `redlight` 那份裡;
兩份都不含 `.claude`(後者是刻意的,理由寫在 `redlight._SEARCH_SKIP` 上方的註解裡:
跳過所有點開頭的目錄會跳過閘門自己)。

### 動工前必須先量(**這一刀不是純合併**)

> **把 `scripts` 納入之後,`redlight.find_implementation` 會少走哪些路徑?**

`find_implementation` 是**由測試找實作**(與 R3 反向),它決定紅燈紀錄裡的
`impl_file` / `impl_hash`。多跳過一個 `scripts/`,代表
**`tests/test_<x>.py` 對應到 `scripts/<x>.py` 的那些會從「找得到」變成「找不到」** ——
而「找不到」在紀錄裡是 `impl_exists=False`,那會改變 R3 的判定。

**要量的三個數字(帶單位)**:
`scripts/` 底下有幾支 `.py`;其中幾支有對應的 `tests/test_<name>.py`;
現行紅燈紀錄裡有幾筆的 `impl_file` 落在 `scripts/`。
**三個都是 0 的話這一刀是純合併;任一個非 0,就要先裁「該不該跳過 `scripts`」。**

### 方向的坑(立案時就寫下來,免得動工才發現)

票面原句是「`redlight` import `gate`」。**那個方向有循環**:
`gate` 已經用 `_redlight()` **以路徑載入** `redlight`(而且該函式的 docstring 寫了為什麼不能用
`import redlight`:測試以 `spec_from_file_location` 載入 `gate` 時,`.claude/hooks/` 不在 `sys.path` 上)。

三個候選:

| | 做法 | 代價 |
|---|---|---|
| **甲(建議)** | 清單搬去 `redlight`,`gate` 從那裡取 | 與現有載入方向一致(`gate` 已經會載 `redlight`),零新機制。但「非原始碼目錄」這個概念住在 redlight 有點怪 |
| 乙 | `redlight` 以同樣的路徑載入法載 `gate` | 兩支互相載入,任一支被單獨載入時都要能活 —— **要驗四種載入順序** |
| 丙 | 開第三支小模組放共用常數 | 最乾淨,但多一個檔案要進 manifest、進安裝、進下游 |

**紅燈形狀(一句)**:斷言兩處取到的是**同一個物件**;在其中一處加一個目錄,另一處立刻看得到。

---

## 刀三 —— B-2:「依票號找票檔」三個實作

**受害符號**:`gate.ticket_untested_modules` / `status._find_ticket_file` / `mcp_server._ticket_path`。

**現況**:三個實作,票 111 已經把 `gate` 那一份的邊界補上,
所以**三份現在行為一致** —— 但仍然是三份。
`status._find_ticket_file` 的 docstring 還留著「`gate.py` 有語意相同的一份,**本票不修它**」
那一句,**而那句話現在過期了**(票 111 修了)。

**這一刀因此有兩半**:收成單一來源;**順手把那句過期的 docstring 改掉**
(`F-036`:舊文不刪,加註「票 111 已修」)。

**方向的坑**:`status.py` 與 `mcp_server.py` 是 `portable`,`gate.py` 是 `hooks`;
共用要嘛由 `gate` 提供、要嘛開第三支 —— 與刀二同一個問題,**兩刀的答案應該一致**。

**紅燈形狀(一句)**:三個呼叫點取到的是**同一個函式物件**;
改那一個函式的邊界規則,三處同時變。

---

## 裝新下游前要不要

**三件都要。**

- B-4:下游的 `sync` 與 `gate` 是同一組判定的兩端,分岔在下游更難查(沒有上游的對照)。
- B-3:`find_implementation` 決定下游紅燈紀錄的 `impl_file` ——
  下游的原始碼樹與上游不同,**重複 basename 在下游才會發生**(本 repo 實測 0 筆)。
- B-2:下游的票號慣例未必補零,三份實作在下游更容易走岔。
