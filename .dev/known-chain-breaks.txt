# 已知斷點白名單 —— `.dev/gate-exemptions.jsonl` 的鏈上,哪幾處「真的對不上」是對的。
#
# 格式:`<前一筆 result_hash> <後一筆 content_hash> <為什麼這一處是對的>`
#       三欄以空白分隔,**理由欄硬性** —— 少了它 `ledger_verify.known_breaks()` 直接拒絕。
#       `#` 開頭與空行是註解。
#
# ## 定位鍵為什麼是雜湊對,不是索引
#
# 索引會隨帳本增長而漂 —— 這份帳本每天都在長。**引身分,不引位置**
# (同票 114「引符號名不引行號」、票 116 B-8「記 ADR 編號不記路徑」)。
#
# ## 這張清單的由來(它有主詞了)
#
# 第六站 2026-08-24 首跑就寫下「帳本鏈已知斷點非缺陷;若帳本鏈驗證常態化,
# 需『已知斷點附理由』白名單」,**而它從未落地** —— `F-110` 的形狀:
# 規矩寫下來了,而沒有東西會因為它沒被做而叫。票 116 B-6 是那句話的主詞。
# **當時 1 處,落地時 4 處** —— 集合長大了而沒有人回頭看。
#
# ## 這張清單**不**影響退出碼
#
# `ledger_verify` 永遠回 0(見該檔 docstring 引的 `F-031`)。
# 這裡登記的效果是**讓報告把它標成「已登記」並印出理由**,
# 讓下一個讀報告的人不必重新查一次。
#
# ## 只作用在「真的對不上」那一桶
#
# 另外三桶不是給人登記的:設計上的斷與格式交界是構造決定的,
# 無從驗證是缺資料 —— 登記它們等於把「查不了」講成「查過了」。
#
# ## per-repo
#
# `.dev/` 標 `generate`,本檔不隨安裝出貨。每個 repo 的帳本有自己的歷史,
# 而別人的判斷不能替你的帳本背書。下游沒有這個檔 = 空白名單(fail-closed)。

# ── 以下四筆,2026-09-08 票 116 B-6 落地時逐筆查過 ──────────────────────────

# (57,58) ticket=19,兩筆都是 Edit,2026-08-13T06:39:33
# 機械證據:**後筆的 content_hash 等於前筆的 content_hash** ——
# 檔案在兩次紀錄之間從 result 回到了 content,也就是**被還原過**,
# 而那次還原不經前哨(Bash `git checkout` / 編輯器復原,依設計不記帳)。
# ⚠ 未查 git 歷史確認是哪一種還原;判準是帳本自己的資料,不是外部證據。
bdfff0b8199799a6c53472f4a03d3307cb22e2a04b425151c27a8d19319ffa19 36c3a116e593b9c5f1f6cd7a981aec957d772006dd3588d5dda786af18d2ac30 還原:後筆 content 等於前筆 content,檔案被還原後再編輯;還原不經前哨依設計不記帳(ticket 19,2026-08-13)

# (73,74) ticket=null -> 29,兩筆都是 Edit,2026-08-14T12:15:12
# 同上形狀:後筆 content == 前筆 content。
ce5d1aefb21788171b37279e967c8a3b38e667594bac20b435bfaf8f8fad6ab5 64cd7af39fcdeafd1becdc16ed9249e795923b68daaf439bf43f523211ed61f1 還原:後筆 content 等於前筆 content,檔案被還原後再編輯;還原不經前哨依設計不記帳(ticket 29,2026-08-14)

# (143,144) ticket=47,兩筆都是 Edit,2026-08-18T12:45:47
# **這一筆是 `ledger_verify.py` 自己 docstring 舉的那個例子**,逐字:
#     5  fa29d055 -> 795dff63     M4
#     6  54dabea0 -> adcc5fb1     M5    <- 第 5 筆結束於 795dff63,這裡卻從 54dabea0 開始
#     斷點來自那一次還原走了 `git checkout`(Bash 在 gate.py 早退,不經前哨,依設計不記帳)
# 與另外三筆不同:它回到的是**更早的一個狀態**,不是前筆的起點。
795dff638f7593b67fe18eaf58572760eba0489f0069d2732b070905519a00a2 54dabea0ff1e2a2cd6e8cbab7cfc5ae8a83608a76f3ee77ce9cd63908991adcd 票 47 批 3 那次還原走了 git checkout,不經前哨依設計不記帳 —— ledger_verify.py 的 docstring 逐字舉的就是這一處

# (156,157) ticket=76,前筆 Edit -> 後筆 pre-commit,2026-08-24T12:02:51
# 機械證據:後筆(pre-commit)的 content_hash 等於前筆的 content_hash ——
# **提交前檔案已經被還原回編輯前的狀態**,所以 pre-commit 看到的是原內容。
2293423e82b4bfbcfef96142e43b058c695eba63207dba1517747c8592dd4b9b 95f001e5e43d24a3cbd86e2f1de79413326e4e0982dfc5b8bebd0320064ea2b8 還原:提交前檔案被還原回編輯前狀態,pre-commit 看到的是原內容(ticket 76,2026-08-24)
