# 使用者層分類表 —— `~/.claude/` 底下的東西,哪些走匯出腳本、怎麼走。
#
# 格式:路徑<空白>標記。以 / 結尾的是目錄,涵蓋底下所有東西。最長前綴者勝
# (不是讀取順序 —— 順序決定的話換個排法就換個行為,那是隱形的)。
# 解析器與 `portable-manifest.txt` 共用同一支(manifest.load_table),
# 只換標記詞彙;「打錯字照樣吵」那條原封不動繼承。
#
# 標記:
#   export  腳本帶走
#   age     加密後才帶走(裸的祕密永遠不上任何雲端、不進任何 repo)
#   human   **腳本不碰** —— 人工複製 + sha 核對(R4)。G1 會擋腳本讀它們
#   never   永不經本腳本匯出
#
# 標記後面加 `!` = **機器相依,匯入後須人工覆核**(票 28)。
# 那是與「怎麼搬」**正交**的性質:`age!` 是「加密帶走,而且搬完要回頭看」。
# 塞進同一個欄位當第五個桶,就得為每個組合造一個值(age-review、human-review…),
# 那是笛卡兒積 —— 所以用旗標。
#
# ── 兩件必須先講清楚的事 ────────────────────────────────────────
#
# **一、沒有預設。未分類 = 拒絕整次匯出。**
# `portable-manifest.txt` 的預設是 copy,理由是它自己表頭寫的:
# 「多帶是吵鬧的、少帶是靜默的」。**這裡的不對稱是反過來的**:
#     多帶 -> 把憑證推上雲端,靜默而且不可逆
#     少帶 -> 換機器當場發現少東西,吵鬧
# 同一個預設值,搬到相反的不對稱上就變成缺陷。所以這裡選「沒有預設」。
#
# **二、`never` = 不走這條門,**不等於**不備份。**
# 這句放在表頭而不是只寫在票裡,因為讀這張表的人正是會問
# 「那它到底有沒有被備份」的那個人。每一條 never 都要註明它的備份通道,
# 沒有通道的才是真的沒備份。

# ── 框架必需的小設定(合計 < 20 KB,這幾條是換機器真正要帶的)──
upstream-roots.txt              export
# G1 掛載寫的是**絕對路徑**(`python "<家目錄>/.claude/hooks/g1_guard.py"`)。
# 原樣匯入 = 掛載指向一個不存在的路徑 = **G1 整層靜默失效**,
# 而檔案在、格式對、settings.json 讀得動(票 28 / 卡點 #2)。
settings.json                   export!
commands/                       export
# age 公鑰(recipient)。**公鑰不是祕密** —— 可以進版控、可以跟著匯出走,
# 新機器要加密自己的匯出時需要它。**私鑰不經本腳本、不經任何自動流程。**
# 形狀抄 upstream-roots.txt:一行、人維護、腳本唯讀。
age-recipient.txt               export

# ── 加密後才帶走 ─────────────────────────────────────────────
# 內容本身就是秘密(使用者名稱、往來機構、資料夾名),
# 明文進私有 repo 等於把「要防的東西」列成一張清單。
# **機器相依**:裡面全是這台機器的形狀(使用者名稱、資料夾名、往來對象)。
# 搬到新機器上一條都不會命中,而且**警告不響、pattern 編得起來、掃描回 0** ——
# 三個綠燈疊在一起而涵蓋率是零(票 28 / 卡點 #13)。
leak-patterns.local.txt         age!

# ── 腳本不碰:R4,人工 + sha ──────────────────────────────────
# 這兩個檔在 G1 保護清單裡,第一級無豁免、不分讀寫 —— 腳本去讀就會被擋。
# 它們必須**本來就不去讀**,而不是讀了以後決定不用。
# 步驟與 sha 核對指令寫在 docs/machine-init.md。
# 另外:保護清單是**照該機器實際存在的路徑**寫的,兩台機器本來就不該相同,
# 所以這一項連「複製」都要人工覆核,不是無腦覆蓋。
# **機器相依**:清單是照該機器實際存在的路徑寫的,兩台機器本來就不該相同。
# machine-init 第二節末已警告過「照桌機抄等於白列,而 g1_verify 會全綠」。
g1-protected.txt                human!
# 不帶 `!`:內容是一行 `SHADOW_MAX=<日期>`,兩台機器相同,搬過去不必逐條覆核。
shadow-clamp.txt                human

# ── 永不經本腳本匯出(每條都註明備份通道)────────────────────────
# 有效憑證。進了版控就留在歷史裡,而歷史改不掉。備份通道:重新登入。
.credentials.json               never
.claude.json                    never
backups/                        never
# 對話逐字稿(數百 MB)。**不經本腳本匯出;備份走獨立封存通道**
# (R3 裁決:帶日期 zip -> 雙雲端,週級)。
projects/                       never
# 編輯歷史。復原開發環境不需要它;要留的話走與 projects/ 同一條封存通道。
file-history/                   never
# 證據檔。TSI-002:代理人不清理、不還原、不搬移證據檔。
g1-trace.log                    never
# 執行期產物,可重建 —— 重裝就有,不必帶。
cache/                          never
debug/                          never
sessions/                       never
session-env/                    never
shell-snapshots/                never
ide/                            never
downloads/                      never
plugins/                        never
.last-cleanup                   never
.oauth_refresh.lock             never
# G1 hook 本體:正本在 repo 的 .claude/portable/g1_guard.py,
# 由 ADR 0009 的三步流程覆蓋過去,不走這條門。
hooks/                          never

# ── 2026-09-03 補登(票 87:匯出被拒,四項未分類)───────────────
# Claude Code App 的更新結果,不是框架的東西。備份通道:**不需要** ——
# 下一次更新就覆寫它,它描述的是那一次更新而不是這台機器的形狀。
.last-update-result.json        never
# Claude Code 的指令歷史 = 個人資料。復原開發環境不需要它;
# 要留的話走與 projects/ 同一條封存通道(帶日期 zip -> 雙雲端)。
history.jsonl                   never
# Claude Code 的貼上快取 = 個人資料(貼進去的東西原封不動躺在裡面)。
# **用目錄前綴**,因為裡面的檔名是流水號 —— 逐檔列等於下次多一個檔就又未分類。
# 備份通道:**沒有,而且不該有** —— 這一項就是不留。
paste-cache/                    never
# 2026-08-28 合併 pattern 時留下的備份副本。正本 leak-patterns.local.txt
# 已在 age 桶,內容由正本涵蓋 —— 這一份**多帶一次就是多一份明文外洩面**。
# 備份通道:正本(age 桶)。本項登記**待 Jeff 手刪**;刪掉之後這一行才能移除,
# 而不是反過來(先移除行,檔還在 = 下次匯出又被拒,那才是正確的行為)。
leak-patterns.local.txt.bak-20260828 never
