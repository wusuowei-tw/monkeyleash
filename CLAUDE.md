# monkeyleash(原 agent-gates)—— 六站流程閘門

<!-- FRAMEWORK:BEGIN -->

## 開發流程

**站別與順序的唯一定義:`.agents/pipeline-stages.yaml`(唯讀,不被任何流程寫入)。**
本檔不複製站名清單——要知道流程順序、每站對應哪個 skill、哪站可寫原始碼,一律讀該檔。

進入點都是顯式 slash command,流程細節在各 skill 內。
`/implement` 內部走 `/tdd`;`/improve-codebase-architecture` 為累積性檢查,非每票執行。

執行期狀態:`.dev/pipeline.json` 的 `current_stage`(+ `feature` / `ticket_id`)。
換新對話先讀它。合法值域由 `pipeline-stages.yaml` 的 `stages[].id` 決定。

## gate.py 強制契約

單一邏輯 `.claude/hooks/gate.py`,兩層呼叫:

- **pre-commit 為唯一權威判定** — `.git/hooks/pre-commit` → `gate.py --pre-commit`,擋 commit。
  **在已安裝的副本上**綁得住所有人(含非 Claude 的 agent 與人工 commit)。
  **未安裝的副本上這句不成立**:`.git/hooks/` 不進版控,clone 不會帶走它,
  而且完全靜默 —— 前哨照跑、測試照綠,沒有東西會說權威層不在。這是已知缺陷,不是留白。
- **agent hook 為前哨** — `.claude/settings.json` PreToolUse → `gate.py`,早點紅比 commit 才紅好。
  「繞過前哨仍會在 commit 被擋」**只在權威層已安裝時成立**;沒裝時前哨會直接說出這件事,
  並指出它自己涵蓋不到誰(clone 下來直接手動 commit 的人)。

規則(**本表不寫共幾條,也不寫成閉區間**。權威來源是 `gate.py` 的 `rule_codes()` ——
它從規則自己的擋下訊息掃出來,加一條規則就自動涵蓋。
寫死條數或結尾編號的話,下次加規則時不會有人記得回來改這裡,
而**表看起來仍然完整**。本表因此只求「列出的每一條都對」,不宣稱「列完了」):

| 規則 | 判定 |
|---|---|
| R1 | `docs/specs/**` 內容含 ``` / `def` / `import` / `function` → 擋 |
| R2 | `current_stage` 不是宣告 `allows_src_write` 的站時寫入原始碼目錄 → 擋 |
| R3 | 寫 `<name>.py` 但 `tests/test_<name>.py` 不存在 → 擋 |
| R4 | `.claude/skills/` 或 `skills/` 的 SKILL.md 與正典 `.agents/skills/` 不一致 → 擋 |
| R5 | 正典 `code-review` 缺第三軸掛載點(patch 未重套)→ 擋 |
| R6 | 紅燈豁免清單列入不在 go-live commit 樹裡的路徑 → 擋。**清單只減不增** —— 入場券是「存在於上線那一刻的樹裡」,而 agent 改不了 git 歷史(`docs/adr/0006`) |

被擋時不要繞過(改路徑、換工具、改 pipeline.json)。跳過流程由使用者自行修改 `.dev/pipeline.json` 的 `current_stage`。

## skills 更新

**唯一入口:`bash scripts/skills-update.sh`。不准直接跑 `npx skills update`** —— 它會靜默覆蓋正典裡的本地 patch。
wrapper 三步:更新 → 冪等重套 patch → gate.py 全規則驗證。R5 是這條規矩的機器保證。
正典為 `.agents/skills/`;`.claude/skills/` 與 `skills/` 是鏡像,已 gitignore。
**鏡像的實體形態由上游工具與檔案系統決定,不是本專案選的** —— 本機實測為檔案層硬連結
(目錄各自獨立、檔案是同一個實體檔),先前觀察過 symlink 佈局。R4 不得假設任一形態。
已知缺口:硬連結佈局下內容由構造保證相同,**R4 的內容比對不會觸發**,
它實際只守得住「檔案在不在」。見 `.scratch/portability/grill.md`。

閘門一律**黑名單、fail-closed**:列出不管的東西,其餘全管,新東西預設被守。
誤擋的處置是**把該目錄加進例外清單**(擋下訊息會直接給檔名與行號),
**不得退回白名單** —— 三次 fail-open 缺陷都源自白名單思維。見 `docs/adr/0003`。

**常駐檢查項:任何要進非原始碼清單的目錄,先問「它會不會裝著判定邏輯」。**
已經三次撞在同一個位置:`.claude` 整個在非原始碼清單、`find_implementation`
跳過點開頭目錄、`scripts/` 在非原始碼清單。共同根因是
**「基礎設施 = 特殊處理」這個習慣** —— 它在多數情境是對的(跳過 `.git` 省時間),
所以會被無意識套用;而閘門、hook、安裝器恰好全住在那些位置。
同理適用於任何「跳過某類路徑」的邏輯:先問它會不會跳過閘門自己。

**常駐檢查項:修好一個偵測器之後,回頭重掃既有資料。**
新規則的第一次全量掃描,問的不是「以後會不會再犯」,是「**以前犯過幾次**」。
規則變動之後,**舊世界不會自動重新受檢** —— 而已經推出去的東西不會等你想起來。
標本:個人 pattern 修好之後,新 pattern 回頭照出一個**已經推上遠端**的真實資料夾名
(F-082)。附帶判準:**遮一半比完全沒遮更容易過關** ——
完全沒遮會被掃描擋下,遮了一半剛好躲過比對,而人看到「有遮」就放心了。

**同一條的近距離版:命中修復後,搜尋半徑至少是「同檔同類」,再往外問跨檔家族。**
由近而遠 —— 最近的那一圈最容易被跳過,因為修的時候人就在那個檔案裡,
**「我剛剛看過這個檔案」感覺像是查過了**。
標本:F-082 修掉了某檔第 348 行的真實資料夾名,**同檔第 291 行的同類沒被修**,
隔 57 行、之後一直在遠端上(F-085)。
要緊的是為什麼上面那條沒接住它:**全量重掃依賴偵測器,而偵測器可能正是
打不中這一類的那個** —— 掃描回 0 不代表沒有。**同檔目視是唯一不依賴偵測器的那一圈。**

**最外圈:在註解或 log 裡命名一個缺陷族之後,立刻用那個名字全庫搜一次。**
命名是為了讓它可搜尋;**只命名不搜尋,等於把索引建好然後不查**。
查法是搜判準裡的**受害字串**(`skills/`、`/tmp`),不是搜「邊界」這個概念 ——
概念搜不到,字串搜得到。
標本:`g1_guard` 的 docstring 早已寫下「前綴要帶邊界…… 與 `skills/` 缺前導斜線
同一族」,而 `scanner.py` 的同一個洞原地不動了整整一段時間,
讓 39 個正典檔從未被掃過(F-086)。
**同一份判準寫在 A 模組的註解裡,不會讓 B 模組變安全 —— 註解不是機制。**

**報告裡的每一個數字都要帶單位。** 「39 blob + 27 tree」而不是「66 個」。
**算術錯會被看出來,單位錯不會**;而落地紀錄會被引用,
**引用的人不會回頭重驗**(F-086 併記)。

**常駐檢查項:動手守一個面之前,先問這一面的集合是開放還是封閉。**
封閉且可窮舉時,**枚舉勝過比對** —— 因為
**比對的漏是未知的,枚舉的漏是不存在的**。
**對封閉集合用 pattern 不是防線弱,是選錯工具**,加強那條 pattern 不會讓它變對。
封閉集合的例子:commit 身分、ref 名稱、檔案模式位元、副檔名、站別清單。
標本:172 筆身分欄位走完整掃描管線命中 0,而其中一筆是真名 + 私人信箱(F-087)。

同條判準句:**「沒有入口」與「我沒去造入口」是兩件事,前者聽起來像事實。**
用「無入口」描述一件**可以做而沒做**的事,會讓它看起來像**做不到**,
於是沒有人會再回來看它。

**常駐檢查項:收了一個入口,就回頭問它的同類入口在哪。**
上一條是資料側,這一條是規則側,合起來才完整。
標本:R7 收了 PowerShell 的寫入動詞(`Set-Content`、`Out-File`…),
**卻漏掉 `Remove-Item` 整個刪改家族**,而 G1 第二級刻意只管專案外 ——
兩層的空隙疊在一起,專案內刪檔同時穿過兩層(F-083)。
問法是「這個東西還有哪些同類」,不是「我還想得到什麼」——
**想不到不等於不存在**,要去列舉來源(語言的動詞表、API 的方法清單、副檔名標準)。

### 以下這一批的引言 —— **它是上面每一條的共同極限**

**推論鏈的品質保證不了前提;而前提越自然,越不會被驗。**
一整條每一步都成立的推論,可以從一個沒人檢查過的前提出發,
走完之後看起來像一次徹底的調查。
標本:「CI 紅了」→ 假設我方壞了 → 查改了哪個檔 → 查它走哪條路徑 → 逐一讀原始碼,
**每一步都對,而紅的是平台自己的報表產生失敗**(F-111)。

> **寫下一條判準,不會讓你在下一次認出它適用。**
> 判準是索引,而**「認出這次該查那個索引」本身沒有索引** ——
> 標本:F-109 寫下「對帳的人會去找一個不存在的缺陷」,
> 幾個小時後同一個 session 裡就發生了一次,而寫下它的人沒有認出來(F-111)。

**所以底下每一條的實際效力都比它讀起來低一階。** 它們擋得住
「想到要查的時候查錯方向」,擋不住「沒想到要查」。
**這一段寫在這裡不是為了謙虛,是為了讓下一個人不要把這份清單當成保險。**

> **併記(2026-08-19)**:F-111 那一次,**Claude 也沒有驗那個前提** ——
> 最後之所以走對,是因為**入口的問法碰巧是「哪一步紅的」而不是「為什麼會紅」**,
> 而那是運氣不是紀律。**碰巧對與驗過,在結果上長得一樣。**
> 記這一句是因為:一則講「前提沒被驗」的紀錄,若自己暗示作者驗過了,
> 它就變成本批最後那條(**識別風險的品質越高,它偽裝成處置的能力越強**)的實例。
>
> (**引名不引行號**:這句原本寫「本文件第 146 行」,而加完引言之後它變成第 171 行 ——
> **在同一次編輯裡就過期了**。行號是位置,標題是身分;F-109 的同一句話換一個場景。)

---

**常駐檢查項:驗收清單天生偏向意圖,而破壞來自意圖之外。**
把 A 併進 B 的動作要驗**兩個方向**,因為它們各自看不見對方的漏:
「A 的都到了嗎」證明你做了事,「**B 的都還在嗎**」證明你**只**做了那件事。
方向 A 的參照物是來源,方向 B 的參照物是目標的過去 ——
**參照物不同,盲區就不同,所以不是同一個檢查跑兩遍。**
只跑 A 是很自然的錯:A 對應「我這次要做的事」,所以讀起來像驗收;
而**沒打算做的事不會出現在待辦清單上,也就不會出現在驗收清單上**。
標本:區段交換式的合併把目標**別的區段**裡的一行刮掉,而方向 A 全綠(F-108)。
適用範圍不只檔案合併:清單合併、設定覆蓋、任何「以某一版為底再加東西」。

**常駐檢查項:說「構造保證」之前,先問這個保證是從方法推出來的,
還是從這次的資料看出來的。**
從資料看出來的那種,下一批資料就會推翻它 ——
而**「這次沒丟」與「這個方法不會丟」是兩句話,前者聽起來像後者**。
標本:「量化那一段是上游那一段的嚴格超集」當時為真,
但那是那一次資料的樣子,不是區段交換法的性質(F-108 併記)。
與「能用構造消掉的不要用紀律管」是同一組:**先確認那真的是構造。**

**常駐檢查項:這個數字會不會在我寫下它與別人讀到它之間改變?
會的話,基準跟數字一起寫。**
一個沒有標明基準的預測,過期之後**看起來像算錯** ——
而對帳的人會去找一個不存在的缺陷,方向還全是錯的(環境、隨機性、安裝)。
格式:`外層 894(以 <commit> 的 collected 910 為底)`;
基準與被對的 commit 不同就**先重算再談差異**。
標本:預測在 A commit 量、貼在 B commit,中間多了 10 條測試,
於是三格裡兩格同差 +10 而數檔案的那格全中(F-109)。
同族:桶檔數、清單長度、以及**觀測動作會自己推高的計數**(F-070)。

**常駐檢查項:「要留存」是祈使句,而祈使句沒有主詞。**
每寫下一句「X 要留存 / 要記得 / 不能當成一次性的」,立刻問
**「不做會有什麼東西叫?」** —— 答不出來的,就改寫成一張票,或當場做掉。
**祈使句要嘛有主詞,要嘛有機制;兩個都沒有就是一句安慰。**
這是 F-086 家族更難察覺的那一型:F-086 是判準寫在**別的地方**,修法是搬過去;
本型是判準寫在**正確的地方、正確的時點**,而**搬到哪裡都沒用** ——
它需要的不是位置,是執行者。
標本:ADR 寫下「建議清單要留存,不能當成一次性的中間產物」,
而逐筆答案仍然永久遺失,5 筆對不回去(F-110)。

**常駐檢查項:會寫入證據檔的批次套用,摘要必須由裁決者本人核准。**
**代理人與助手都不得代核准** —— 包含 agent 自己、包含轉述、包含
「上一則訊息裡看起來像核准的那句話」。

摘要**必須包含判準本身,不只計數** ——
讓核准者判的是**判準**,不是一個他無從驗證的數字。
只給「130 筆」,核准的是一個總和;給「訊息自述切不出目標所以擋 ⇒ 算照設計動作的成本,
不算誤判」,核准的才是那條規則。

> **一個無從反駁的摘要,收到的核准也是無效的** ——
> 它證明的只是對方按了同意,**不是對方同意了什麼**。
>
> 這一句適用範圍不只批次套用:任何要人點頭的東西
> (計畫、遷移、刪除清單、規格),**只給結論不給判準,拿到的就是一個空的同意**。
> 而空的同意事後**看起來與真的同意一模一樣** —— 那正是它危險的地方。

> **這條規矩存在的理由是:承諾沒有主詞也沒有機制,寫進來才跨得過 session。**
> 分工講好了「產摘要 → 裁決者核准 → 套用」,而那句話住在對話裡,
> 而**對話會結束**(F-112)。

**要緊的機制細節**:agent 收到的核准是**一段文字**,而文字**不帶身分** ——
從 agent 這一側**看不出**它來自裁決者本人還是代理人。
所以這條規矩擋不住「代簽」這個動作本身;它擋的是
**「沒有人記得該問」**:寫在這裡,下一個 session 的 agent 至少會知道要問一句
「這是裁決者本人的核准嗎」。**那句問話就是目前唯一的機制。**

**常駐檢查項:不要拿一個沒發生的機制,去解釋一個發生了的錯。**
那會讓紀錄**看起來更有結構**,而下一個讀的人會學到一條**在這裡不適用**的規則。

判法:一個候選解釋提出來之後,問**「它預測的東西,在這一次真的出現了嗎」** ——
沒出現就不是它,即使那條規則本身完全正確。
**正確的規則與正確的解釋是兩件事**;把前者當後者用,等於用一個真命題去掩蓋一個真錯誤。

標本:巢狀測試數預測 913 實測 908,提出的解釋是
「外層加數與巢狀加數的持有者不同」—— **那條規則對**,
但實測兩支測試檔都標 `copy`、兩個加數都是 14,**所以它沒有發生**;
真正的成因是取用時抓錯了相鄰的數字(F-113)。

**這一條與那 72 筆「留白比蓋章誠實」是同一句話,換到紀錄面。**
(**出處**:票 67 / 票 68 —— R7 影子日誌 202 筆裡有 72 筆從日誌本身**判不出來**,
因為當時沒有記下指令。裁決是給它們一個獨立的「無法判定」桶,
**而不是併進「可轉正」蓋章** —— 全部標成無法判定的話,R7 會變成
「已判定 202 / 202,假陽率 0.0%」而過關,那是一個由留白換來的綠燈。)

**常駐檢查項:識別風險的品質越高,它偽裝成處置的能力越強。**
一句準確描述風險的話,會讓讀的人(**包括寫的人**)產生「這件事被看到了」的感覺,
而**被看到與被處理之間沒有任何東西**。
所以讀到一段寫得很好的風險描述時,要問的不是「這說得對不對」,
而是**「除了這段文字之外,還有什麼在管它?」**——
沒有的話,那段文字的品質正是它至今沒被處理的原因。

## G1(檔案系統災難防護)

獨立於六站流程,不進 R 系列編號 —— R 系列是流程規則,**性質上**各自獨立、可分開裝;
災難防護則任何專案都該開。
**注意這是性質描述,不是功能宣告:目前沒有 per-repo 開關,R 系列全部無條件生效。**
(**刻意不寫成閉區間** —— 寫死一個結尾編號會讓讀的人認定它後面那一條不存在,
而那一條存在:R8 在下游擋過人。條列見 `gate.py` 的 `rule_codes()`。)
掛在使用者層 `~/.claude/settings.json`,邏輯在 `~/.claude/hooks/g1_guard.py`,
保護清單是獨立純文字檔 `~/.claude/g1-protected.txt`。

**G1 把自己與保護清單都列在保護清單裡,所以 agent 改不動它。**
要改它:草稿放專案內、跑完 `.claude/portable/g1_verify.py` 的**全套**驗收、附 diff,**覆蓋那一步只有人能做**。
**條數刻意不寫在這裡** —— 案例是執行時從實際保護清單生成的(該檔 docstring:
「案例是**執行時從實際清單生成的**,不是寫死在這裡」),清單長出一條,驗收就自動多一條。
寫一個數字在這裡等於保證它過期,而**過期時沒有東西會說話**。
完整流程見 `docs/adr/0009`。不得寫腳本代勞那一步 —— 那會讓保護消失。

## Code review 的分流判準

發現問題時要問的不是「要不要改」,而是「**不改會不會讓別的規則失效**」。
前者可以擱置,後者擱置就是把已偵測到的洞留在原地。見 `docs/adr/0003`。

## 重構歸屬

- **跨越本票宣告接縫(seam)的重構** → 不在紅綠燈迴圈內做,留給 `/code-review` 與 `/improve-codebase-architecture`
- **不跨接縫的重構**(改名、抽本地 helper、消除剛製造的重複)→ 留在紅綠燈迴圈內

## 回報格式

**每次回報兩段,順序固定。** 讀回報的有兩種人:**做判斷的人**(不寫程式,靠讀證據決定下一步)
與**逐字核對的助手**。一種密度服務不了兩種人 —— 為後者寫的密度會讓前者
**事後才知道發生過什麼**,那時代價已經付了。

**這是增加,不是取代。** 底下「不可切換的核心」那節列的紀律一條都不動。

### 第一段【給裁決者】

白話,**硬上限 5 行**。四件事:做了什麼、卡在哪、要你決定什麼、不決定會怎樣。

超過 5 行就刪到 5 行,**不得把證據段的內容搬上來重講**來填。重講不會讓人更懂;
它製造的是兩段同一件事的兩個版本,而**兩個版本不一致時沒有人知道哪一個是真的**。

**術語第一次出現時,括號內用生活化比喻解釋一次**
(例:hash(指紋,內容變一個字就不同))。之後同一個術語不再解釋 ——
重複解釋會把 5 行的額度吃光,而額度是這一段唯一的紀律。

**白話段不得出現證據段沒有的東西。** 它是摘要,不是新宣稱。
反向也成立:白話段講了什麼,證據段就要拿得出對應的原始資料。

### 第二段【給裁決助手】

原始證據,**密度不減**:raw stdout、雜湊、退出碼、行號、原始擋下訊息,一個字都不刪。
這一段的讀者會逐字核對,壓縮它等於把判斷依據丟掉 ——
而丟掉的東西**看起來跟沒丟一樣**,直到有人要回去查。

### 要裁決的事怎麼寫

**寫成「A 還是 B、差別是什麼」,不丟開放題。**
開放題把「盤點選項」推回給裁決者,而盤點正是回報方該做完的那一半。

**真有三個以上的選項就照列**,逐項標出建議與各自的代價。
**不得為了湊成二選一而藏掉選項** —— 藏掉的那個不會消失,
它會在事情做完之後才被想起來。

### 不可切換的核心

以上講的是**怎麼說**。**說什麼**的紀律完全不變,且不隨任何顯示設定改動:

- 被擋即停,不繞過(不改路徑、不換工具、不改流程狀態檔)
- 附**原始**擋下訊息,不轉述、不摘要(與個人 token 相撞時的唯一處置見 `F-116` —— 遮了就不得再稱原始)
- 不用「已修正」「已確認」這類宣稱句 —— 要說做完,就附上讓人自己看得出來的證據

把這三條做成可切換,等於讓「換一個回報樣式」變成「靜默關掉安全網」,
而那是 fail-open。所以它們寫在這裡,不寫在任何可切換的樣式設定裡。

## Agent skills

### Issue tracker

Local markdown under `.scratch/<feature>/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.

### Friction log 發號

`docs/agents/friction-log.md` 在 portable-manifest 裡標 `copy`,**整份跟著裝進每個 repo**,
所以編號不是本地的事。`F-` 屬於框架(monkeyleash 發號);
**每個安裝的 repo 用自己的三字母前綴,且前綴進號碼本身**(`TSA-001`,不是「F-001(影音)」)。
專案發現的框架層事實回 monkeyleash 開票,落地才給 `F-` 號;專案那則不刪不改號,原地加一行指過去。
判準:**這一則搬到另一個專案還成立嗎?** 成立就是框架層。完整規則寫在 friction-log.md 開頭。

<!-- FRAMEWORK:END -->

## 這個專案自己的規範

(還沒有。寫在這一段裡的東西**不會**被帶進別的專案 ——
框架規範寫在上面那對 FRAMEWORK 界線之間,只有那一段會跟著走。)
