# 由 skills 工具重建的鏡像。
#
# **前導斜線是必要的,不是風格。** 寫成 `skills/` 的話,gitignore 會在
# **任何深度**比對同名目錄 —— 於是 `.agents/skills/`(正典)也被排除,
# 而 git 對 ignored 檔案依定義是靜默的:首次 commit 少了 34 個檔案,
# 沒有任何訊息,要自己去數 41 vs 75 才看得到。
# R4 存在的目的正是守住正典與鏡像的一致,結果正典被 .gitignore 吃掉了。
/.claude/skills/
/skills/

# 產生物
__pycache__/
.pytest_cache/
.cache/
*.pyc

# `.dev/` 分軌(票 31,2026-08-14 裁決)
#
# **舊理由已過期,照 F-036 保留在此**:原本寫「安裝目標 repo 才有的狀態與證據
# —— 本 repo 是發布來源,不是工作 repo」。那句曾經自洽,現在不成立:
# agent-gates 有自己的四十幾個 commit 與九百多筆測試紀錄,**它就是工作 repo**;
# 票 26 更證明它從未被 install.py 裝過,所以它從頭到尾**只**是工作 repo。
# 而下游(台股資訊收集、影音)反而都把 .dev/ 進版控 —— 上游對自己的紀律低於下游。
#
# 新分軌的判準是**可審計性 vs 流量**,而兩邊各自有一條存續管道:
#   帳本類  進版控   豁免史必須可逐筆對帳(票 24 的精神);不進版控的帳本對不了帳
#   高流量  不進版控 每跑一次測試就長,進版控會讓每次測試都髒工作樹;
#                    **存續歸 R5**(週級 zip 異地備份,已有第一份)
#   執行期狀態 不進版控 每台機器不同
/.dev/*
!/.dev/gate-exemptions.jsonl
!/.dev/provenance.jsonl

# 紅燈豁免清單:manifest 標 generate,**綁死產生它的那個 repo**(go-live sha +
# 路徑清單)。跟著發布來源走的話,下游拿到一個在自己 repo 不存在的 commit 去驗
# 每一筆,check_legacy_list fail-closed → 目標 repo 的每一次 commit 當場全擋。
# 與 /.dev/ 同一類:per-repo 狀態,不是框架內容。
/.agents/legacy-no-redlight.txt

# 工作階段暫存(票已搬進版控的 docs/tickets/;.scratch 只留真正的暫存物)
/.scratch/
