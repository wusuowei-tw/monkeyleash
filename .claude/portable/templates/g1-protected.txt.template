# G1 保護清單 —— 第一級:出現在指令字串裡就擋,無豁免。
#
# 【這是範本】安裝時複製到 ~/.claude/g1-protected.txt,然後**由人**填上實際路徑。
# 範本裡沒有任何真實路徑 —— 路徑是每個人自己的,判準才是通用的。
#
# ─────────────────────────────────────────────────────────────
# 排序理由是【不對稱】,不是價值高低
# ─────────────────────────────────────────────────────────────
#
#   原檔被刪、備份還在  ->  復原得回來
#   備份被刪、原檔還在  ->  **你不會立刻發現**,真需要時才知道沒了
#
# 第二種是靜默的。**所以備份排第一**,不是因為備份比原檔重要,
# 是因為備份出事時沒有任何訊號。
#
# 成本近乎零:日常工作不會碰這些目錄,多列一條不增加誤擋 —— 寧可多列。
#
# ─────────────────────────────────────────────────────────────
# 比對方式:帶邊界的前綴
# ─────────────────────────────────────────────────────────────
#
# 寫 D:\<目錄> 就涵蓋 D:\<目錄>\2023\x.jpg(子目錄自動涵蓋),
# 但**不會**誤中 D:\<目錄>2 這種相鄰名稱。
# 四種寫法都認得:D:\x、D:/x、d:\x、/d/x(git bash)。
#
# ─────────────────────────────────────────────────────────────
# 【清單自己必須在清單裡】
# ─────────────────────────────────────────────────────────────
#
# 最後兩行不是筆誤。少了它們,agent 可以先改清單再刪東西 ——
# 與「豁免來源不可自我服務」是同一個遞迴,終點是人的審查。
# 要改清單,人自己開編輯器改(見 docs/adr/0009)。

# ── 1. 備份(最高優先:被刪是靜默的)────────────────────────
# 把你的備份目錄寫在這裡,例如:
#   C:\Users\<你>\<雲端同步資料夾>\<備份>
#   D:\<備份目錄>

# ── 2. 不可重製的原始資料 ──────────────────────────────────
# 判準:重新產生得出來嗎?
#   重新下載/重跑就有 -> 不用列(慢不等於不可重製)
#   憑證、照片、手工整理的資料、沒有 remote 的 repo -> 列
#
# 一個實務上的排序方法:對每個目錄問「刪掉之後我要花多久才會發現」,
# 越久的排越前面。

# ── 3. 防護機制自己(這兩行照填,把 <你> 換掉)──────────────
# C:\Users\<你>\.claude\g1-protected.txt
# C:\Users\<你>\.claude\hooks\g1_guard.py
